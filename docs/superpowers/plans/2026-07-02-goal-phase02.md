# Goal Phase 2 实现计划(趋势曲线)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** goal 详情页趋势曲线 — server `GetGoalProgressHistory` RPC 读 `goal_progress_snapshot` 范围 + Flutter `fl_chart` 画 progress 趋势(替 Phase 1 占位)。

**Architecture:** server:snapshot repo 加 `FindSnapshotRange(goal_id, from, to)`(读 ent `goal_progress_snapshot`)+ application `GetGoalProgressHistory` + proto `GetGoalProgressHistory` RPC + handler。Flutter:`GoalRemoteDataSource.getProgressHistory` + `GoalProgressPoint` entity + GoalDetailPage `fl_chart` LineChart(照 holding perf curve 1.x API)。

**Tech Stack:** Go(ent + grpc)+ proto3 + Flutter(fl_chart 1.2.0 + flutter_bloc)

**Spec:** [docs/superpowers/specs/2026-07-02-goal-module-design.md](../specs/2026-07-02-goal-module-design.md) §10 Phase 2

## Global Constraints

- **分支**:`holding-asset-management`
- **goal_progress_snapshot 表已建**(Phase 0+1 Task 2 ent schema:tenant_id + goal_id + snapshot_date + current_amount_cents + Unique(tenant_id, goal_id, snapshot_date))
- **WriteSnapshot 已实现**(Phase 0+1 Task 6,goal_repo.go:238,delete-then-insert upsert;scheduler SyncAllGoals 每日写)
- **goal proto 无 GetGoalProgressHistory**(Phase 0+1 Task 7 未加,本 Phase 加)
- **fl_chart 1.2.0**(1.x API:公开 `LineChart`/`PieChart` 类、`LineChartBarData.color` 单数、`.withValues(alpha:)`;照 holding perf curve 范式)
- **English structured logs**(slog)
- **proto regen**:Go `buf generate`(无网 fallback 本地 protoc 手修 package)+ Dart `make gen-dart`(protoc_plugin 25.0.0)
- **复用第一**:snapshot repo 照 holding_snapshot 读模式;fl_chart 照 holding perf curve
- **每 task commit**(中文 conventional `feat(holding-goal-...): ...`)

## File Structure

### server 修改
- `yucai/server/internal/goal/adapter/driven/repository/goal_repo.go` — `FindSnapshotRange(ctx, tenantID, goalID, from, to)`(读 ent GoalProgressSnapshot)
- `yucai/server/internal/goal/domain/repository.go` — GoalRepository interface 加 FindSnapshotRange
- `yucai/server/internal/goal/application/service.go` — `GetGoalProgressHistory(ctx, tenantID, goalID, from, to)`
- `yucai/server/internal/goal/application/dto.go` — ProgressPoint DTO
- `yucai/server/internal/goal/adapter/driving/grpc/goal_handler.go` — GetGoalProgressHistory handler
- `yucai/proto/goal/v1/goal.proto` — GetGoalProgressHistory RPC + messages

### Flutter 修改
- `yucai/client/lib/goal/data/goal_remote_ds.dart` — getProgressHistory + mapper
- `yucai/client/lib/goal/domain/entities/goal_entity.dart` — GoalProgressPoint
- `yucai/client/lib/goal/presentation/pages/goal_detail_page.dart` — 趋势曲线(fl_chart,替 Phase 1 占位)

---

## Task 0: 前置(确认 + 清工作区)

**Files:** 无改动(验证)

- [ ] **Step 1: 确认 snapshot 表 + WriteSnapshot + fl_chart + holding perf curve**

```bash
grep -n "goal_progress_snapshot\|GoalProgressSnapshot" yucai/server/internal/goal/adapter/driven/repository/goal_repo.go | head -3
grep -n "fl_chart" yucai/client/pubspec.yaml
find yucai/client/lib/holding -name "*curve*" -o -name "*perf_curve*"
grep -n "GetGoalProgressHistory" yucai/proto/goal/v1/goal.proto  # 应无(Phase 2 加)
```
Expected:WriteSnapshot 在 goal_repo.go;fl_chart 1.2.0 在 pubspec;holding perf curve widget 存在(照范式);proto 无 GetGoalProgressHistory。

- [ ] **Step 2: 工作区干净 + build 绿**

```bash
git status --short
cd yucai/server && go build ./...
```
Expected:干净 + build 绿。记 HEAD(Phase 2 起点)。

(无 commit)

---

## Task 1: server GetGoalProgressHistory(snapshot 读 + RPC + handler)

**Files:**
- Modify: `yucai/server/internal/goal/adapter/driven/repository/goal_repo.go`(FindSnapshotRange)
- Modify: `yucai/server/internal/goal/domain/repository.go`(interface)
- Modify: `yucai/server/internal/goal/application/service.go`(GetGoalProgressHistory)
- Modify: `yucai/server/internal/goal/application/dto.go`(ProgressPointDTO)
- Modify: `yucai/server/internal/goal/adapter/driving/grpc/goal_handler.go`(handler)
- Modify: `yucai/proto/goal/v1/goal.proto`(RPC + messages)
- Test: `yucai/server/internal/goal/application/service_test.go`

**Interfaces:**
- Produces:`FindSnapshotRange(ctx, tenantID, goalID, from, to time.Time) ([]ProgressPoint, error)` + `Service.GetGoalProgressHistory(ctx, tenantID, goalID, from, to) ([]ProgressPointDTO, error)` + proto `GetGoalProgressHistory(GetGoalProgressHistoryRequest{goal_id, from, to}) returns (GetGoalProgressHistoryResponse{repeated ProgressPoint})`。Task 2/3 消费。

**背景**:snapshot 表(Task 2 建)+ WriteSnapshot(Task 6 写,scheduler 每日)。Phase 2 加读(RPC 返范围)。

- [ ] **Step 1: 写失败测试(service_test,mock repo FindSnapshotRange)**

```go
func TestGetGoalProgressHistory(t *testing.T) {
	repo := &fakeGoalRepo{snapshots: []domain.ProgressPoint{
		{Date: time.Date(2026, 7, 1, 0, 0, 0, 0, time.UTC), CurrentAmountCents: 100000},
		{Date: time.Date(2026, 7, 2, 0, 0, 0, 0, time.UTC), CurrentAmountCents: 200000},
	}}
	svc := application.NewService(repo)
	from := time.Date(2026, 7, 1, 0, 0, 0, 0, time.UTC)
	to := time.Date(2026, 7, 31, 0, 0, 0, 0, time.UTC)
	pts, err := svc.GetGoalProgressHistory(context.Background(), uuid.New(), uuid.New(), from, to)
	if err != nil || len(pts) != 2 || pts[0].CurrentAmountCents != 100000 {
		t.Fatalf("history: pts=%v err=%v", pts, err)
	}
}
```
(实现者读 service_test.go 现有 fake repo 模式调整;domain.ProgressPoint 新值对象)

- [ ] **Step 2: 跑测试验证 FAIL**

```bash
cd yucai/server && go test ./internal/goal/application/ -run GetGoalProgressHistory -v -count=1
```
Expected:FAIL(GetGoalProgressHistory undefined)。

- [ ] **Step 3: domain ProgressPoint + repository.go interface**

`domain/entity.go` 加:
```go
// ProgressPoint 是 goal progress 历史的一个点(每日 snapshot)。
type ProgressPoint struct {
	Date                time.Time
	CurrentAmountCents  int64
}
```
`domain/repository.go` GoalRepository interface 加:
```go
// FindSnapshotRange returns progress snapshots for goalID in [from, to], ordered by date asc.
FindSnapshotRange(ctx context.Context, tenantID, goalID uuid.UUID, from, to time.Time) ([]ProgressPoint, error)
```

- [ ] **Step 4: repo FindSnapshotRange(goal_repo.go,读 ent GoalProgressSnapshot)**

```go
// FindSnapshotRange returns progress snapshots for goalID in [from, to] (snapshot_date
// between from and to inclusive), ordered by date asc。Phase 2 趋势曲线数据源。
func (r *GoalRepository) FindSnapshotRange(ctx context.Context, tenantID, goalID uuid.UUID, from, to time.Time) ([]domain.ProgressPoint, error) {
	snapshots, err := r.client.GoalProgressSnapshot.Query().
		Where(
			goalprogresssnapshot.TenantIDEQ(tenantID),
			goalprogresssnapshot.GoalIDEQ(goalID),
			goalprogresssnapshot.SnapshotDateGTE(from),
			goalprogresssnapshot.SnapshotDateLTE(to),
		).
		Order(ent.Asc(goalprogresssnapshot.FieldSnapshotDate)).
		All(ctx)
	if err != nil {
		return nil, fmt.Errorf("find snapshot range: %w", err)
	}
	pts := make([]domain.ProgressPoint, len(snapshots))
	for i, s := range snapshots {
		pts[i] = domain.ProgressPoint{Date: s.SnapshotDate, CurrentAmountCents: s.CurrentAmountCents}
	}
	return pts, nil
}
```
(实现者读 goal_repo.go 现有 ent import + goalprogresssnapshot predicate 命名调整;照 holding_snapshot 读模式)

- [ ] **Step 5: application GetGoalProgressHistory + dto**

`application/dto.go` 加:
```go
type ProgressPointDTO struct {
	Date                time.Time
	CurrentAmountCents  int64
}
```
`application/service.go` 加:
```go
// GetGoalProgressHistory returns progress snapshots for goalID in [from, to]。
func (s *Service) GetGoalProgressHistory(ctx context.Context, tenantID, goalID uuid.UUID, from, to time.Time) ([]ProgressPointDTO, error) {
	pts, err := s.repo.FindSnapshotRange(ctx, tenantID, goalID, from, to)
	if err != nil {
		return nil, fmt.Errorf("goal progress history: %w", err)
	}
	dtos := make([]ProgressPointDTO, len(pts))
	for i, p := range pts {
		dtos[i] = ProgressPointDTO{Date: p.Date, CurrentAmountCents: p.CurrentAmountCents}
	}
	return dtos, nil
}
```

- [ ] **Step 6: proto GetGoalProgressHistory RPC + messages**

`goal.proto` service 加 `rpc GetGoalProgressHistory(GetGoalProgressHistoryRequest) returns (GetGoalProgressHistoryResponse);`,messages:
```proto
message GetGoalProgressHistoryRequest {
  string goal_id = 1;
  google.protobuf.Timestamp from = 2;
  google.protobuf.Timestamp to = 3;
}
message ProgressPoint {
  google.protobuf.Timestamp date = 1;
  int64 current_amount_cents = 2;
}
message GetGoalProgressHistoryResponse {
  repeated ProgressPoint points = 1;
}
```

- [ ] **Step 7: handler GetGoalProgressHistory(goal_handler.go,照现有 handler 模式)**

```go
func (h *GoalHandler) GetGoalProgressHistory(ctx context.Context, req *pb.GetGoalProgressHistoryRequest) (*pb.GetGoalProgressHistoryResponse, error) {
	tenantID, err := getTenantID(ctx)
	if err != nil {
		return nil, status.Error(codes.Unauthenticated, err.Error())
	}
	goalID, err := uuid.Parse(req.GetGoalId())
	if err != nil {
		return nil, status.Error(codes.InvalidArgument, "invalid goal_id")
	}
	from := req.GetFrom().AsTime()
	to := req.GetTo().AsTime()
	pts, err := h.service.GetGoalProgressHistory(ctx, tenantID, goalID, from, to)
	if err != nil {
		return nil, status.Error(codes.Internal, err.Error())
	}
	pbPts := make([]*pb.ProgressPoint, len(pts))
	for i, p := range pts {
		pbPts[i] = &pb.ProgressPoint{Date: timestamppb.New(p.Date), CurrentAmountCents: p.CurrentAmountCents}
	}
	return &pb.GetGoalProgressHistoryResponse{Points: pbPts}, nil
}
```
(替换 Unimplemented 默认,照 D-goal Task 7 handler 模式;import timestamppb)

- [ ] **Step 8: regen stub + build + test**

```bash
cd yucai/server && buf generate --template buf.gen.go.yaml  # 无网 fallback protoc + 手修 package
cd /e/projects/syfinance/yucai && make gen-dart  # protoc_plugin 25.0.0
cd yucai/server && go test ./internal/goal/... -count=1 && go build ./...
```
Expected:stub regen + 测绿 + build 绿。fakeGoalRepo 加 FindSnapshotRange stub(若 fake 实现接口)。

- [ ] **Step 9: Commit**

```bash
git add yucai/proto/goal/ yucai/server/internal/goal/ yucai/server/internal/proto/goal/ yucai/client/lib/proto/goal/
git commit -m "feat(holding-goal-server): GetGoalProgressHistory RPC + snapshot FindSnapshotRange(Task 1)"
```

---

## Task 2: Flutter data(getProgressHistory + GoalProgressPoint)

**Files:**
- Modify: `yucai/client/lib/goal/domain/entities/goal_entity.dart`(GoalProgressPoint)
- Modify: `yucai/client/lib/goal/data/goal_remote_ds.dart`(getProgressHistory + mapper)
- Modify: `yucai/client/lib/goal/domain/repositories/goal_repository.dart`(abstract)
- Modify: `yucai/client/lib/goal/data/goal_repository_impl.dart`(impl)
- Test: `yucai/client/test/goal/data/goal_remote_ds_test.dart`

**Interfaces:**
- Consumes:Task 1 proto GetGoalProgressHistory + ProgressPoint。
- Produces:`GoalProgressPoint{date, currentAmountCents}` + `GoalRepository.getProgressHistory(goalId, from, to)` + ds mapper。Task 3 用。

**背景**:照 goal Task 10 data 范式(GrpcClient+AuthRetryCaller)。mapper ProgressPoint(proto Timestamp→DateTime, Int64→int)。

- [ ] **Step 1: GoalProgressPoint entity**

`goal_entity.dart` 加:
```dart
class GoalProgressPoint extends Equatable {
  const GoalProgressPoint({required this.date, required this.currentAmountCents});
  final DateTime date;
  final int currentAmountCents;
  @override
  List<Object?> get props => [date, currentAmountCents];
}
```

- [ ] **Step 2: GoalRemoteDataSource.getProgressHistory + mapper**

`goal_remote_ds.dart` 加(照现有 RPC 方法范式,_retry 包):
```dart
Future<List<GoalProgressPoint>> getProgressHistory({
  required String goalId,
  required DateTime from,
  required DateTime to,
}) async {
  final resp = await _retry.call(() async {
    final req = pb.GetGoalProgressHistoryRequest(
      goalId: goalId,
      from: Timestamp.fromDateTime(from),
      to: Timestamp.fromDateTime(to),
    );
    return _client.getGoalProgressHistory(req);
  });
  return resp.points.map(progressPointToView).toList();
}

GoalProgressPoint progressPointToView(pb.ProgressPoint p) => GoalProgressPoint(
  date: p.date.toDateTime(),
  currentAmountCents: p.currentAmountCents.toInt(),
);
```
(import protobuf Timestamp;照 goal ds 现有 _retry + mapper 模式)

- [ ] **Step 3: Repository abstract + impl**

`goal_repository.dart` 加 `Future<List<GoalProgressPoint>> getProgressHistory({required String goalId, required DateTime from, required DateTime to});`。`goal_repository_impl.dart` 委托 ds + _guard。

- [ ] **Step 4: 测(mapper + ds constructibility,照 goal Task 10 测)**

`test/goal/data/goal_remote_ds_test.dart` 加:progressPointToView(Timestamp→DateTime, Int64→int)mapper 测。

- [ ] **Step 5: analyze + test + commit**

```bash
cd yucai/client && flutter test test/goal/data/ && flutter analyze lib/goal test/goal
git add yucai/client/lib/goal/ yucai/client/test/goal/
git commit -m "feat(holding-goal-flutter): data 层 getProgressHistory + GoalProgressPoint(Task 2)"
```

---

## Task 3: Flutter GoalDetailPage 趋势曲线(fl_chart)

**Files:**
- Modify: `yucai/client/lib/goal/presentation/pages/goal_detail_page.dart`(趋势曲线区,替 Phase 1 占位)
- Test: `yucai/client/test/goal/presentation/pages/goal_detail_page_test.dart`

**Interfaces:**
- Consumes:Task 2 getProgressHistory + GoalProgressPoint。Task 1 RPC。
- Produces:GoalDetailPage 趋势曲线(fl_chart LineChart)。

**背景**:照 holding perf curve(`lib/holding/presentation/pages/performance_page.dart` + perf curve widget)fl_chart 1.x API。替 Phase 1 占位("趋势曲线 Phase 2")。GoalDetailPage initState 加载 history(FutureBuilder)+ fl_chart 画。

- [ ] **Step 1: 读 holding perf curve 范式**

读 `lib/holding/presentation/pages/performance_page.dart`(fl_chart LineChart 用法 1.x API:LineChart/LineChartBarData.color/.withValues/LineChartData)+ holding perf curve widget(若有独立 widget)。照 fl_chart 1.x API(非 0.69)。

- [ ] **Step 2: GoalDetailPage 趋势曲线区(替占位)**

`goal_detail_page.dart`:趋势区改 FutureBuilder(getProgressHistory(goalId, from=now-30d, to=now))→ fl_chart LineChart(points: current_amount_cents over date)。御财金曲线 + 基线(target)。loading/error/empty 态。

```dart
// 替 Phase 1 占位 Text('趋势曲线 Phase 2'):
FutureBuilder<List<GoalProgressPoint>>(
  future: _historyFuture,  // initState: _repo.getProgressHistory(goalId, from, to)
  builder: (ctx, snap) {
    if (snap.connectionState != ConnectionState.done) return const SizedBox(height: 120, child: Center(child: CircularProgressIndicator()));
    final pts = snap.data ?? const [];
    if (pts.isEmpty) return const _HistoryEmpty();  // '暂无趋势数据(scheduler 每日记录)'
    return _TrendChart(points: pts, targetCents: goal.targetAmountCents);  // fl_chart LineChart,御财金 + target 基线
  },
)
```
(_TrendChart:fl_chart LineChart,照 holding perf curve 1.x API;X=date,Y=current;target 虚线基线)

- [ ] **Step 3: 测(趋势区渲染,照 goal detail 测)**

`test/goal/presentation/pages/goal_detail_page_test.dart` 加:mock repo getProgressHistory(seed 3 points)+ 验 fl_chart 渲染(LineChart found + empty 态 + loading)。fl_chart widget 测(find.byType(LineChart) 或 find text)。

- [ ] **Step 4: analyze + test + commit**

```bash
cd yucai/client && flutter test test/goal/presentation/pages/goal_detail_page_test.dart && flutter analyze lib/goal test/goal
git add yucai/client/lib/goal/presentation/pages/goal_detail_page.dart yucai/client/test/goal/presentation/pages/goal_detail_page_test.dart
git commit -m "feat(holding-goal-flutter): GoalDetailPage 趋势曲线 fl_chart(替 Phase 1 占位,Task 3)"
```

---

## Task 4: 全链路 + final review

**Files:** 无新改动(验证 + review)

- [ ] **Step 1: server 全量测 + build**

```bash
cd yucai/server && go test ./... -count=1 && go build ./...
```
Expected:全绿。

- [ ] **Step 2: client 全量测 + analyze**

```bash
cd yucai/client && flutter test && flutter analyze 2>&1 | grep -E "error -" | grep -v "pbserver\|\.pb\.dart"
```
Expected:仅 3 预存 fail(account/debt/transaction_detail);analyze 非 pbserver error=0。

- [ ] **Step 3: e2e(grpcurl 或 app)验证 GetGoalProgressHistory**

启 server + grpcurl GetGoalProgressHistory(goal_id, from, to)→ 验返 snapshot points(若 goal_progress_snapshot 有数据)。或 app goal detail 看趋势曲线渲染。

- [ ] **Step 4: final whole-branch review(opus, Phase 2 起点..HEAD)**

review Phase 2 commits:GetGoalProgressHistory RPC + snapshot 读 + fl_chart 曲线 + 测。Ready to merge 判定。

- [ ] **Step 5: 记 progress.md + memory**

`.superpowers/sdd/progress.md` 加 goal Phase 2 section。memory 更新 goal Phase 2 ✅。

---

## Self-Review

**Spec coverage**(spec §10 Phase 2):
- server GetGoalProgressHistory RPC(读 snapshot)→ Task 1 ✓
- Flutter fl_chart 趋势曲线 → Task 2(data)+ Task 3(UI)✓
- e2e + review → Task 4 ✓

**Placeholder**:无 TBD;server Task 1 完整 Go 代码;Flutter Task 2/3 照 goal Task 10/holding perf curve 范式 + 具体代码。

**Type consistency**:FindSnapshotRange(domain)→ GetGoalProgressHistory(application)→ proto RPC → ds getProgressHistory → GoalProgressPoint → _TrendChart,签名一致。

## 风险
- **fl_chart 1.x API**(Task 3):照 holding perf curve(已 1.x),勿照 0.69 教程。
- **snapshot 数据稀疏**(scheduler 每日写,若 goal 新建不足 1 天可能 1 点):empty 态 + 单点处理。
- **proto Timestamp**(Task 1/2):from/to 用 google.protobuf.Timestamp(handler timestamppb;ds Timestamp.fromDateTime)。
- **fakeGoalRepo FindSnapshotRange stub**(Task 1):interface 加方法 → fake 实现(照 D-budget Task 1 fake 教训)。
