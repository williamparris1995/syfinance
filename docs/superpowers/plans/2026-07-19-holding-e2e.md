# holding performance 计算 e2e Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 holding performance 计算(XIRR / TWR / CAGR,full 期)建精确数值 integration 回归网,补 `portfolioXIRR` / `portfolioTWR` 零覆盖,固化此前靠 Excel/GIPS 手算的验证。

**Architecture:** enttest SQLite integration(service 层入口),真 ent repo + 固定时序 fixture + ±1e-6 精确断言。含一处生产改动:`Service` 加 `now func() time.Time` 字段 + `SetNow` setter(`time.Now()` 内部化,test 可控评估日)。走 `svc.BuyHolding` / `RecordDividend` / `RecordSplit`(3 参数 service,不涉双写)建 trade,再 `GetHoldingPerformance` / `GetPortfolioPerformance` 黑盒验算。

**Tech Stack:** Go testing + enttest SQLite(`modernc.org/sqlite`)· TDD

---

## Plan 对 spec 的细化(实现发现)

spec 说"full + range 同法",实现发现两点边缘 → **本 plan 所有场景只验 full 期**(干净可手算 0.30):
1. `curveWindow`(service.go:466)只有 MONTH/YEAR/DAY 三个 rangeName,**无全期/ALL**。
2. `QtyAtDate` 用 strict `< day`(rangeStart = trade 日时 qty@rangeStart=0,range XIRR 退化);rangeStart 晚于 trade 则 full 年化天数漂移 → full 与 range 无法同时精确 0.30。
→ range 精确验证留 future(独立 range 场景 spec)。本 plan 锁 full 期绝对正确性,已是最大回归风险点(domain 单测连 full 都只验方向)。

S3 split:fixture 在 split 后必须把 `sec.CurrentPriceCents` + split 后的 price_history 点 ÷ ratio(维持市值中性;`RecordSplit` 不自动复价)。

## Global Constraints

- enttest SQLite in-memory:`sql.Open("sqlite", "file:<name>?mode=memory")` + `db.SetMaxOpenConns(1)` + `PRAGMA foreign_keys = ON`(照 [holding_doublewrite_integration_test.go:33-66](../../yucai/server/tests/holding_doublewrite_integration_test.go))
- **单币 CNY**:`rateRepo` nil(`rateForCode` 返 1.0 graceful,照 holding_handler_test.go:467 注释)
- **now 注入**:`svc.SetNow(func() time.Time { return evalDate })`,评估日固定 `2021-01-01`
- **日期**:`time.Parse("2006-01-02", "2020-01-01").UTC()`(避免时区漂移;service 内 `truncateToDate` 强制 UTC)
- **tenant_id**:全 fixture 同一个 `uuid.New()`(`FindAll`/`FindSnapshots` 用 tenant 过滤,`uuid.Nil` 返空集)
- **trade 创建**:`svc.BuyHolding` / `RecordDividend` / `RecordSplit`(3 参数 `application.NewService`,不涉 account 双写)
- English slog(`slog.Error("op", "k", v)`),无 CJK 在 log 串
- TDD:failing test → minimal impl → pass → commit;`commit multi -m`
- 零 proto/schema/wire 改(仅 service.go 加 `now` 字段 + setter + 替换 `time.Now`/`time.Since`)

## File Structure

| 文件 | 责任 |
|---|---|
| [service.go](../../yucai/server/internal/holding/application/service.go)(改) | `Service` 加 `now func() time.Time` 字段 + `SetNow` setter + 19 处 `time.Now()`/`time.Since(x)` → `s.now()`/`s.now().Sub(x)` |
| `yucai/server/tests/holding_performance_integration_test.go`(新,`package tests`) | `setupPerformanceHarness` + seed helpers + `approxFloat` + TestS1–S4 |

---

## Task 1: Service `now` 注入(生产改动,基础)

**Files:**
- Modify: [service.go](../../yucai/server/internal/holding/application/service.go)(struct 定义 ~L19-30 + `NewService` ~L33 + setter 块 ~L364-389 + 19 处 call site)

**Interfaces:**
- Produces: `func (s *Service) SetNow(f func() time.Time)`;`s.now()` 用于全 perf 路径时间

- [ ] **Step 1: 加 `now` 字段 + `NewService` 默认**

[service.go:19-30](../../yucai/server/internal/holding/application/service.go) `Service` struct 加末尾字段:
```go
type Service struct {
	securityRepo       domain.SecurityRepository
	holdingRepo        domain.HoldingRepository
	tradeRepo          domain.TradeRepository
	priceRouter        priceprovider.Router
	lotRepo            domain.LotRepository
	snapshotRepo       domain.SnapshotRepository
	priceHistoryRepo   domain.PriceHistoryRepository
	historicalProvider priceprovider.HistoricalProvider
	rateRepo           domain.RateHistoryRepository
	tenantLister       domain.TenantLister
	now                func() time.Time // perf e2e 注入固定评估日;默认 time.Now
}
```

[service.go:33-35](../../yucai/server/internal/holding/application/service.go) `NewService` 设默认:
```go
func NewService(secRepo domain.SecurityRepository, hRepo domain.HoldingRepository, tRepo domain.TradeRepository) *Service {
	return &Service{securityRepo: secRepo, holdingRepo: hRepo, tradeRepo: tRepo, now: time.Now}
}
```

- [ ] **Step 2: 加 `SetNow` setter**

在 setter 块(~L364-389,`SetTenantLister` 后)加:
```go
// SetNow overrides the clock (perf e2e injects a fixed eval date; production uses time.Now).
func (s *Service) SetNow(f func() time.Time) {
	if f == nil {
		f = time.Now
	}
	s.now = f
}
```

- [ ] **Step 3: 替换 19 处 call site**

替换规则(全局,service.go 内):
- `time.Now()` → `s.now()`(13 处:L345, 441, 467, 485, 837, 882, 915, 987, 1063, 1223, 1234, 1425, 1569)
- `time.Since(x)` → `s.now().Sub(x)`(6 处:L1324, 1471, 1536, 1544, 1582, 1591)

实现:用编辑器全文替换,或 `gofmt` 后人工核对每处(注意 `SnapshotHoldings` L485、`SeedSampleHoldings` L345、`SyncPrices` L441 虽非 perf 路径,但统一替换保持 `now` 单一来源)。

- [ ] **Step 4: 加 `now` 注入单测(验证 setter 生效)**

在 `application` 包加 `now_inject_test.go`(或并入现有 test):
```go
package application

import (
	"testing"
	"time"
)

func TestSetNowInjectsFixedClock(t *testing.T) {
	fixed := time.Date(2021, 1, 1, 0, 0, 0, 0, time.UTC)
	svc := NewService(nil, nil, nil)
	svc.SetNow(func() time.Time { return fixed })
	if got := svc.now(); !got.Equal(fixed) {
		t.Errorf("SetNow: got %v, want %v", got, fixed)
	}
}

func TestNewServiceDefaultsToTimeNow(t *testing.T) {
	svc := NewService(nil, nil, nil)
	before := time.Now()
	got := svc.now()
	after := time.Now()
	if got.Before(before) || got.After(after) {
		t.Errorf("default now: got %v, want within [%v, %v]", got, before, after)
	}
}
```

- [ ] **Step 5: 跑 application 包测,验证零回归**

Run: `cd yucai/server && go test ./internal/holding/application/... -count=1`
Expected: PASS(现有 domain 单测全过;`now` 默认 `time.Now` 行为不变)

- [ ] **Step 6: 跑全量 server 测**

Run: `cd yucai/server && go test ./... -count=1`
Expected: PASS(零回归;`now` 注入透明)

- [ ] **Step 7: Commit**

```bash
cd e:/projects/syfinance
git add yucai/server/internal/holding/application/service.go yucai/server/internal/holding/application/now_inject_test.go
git commit -m "refactor(holding): Service now injectable (perf e2e fixed eval date)" -m "Service 加 now func() time.Time 字段 + SetNow setter, 19 处 time.Now()/time.Since(x) -> s.now()/s.now().Sub(x). NewService 默认 time.Now (production 行为不变). perf e2e 注入固定评估日 2021-01-01 控年化天数. 零计算逻辑改动."
```

---

## Task 2: setupPerformanceHarness + seed helpers + S1(单标的基线 0.30)

**Files:**
- Create: `yucai/server/tests/holding_performance_integration_test.go`

**Interfaces:**
- Consumes: Task 1 的 `SetNow`;`application.NewService` + `SetSnapshotRepository`/`SetPriceHistoryRepository`
- Produces: `setupPerformanceHarness`、`day()`、`seedPriceHistory`、`approxFloat`、S1 测

- [ ] **Step 1: 写文件骨架 + harness + helpers + S1 failing test**

创建 `yucai/server/tests/holding_performance_integration_test.go`:
```go
package tests

import (
	"context"
	"database/sql"
	"math"
	"testing"
	"time"

	entsql "entgo.io/ent/dialect/sql"
	_ "modernc.org/sqlite"

	"github.com/google/uuid"
	"github.com/yucai/server/internal/holding/adapter/driven/repository"
	"github.com/yucai/server/internal/holding/application"
	"github.com/yucai/server/internal/holding/domain"
	holdingent "github.com/yucai/server/internal/holding/ent"
)

const perfEvalDate = "2021-01-01" // 固定评估日(now 注入)

// setupPerformanceHarness wires a real ent-backed holding Service against one
// in-memory sqlite (CNY only — rateRepo intentionally nil). Returns the service,
// the ent client (for seed helpers), and a fresh tenant/account.
func setupPerformanceHarness(t *testing.T) (svc *application.Service, client *holdingent.Client, phRepo domain.PriceHistoryRepository, holdRepo domain.HoldingRepository, tenantID, accountID uuid.UUID) {
	t.Helper()
	db, err := sql.Open("sqlite", "file:hold_perf_"+t.Name()+"?mode=memory")
	if err != nil {
		t.Fatalf("open sqlite: %v", err)
	}
	t.Cleanup(func() { db.Close() })
	db.SetMaxOpenConns(1)
	if _, err := db.Exec("PRAGMA foreign_keys = ON"); err != nil {
		t.Fatalf("enable foreign keys: %v", err)
	}

	drv := entsql.OpenDB("sqlite3", db)
	client = holdingent.NewClient(holdingent.Driver(drv))
	if err := client.Schema.Create(context.Background()); err != nil {
		t.Fatalf("create holding schema: %v", err)
	}
	t.Cleanup(func() { client.Close() })

	secRepo := repository.NewSecurityRepository(client)
	holdRepo := repository.NewHoldingRepository(client)
	tradeRepo := repository.NewTradeRepository(client)
	snapRepo := repository.NewSnapshotRepository(client)
	phRepo := repository.NewPriceHistoryRepository(client)

	svc = application.NewService(secRepo, holdRepo, tradeRepo)
	svc.SetSnapshotRepository(snapRepo)
	svc.SetPriceHistoryRepository(phRepo)
	// rateRepo intentionally nil: all CNY (rateForCode returns 1.0).

	evalTime, err := time.Parse("2006-01-02", perfEvalDate)
	if err != nil {
		t.Fatalf("parse eval date: %v", err)
	}
	svc.SetNow(func() time.Time { return evalTime.UTC() })

	tenantID, accountID = uuid.New(), uuid.New()
	return svc, client, phRepo, holdRepo, tenantID, accountID
}

// day parses a YYYY-MM-DD date to UTC midnight (avoids timezone drift; service
// truncates to UTC day internally).
func day(t *testing.T, s string) time.Time {
	t.Helper()
	d, err := time.Parse("2006-01-02", s)
	if err != nil {
		t.Fatalf("parse day %q: %v", s, err)
	}
	return d.UTC()
}

// seedPriceHistory writes one price point (CAGR firstPrice + range rebuild).
func seedPriceHistory(t *testing.T, phRepo domain.PriceHistoryRepository, securityID uuid.UUID, priceDate time.Time, priceCents int64) {
	t.Helper()
	if err := phRepo.Save(context.Background(), domain.SecurityPriceHistory{
		SecurityID: securityID, PriceDate: priceDate, PriceCents: priceCents,
		CurrencyCode: "CNY", Source: "perf-e2e",
	}); err != nil {
		t.Fatalf("seed price history: %v", err)
	}
}

// approxFloat asserts got ≈ want within 1e-6 (XIRR/TWR/CAGR precision).
func approxFloat(t *testing.T, got *float64, want float64, msg string) {
	t.Helper()
	if got == nil {
		t.Fatalf("%s: nil, want %.9f", msg, want)
	}
	if math.Abs(*got-want) > 1e-6 {
		t.Errorf("%s: got %.9f, want %.9f (Δ %.9f)", msg, *got, want, *got-want)
	}
}

// TestS1_SingleHolding_Baseline: buy 100 @ ¥100 (2020-01-01) → current ¥130
// (eval 2021-01-01, 365 天). 无 dividend/sell/split → XIRR=CAGR=TWR=0.30.
func TestS1_SingleHolding_Baseline(t *testing.T) {
	svc, _, tenantID, accountID := setupPerformanceHarness(t)
	ctx := context.Background()

	// CreateSecurity 返 *Security;price 单独 UpdateSecurityPrice(照 TestSecurityCRUD).
	sec, err := svc.CreateSecurity(ctx, application.CreateSecurityRequest{
		Symbol: "600519.SH", Name: "Kweichow Moutai", SecurityType: domain.SecurityTypeStock,
		Exchange: "SSE", CurrencyCode: "CNY",
	})
	if err != nil {
		t.Fatalf("CreateSecurity: %v", err)
	}
	if err := svc.UpdateSecurityPrice(ctx, sec.ID, 13000); err != nil { // ¥130
		t.Fatalf("UpdateSecurityPrice: %v", err)
	}
	secID := sec.ID

	// buy 100 @ ¥100 (10000 cents) on 2020-01-01.
	trade, err := svc.BuyHolding(ctx, application.HoldingTradeRequest{
		TenantID: tenantID, AccountID: accountID, SecurityID: secID,
		Quantity: 100, PriceCents: 10000, TradeDate: day(t, "2020-01-01"),
	})
	if err != nil {
		t.Fatalf("BuyHolding: %v", err)
	}
	_ = trade

	// price_history firstPrice = ¥100 (CAGR full: firstPrice→current).
	seedPriceHistory(t, phRepo, secID, day(t, "2020-01-01"), 10000)

	holding, err := holdRepo.FindByAccountAndSecurity(ctx, tenantID, accountID, secID)
	if err != nil || holding == nil {
		t.Fatalf("find holding: %v", err)
	}
	perf, err := svc.GetHoldingPerformance(ctx, holding.ID, "MONTH", "CNY")
	if err != nil {
		t.Fatalf("GetHoldingPerformance: %v", err)
	}

	// full 期:单笔 buy、无中断、整 365 天 → XIRR=CAGR=TWR=0.30.
	approxFloat(t, perf.AnnualizedPct, 0.30, "S1 full XIRR")
	approxFloat(t, perf.CagrAnnualizedPct, 0.30, "S1 full CAGR")
	approxFloat(t, perf.TwrAnnualizedPct, 0.30, "S1 full TWR")
}
```

- [ ] **Step 2: 确认 harness 返回值**

S1 在 Step 1 已用 `setupPerformanceHarness` 返回的 `phRepo`(`seedPriceHistory`)+ `holdRepo`(`FindByAccountAndSecurity` 拿 holding)。harness 返 6 值(svc, client, phRepo, holdRepo, tenantID, accountID)。无额外动作。

- [ ] **Step 3: 跑 S1,验证它 fail(期望 0.30,实际可能因 fixture/harness bug 偏差)**

Run: `cd yucai/server && go test ./tests/ -run TestS1 -v -count=1`
Expected: 初始可能 FAIL(fixture/harness 调通前),逐步修到 PASS。先确保编译过(`CreateSecurity` 签名/`SearchSecurities` 返回值照 service.go 实际)。

- [ ] **Step 4: 调通 S1 至 PASS(XIRR=CAGR=TWR=0.30 ±1e-6)**

修 fixture/harness 细节直至 `got 0.300000xxx, want 0.300000000`。常见坑:
- `CreateSecurity` 签名(确认 `application.CreateSecurityRequest` 字段)
- `BuyHolding` 后 `holding.AvgCostCents` = PriceCents=10000
- `FindByAccountAndSecurity(ctx, tenantID, accountID, secID)` 返回 `*domain.Holding`

Run: `cd yucai/server && go test ./tests/ -run TestS1 -v -count=1`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
cd e:/projects/syfinance
git add yucai/server/tests/holding_performance_integration_test.go
git commit -m "test(holding): S1 single-holding perf e2e baseline (XIRR=CAGR=TWR=0.30)" -m "setupPerformanceHarness (enttest SQLite, CNY, now 注入 2021-01-01) + seed helpers + approxFloat. S1: buy 100@100 (2020-01-01) -> current 130, full 期 XIRR/CAGR/TWR = 0.30 ±1e-6. 锁定单标的计算基线(domain 单测此前只验方向/非空)."
```

---

## Task 3: S2 + dividend(TWR 中性 + XIRR 增益)

**Files:**
- Modify: `yucai/server/tests/holding_performance_integration_test.go`

**Interfaces:**
- Consumes: Task 2 harness + helpers;`svc.RecordDividend`

- [ ] **Step 1: 写 S2 failing test**

加 `TestS2_WithDividend`:
```go
// TestS2_WithDividend: S1 + dividend ¥500 (2020-07-01).
// TWR: dividend 切点数学中性 (BV_before==BV_after 链 telescoping) -> TWR 不变 = 0.30.
// XIRR: dividend 是真实现金流 -> 增益 (XIRR > 0.30).
func TestS2_WithDividend(t *testing.T) {
	svc, _, phRepo, holdRepo, tenantID, accountID := setupPerformanceHarness(t)
	ctx := context.Background()

	// 建 S1 基线(照 TestS1 的 seed 流程,提 shared helper seedBaselineHolding)
	secID, holding := seedBaselineHolding(t, ctx, svc, phRepo, holdRepo, tenantID, accountID)

	// + dividend ¥500 (每股 ¥5 × 100) on 2020-07-01.
	if _, err := svc.RecordDividend(ctx, application.RecordDividendRequest{
		TenantID: tenantID, AccountID: accountID, SecurityID: secID,
		Quantity: 100, CashPerShareCents: 500, TotalAmountCents: 50000,
		TradeDate: day(t, "2020-07-01"),
	}); err != nil {
		t.Fatalf("RecordDividend: %v", err)
	}
	_ = holding

	perf, err := svc.GetHoldingPerformance(ctx, holding.ID, "MONTH", "CNY")
	if err != nil {
		t.Fatalf("GetHoldingPerformance: %v", err)
	}

	// TWR 中性:dividend 切点不改变 cumulative(memory split-adjusted 教训).
	approxFloat(t, perf.TwrAnnualizedPct, 0.30, "S2 TWR (dividend neutral)")

	// XIRR 增益:dividend 是真实现金流入,XIRR > 0.30.
	if perf.AnnualizedPct == nil || *perf.AnnualizedPct <= 0.30 {
		t.Errorf("S2 XIRR = %v, want > 0.30 (dividend boost)", perf.AnnualizedPct)
	}
	// 可选精确值:Excel =XIRR([-10000,500,13000],[2020-01-01,2020-07-01,2021-01-01]) ≈ 0.358.
	// 绝对值实现时 Excel 确认后可加 approxFloat(t, perf.AnnualizedPct, 0.358xxx, "S2 XIRR abs").
}
```

`seedBaselineHolding` helper(提取 S1 seed 流程,返 secID + holding):
```go
// seedBaselineHolding 建一个 buy 100@100 (2020-01-01) + price_history firstPrice 的基线持仓.
func seedBaselineHolding(t *testing.T, ctx context.Context, svc *application.Service,
	phRepo domain.PriceHistoryRepository, holdRepo domain.HoldingRepository,
	tenantID, accountID uuid.UUID) (secID uuid.UUID, holding *domain.Holding) {
	t.Helper()
	sec, err := svc.CreateSecurity(ctx, application.CreateSecurityRequest{
		Symbol: "600519.SH", Name: "Kweichow Moutai", SecurityType: domain.SecurityTypeStock,
		Exchange: "SSE", CurrencyCode: "CNY",
	})
	if err != nil {
		t.Fatalf("CreateSecurity: %v", err)
	}
	if err := svc.UpdateSecurityPrice(ctx, sec.ID, 13000); err != nil {
		t.Fatalf("UpdateSecurityPrice: %v", err)
	}
	secID = sec.ID
	if _, err := svc.BuyHolding(ctx, application.HoldingTradeRequest{
		TenantID: tenantID, AccountID: accountID, SecurityID: secID,
		Quantity: 100, PriceCents: 10000, TradeDate: day(t, "2020-01-01"),
	}); err != nil {
		t.Fatalf("BuyHolding: %v", err)
	}
	seedPriceHistory(t, phRepo, secID, day(t, "2020-01-01"), 10000)
	holding, err = holdRepo.FindByAccountAndSecurity(ctx, tenantID, accountID, secID)
	if err != nil || holding == nil {
		t.Fatalf("find holding: %v", err)
	}
	return secID, holding
}
```
同时把 `TestS1` 重构为调用 `seedBaselineHolding`(DRY)。

- [ ] **Step 2: 跑 S2,验证 fail 或 pass**

Run: `cd yucai/server && go test ./tests/ -run TestS2 -v -count=1`
Expected: TWR 中性断言应 PASS(若 memory dividend-neutral 教训成立);XIRR 增益应 PASS。若 TWR 偏离 → 暴露 dividend 切点 bug(真回归)。

- [ ] **Step 3: 调通 S2 至 PASS**

Run: `cd yucai/server && go test ./tests/ -run TestS2 -v -count=1`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
cd e:/projects/syfinance
git add yucai/server/tests/holding_performance_integration_test.go
git commit -m "test(holding): S2 dividend perf e2e (TWR neutral + XIRR boost)" -m "S2: S1 baseline + dividend 500 (2020-07-01). TWR 中性 (dividend 切点 BV telescoping) = 0.30; XIRR 增益 > 0.30. 提取 seedBaselineHolding shared helper (S1 复用 DRY)."
```

---

## Task 4: S3 + split(split 市值中性)

**Files:**
- Modify: `yucai/server/tests/holding_performance_integration_test.go`

**Interfaces:**
- Consumes: Task 3 `seedBaselineHolding`;`svc.RecordSplit`

- [ ] **Step 1: 写 S3 failing test**

加 `TestS3_WithSplit`:
```go
// TestS3_WithSplit: S2 + 2:1 split (2020-10-01).
// split 非现金流、市值中性 -> TWR / XIRR 不变(需 fixture split 后 price ÷ ratio 维持市值).
func TestS3_WithSplit(t *testing.T) {
	svc, _, phRepo, holdRepo, tenantID, accountID := setupPerformanceHarness(t)
	ctx := context.Background()

	secID, holding := seedBaselineHolding(t, ctx, svc, phRepo, holdRepo, tenantID, accountID)

	// S2 dividend(让 XIRR 有 dividend 增益基线,与 S2 同)
	if _, err := svc.RecordDividend(ctx, application.RecordDividendRequest{
		TenantID: tenantID, AccountID: accountID, SecurityID: secID,
		Quantity: 100, CashPerShareCents: 500, TotalAmountCents: 50000,
		TradeDate: day(t, "2020-07-01"),
	}); err != nil {
		t.Fatalf("RecordDividend: %v", err)
	}
	perfS2, err := svc.GetHoldingPerformance(ctx, holding.ID, "MONTH", "CNY")
	if err != nil {
		t.Fatalf("GetHoldingPerformance S2: %v", err)
	}

	// + 2:1 split on 2020-10-01 (qty 100 -> 200, avgCost 10000 -> 5000).
	if _, err := svc.RecordSplit(ctx, application.RecordSplitRequest{
		TenantID: tenantID, AccountID: accountID, SecurityID: secID,
		Ratio: 2.0, SplitDate: day(t, "2020-10-01"),
	}); err != nil {
		t.Fatalf("RecordSplit: %v", err)
	}

	// fixture: split 后 current price ÷ 2 维持市值(sec.CurrentPriceCents 不自动复价).
	if err := svc.UpdateSecurityPrice(ctx, secID, 6500); err != nil { // 13000/2
		t.Fatalf("UpdateSecurityPrice post-split: %v", err)
	}
	// price_history: split 后的 firstPrice 也 ÷ 2(CAGR firstPrice 一致性;split-adjusted).
	seedPriceHistory(t, phRepo, secID, day(t, "2020-10-02"), 6500)

	// 重新查 holding(split 后 qty/avgCost 已更新).
	holding2, err := holdRepo.FindByAccountAndSecurity(ctx, tenantID, accountID, secID)
	if err != nil || holding2 == nil {
		t.Fatalf("find holding post-split: %v", err)
	}
	perfS3, err := svc.GetHoldingPerformance(ctx, holding2.ID, "MONTH", "CNY")
	if err != nil {
		t.Fatalf("GetHoldingPerformance S3: %v", err)
	}

	// split 市值中性:TWR / XIRR 与 S2 一致.
	approxFloat(t, perfS3.TwrAnnualizedPct, *perfS2.TwrAnnualizedPct, "S3 TWR (split neutral)")
	approxFloat(t, perfS3.AnnualizedPct, *perfS2.AnnualizedPct, "S3 XIRR (split neutral)")
}
```

- [ ] **Step 2: 跑 S3,验证 split 中性**

Run: `cd yucai/server && go test ./tests/ -run TestS3 -v -count=1`
Expected: PASS(split 市值中性);若 TWR/XIRR 偏离 S2 → 暴露 split 价格复权 bug。

- [ ] **Step 3: 调通 S3 至 PASS**

注意:`holdingCAGR` 用 `FindBySecurity(securityID, time.Time{}, time.Now())` 全历史扫描 firstPrice —— 若 firstPrice 取到 split 前 10000(@2020-01-01)而非 split 后 6500,CAGR 会偏。若发生,seed 一条 split 后的更早点覆盖,或接受 CAGR 在 S3 用 S2 对比(已在断言)。

Run: `cd yucai/server && go test ./tests/ -run TestS3 -v -count=1`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
cd e:/projects/syfinance
git add yucai/server/tests/holding_performance_integration_test.go
git commit -m "test(holding): S3 split perf e2e (market-value neutral)" -m "S3: S2 + 2:1 split (2020-10-01). fixture split 后 current price 13000->6500 + price_history 点 ÷2 维持市值中性. TWR/XIRR 与 S2 byte-identical ±1e-6 (split 非现金流)."
```

---

## Task 5: S4 组合(portfolioXIRR/TWR/CAGR + cache 透明)

**Files:**
- Modify: `yucai/server/tests/holding_performance_integration_test.go`

**Interfaces:**
- Consumes: Task 2 harness + helpers;`svc.GetPortfolioPerformance`;`snapshotRepo.Save`(via harness client)

- [ ] **Step 1: 写 S4 failing test**

加 `TestS4_Portfolio_CacheTransparent`:
```go
// TestS4_Portfolio: 2 holdings(同 account,各 buy 100@100 @2020-01-01)+
// 按月 snapshot. 组合 portfolioXIRR/TWR/CAGR full = 0.30(合并现金流
// [-2000000@2020-01-01] + terminal 2600000,365 天). 补 portfolioXIRR/TWR 零覆盖.
// cache 透明:两次调用结果 byte-identical(mvCache request-scoped).
func TestS4_Portfolio_CacheTransparent(t *testing.T) {
	svc, client, phRepo, _, tenantID, accountID := setupPerformanceHarness(t)
	ctx := context.Background()

	// 2 个 security(同收益结构)+ 各 buy 100@100.
	secIDs := make([]uuid.UUID, 2)
	for i, sym := range []string{"600519.SH", "000001.SZ"} {
		if _, err := svc.CreateSecurity(ctx, application.CreateSecurityRequest{
			Symbol: sym, Name: sym, SecurityType: domain.SecurityTypeStock,
			Exchange: "SSE", CurrencyCode: "CNY", CurrentPriceCents: 13000,
		}); err != nil {
			t.Fatalf("CreateSecurity %s: %v", sym, err)
		}
		listed, _ := svc.SearchSecurities(ctx, sym, 1)
		secIDs[i] = listed[0].ID
		seedPriceHistory(t, phRepo, secIDs[i], day(t, "2020-01-01"), 10000)
		if _, err := svc.BuyHolding(ctx, application.HoldingTradeRequest{
			TenantID: tenantID, AccountID: accountID, SecurityID: secIDs[i],
			Quantity: 100, PriceCents: 10000, TradeDate: day(t, "2020-01-01"),
		}); err != nil {
			t.Fatalf("BuyHolding %s: %v", sym, err)
		}
	}

	// 按月 seed snapshot(让 GetPortfolioPerformance 入口通过 + curve 非空;XIRR/TWR 不依赖 snapshot 值).
	seedMonthlySnapshots(t, client, tenantID, accountID, secIDs)

	// 第一次调用.
	perf1, err := svc.GetPortfolioPerformance(ctx, tenantID, &accountID, "MONTH", false, "CNY")
	if err != nil {
		t.Fatalf("GetPortfolioPerformance #1: %v", err)
	}

	// full 期组合 = 0.30(合并现金流 + terminal,365 天).
	approxFloat(t, perf1.AnnualizedPct, 0.30, "S4 portfolio full XIRR")
	approxFloat(t, perf1.TwrAnnualizedPct, 0.30, "S4 portfolio full TWR")
	approxFloat(t, perf1.CagrAnnualizedPct, 0.30, "S4 portfolio full CAGR")

	// cache 透明:第二次调用 byte-identical.
	perf2, err := svc.GetPortfolioPerformance(ctx, tenantID, &accountID, "MONTH", false, "CNY")
	if err != nil {
		t.Fatalf("GetPortfolioPerformance #2: %v", err)
	}
	if perf1.AnnualizedPct != nil && perf2.AnnualizedPct != nil && *perf1.AnnualizedPct != *perf2.AnnualizedPct {
		t.Errorf("cache transparency: XIRR call1=%.9f call2=%.9f", *perf1.AnnualizedPct, *perf2.AnnualizedPct)
	}
	if perf1.TwrAnnualizedPct != nil && perf2.TwrAnnualizedPct != nil && *perf1.TwrAnnualizedPct != *perf2.TwrAnnualizedPct {
		t.Errorf("cache transparency: TWR call1=%.9f call2=%.9f", *perf1.TwrAnnualizedPct, *perf2.TwrAnnualizedPct)
	}
}
```
修掉占位 typo(`accountID_placeholder: 0` 删;`holdings, _ := ...` 行简化)。`seedMonthlySnapshots` helper:用 `client.HoldingSnapshot.Create()` 按月写点(2020-02..2021-01),MarketValueCents 任意合理递增值(10000→13000 区间)。

`seedMonthlySnapshots` 最小实现:
```go
// seedMonthlySnapshots writes one snapshot per holding per month (2020-02..2021-01).
// XIRR/TWR/CAGR 不依赖 snapshot 具体值(snapshot 只为 GetPortfolioPerformance 入口 + curve).
func seedMonthlySnapshots(t *testing.T, client *holdingent.Client, tenantID, accountID uuid.UUID, secIDs []uuid.UUID) {
	t.Helper()
	ctx := context.Background()
	holdRepo := repository.NewHoldingRepository(client)
	for _, secID := range secIDs {
		h, err := holdRepo.FindByAccountAndSecurity(ctx, tenantID, accountID, secID)
		if err != nil || h == nil {
			t.Fatalf("find holding for snapshot: %v", err)
		}
		months := []string{"2020-02-01", "2020-03-01", "2020-06-01", "2020-09-01", "2020-12-01", "2021-01-01"}
		for i, m := range months {
			mv := int64(1000000 + i*50000) // 任意合理递增(cents);不影响 XIRR/TWR
			if _, err := client.HoldingSnapshot.Create().
				SetTenantID(tenantID).SetHoldingID(h.ID).SetSecurityID(secID).SetAccountID(accountID).
				SetSnapshotDate(day(t, m)).SetMarketValueCents(mv).SetUnrealizedPnlCents(0).
				SetCurrencyCode("CNY").Save(ctx); err != nil {
				t.Fatalf("seed snapshot %s: %v", m, err)
			}
		}
	}
}
```

- [ ] **Step 2: 跑 S4,验证组合计算 + cache 透明**

Run: `cd yucai/server && go test ./tests/ -run TestS4 -v -count=1`
Expected: PASS(组合 XIRR/TWR/CAGR full=0.30;两次调用 byte-identical)。组合 XIRR 偏离 → 暴露 portfolioXIRR bug(补零覆盖的价值)。

- [ ] **Step 3: 调通 S4 至 PASS**

Run: `cd yucai/server && go test ./tests/ -run TestS4 -v -count=1`
Expected: PASS

- [ ] **Step 4: 全量回归**

Run: `cd yucai/server && go test ./... -count=1`
Expected: PASS(零生产回归 + 新 perf e2e 4 场景绿)

- [ ] **Step 5: Commit**

```bash
cd e:/projects/syfinance
git add yucai/server/tests/holding_performance_integration_test.go
git commit -m "test(holding): S4 portfolio perf e2e (补 portfolioXIRR/TWR 零覆盖 + cache 透明)" -m "S4: 2 holdings 各 buy 100@100 + 按月 snapshot. 组合 portfolioXIRR/TWR/CAGR full = 0.30 ±1e-6 (合并现金流 + terminal). cache 透明: 两次 GetPortfolioPerformance byte-identical (mvCache request-scoped). 补 codegraph 警告的 portfolioXIRR/portfolioTWR 零覆盖."
```

---

## 全量验证(plan 收尾)

- [ ] `cd yucai/server && go test ./tests/ -run "TestS[1-4]" -v -count=1` — 4 场景全 PASS
- [ ] `cd yucai/server && go test ./... -count=1` — 零回归
- [ ] `cd yucai/server && go build ./...` — build 绿
