# range TWR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TWR 从「只全期」扩展到「全期 + 区间」(组合级,对齐 XIRR 区间),兑现 [`2026-07-15-range-twr-design.md`](../specs/2026-07-15-range-twr-design.md)。

**Architecture:** `computeTWR(rangeStart)` 通用化(全期 = rangeStart=首笔 特例,零回归)→ `portfolioTWR(rangeStart)` 返 `(full, rng)` 镜像 `portfolioXIRR` → proto `range_twr_annualized_pct` (field 12) → client 区间 tab 加 range TWR 行。**domain `TWR()` 不改**。

**Tech Stack:** Go(server DDD)+ Flutter(client)+ proto3 + protoc_plugin **25.0.0**。

## Global Constraints

- **domain/twr.go 不改** —— `TWR(subPeriods, finalValue, lastAfterCF, totalDays)` 纯函数已通用,全期/区间只换入参。
- **wire 不改 / 零 schema** —— 只改 application + proto + client;portfolioTWR 改签名但无新 provider。
- **零回归** —— 全期 TWR 必须 byte-identical(Task 1 验证:computeTWR(rangeStart=cashFlowDays[0]) == 原 portfolioTWR)。
- **对齐 XIRR** —— portfolioTWR 镜像 portfolioXIRR(`(ctx, tenantID, accountID, base, rangeStart) (full, rng, err)`)。
- **英文结构化日志**(slog,无 CJK 在 log 串)。
- **protoc_plugin 25.0.0**(Dart);改 proto 后 Go + Dart stub 都 regen。
- **单标的 holdingTWR 不变**(only full,对齐 holdingXIRR)。
- **运行目录**:server 命令在 `yucai/server/`;client 在 `yucai/client/`;proto regen 在 `yucai/`。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `yucai/server/internal/holding/application/service.go` | computeTWR 抽出 + portfolioTWR rangeStart + GetPortfolioPerformance + DTO | Modify |
| `yucai/proto/holding/v1/holding.proto` | `range_twr_annualized_pct = 12` | Modify |
| `yucai/server/internal/holding/application/...`(test) | computeTWR/portfolioTWR/handler 测 | Create/Modify |
| `yucai/client/lib/holding/domain/entities/performance_entity.dart` | `rangeTwrAnnualizedPct` | Modify |
| `yucai/client/lib/holding/data/mappers/...` | mapper optional→null | Modify |
| `yucai/client/lib/holding/presentation/pages/performance_page.dart` | 区间 tab range TWR 行 | Modify |
| **不改** | `domain/twr.go`(TWR 纯函数通用) | — |

---

### Task 1: computeTWR 通用化(refactor,全期零回归)

**Files:**
- Modify: `yucai/server/internal/holding/application/service.go`(新增 `computeTWR`,重构 `portfolioTWR` 调它)
- Test: `yucai/server/internal/holding/application/service_test.go`(或同包测试)

**Interfaces:**
- Consumes: `marketValueAtDateAsOfWithTrades`、`uniqueSortedTradeDates`、`allTradesForTenant`、`currentMarketValueInBase`、`domain.TWR`、`domain.SubPeriodReturn`(均现有)。
- Produces: `computeTWR(ctx, tenantID, accountID, base, rateBase, trades, cashFlowDays, rangeStart) (*float64, error)` —— Task 2 的 portfolioTWR 依赖它算 full + rng。

- [ ] **Step 1: 新增 computeTWR(从 portfolioTWR 抽出 + 加 rangeStart 参数)**

在 `service.go` 的 `portfolioTWR` 前(L1231 前)新增:

```go
// computeTWR computes GIPS TWR over [rangeStart, now] using sub-period chaining.
// Generalized from portfolioTWR: full-period is rangeStart=cashFlowDays[0] (a
// special case — byte-identical to the original portfolioTWR). Returns nil on
// insufficient data or missing prices (degrade, mirrors portfolioTWR).
//
//   effectiveDays = cashFlowDays strictly after rangeStart (sub-period endpoints)
//   begin = BV_after(rangeStart) = qty@rangeStart+1d × price@rangeStart
//   subPeriods[i] = {Begin: prevAfter, End: BV_before(day_i)}
//   finalValue = current market value; totalDays = rangeStart→now
func (s *Service) computeTWR(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, base string, rateBase float64, trades []domain.HoldingTransaction, cashFlowDays []time.Time, rangeStart time.Time) (*float64, error) {
	var effectiveDays []time.Time
	for _, d := range cashFlowDays {
		if d.After(rangeStart) {
			effectiveDays = append(effectiveDays, d)
		}
	}
	if len(effectiveDays) == 0 {
		return nil, nil
	}
	beginAfter, ok := s.marketValueAtDateAsOfWithTrades(ctx, trades, tenantID, accountID, rangeStart.AddDate(0, 0, 1), rangeStart, rateBase, base)
	if !ok {
		return nil, nil
	}
	prevAfter := float64(beginAfter)
	subPeriods := make([]domain.SubPeriodReturn, 0, len(effectiveDays))
	for _, day := range effectiveDays {
		bvBefore, ok := s.marketValueAtDateAsOfWithTrades(ctx, trades, tenantID, accountID, day, day, rateBase, base)
		if !ok {
			return nil, nil
		}
		subPeriods = append(subPeriods, domain.SubPeriodReturn{
			BeginValueAfterCF: prevAfter,
			EndValueBeforeCF:  float64(bvBefore),
		})
		bvAfter, ok := s.marketValueAtDateAsOfWithTrades(ctx, trades, tenantID, accountID, day.AddDate(0, 0, 1), day, rateBase, base)
		if !ok {
			return nil, nil
		}
		prevAfter = float64(bvAfter)
	}
	finalValue := s.currentMarketValueInBase(ctx, tenantID, accountID, base)
	totalDays := int(time.Since(rangeStart).Hours() / 24)
	rate, err := domain.TWR(subPeriods, float64(finalValue), prevAfter, totalDays)
	if err != nil {
		return nil, nil
	}
	return ptrFloat(rate), nil
}
```

- [ ] **Step 2: 重构 portfolioTWR 调 computeTWR(全期,签名暂不变)**

把现有 `portfolioTWR`(L1231-1275)**整体替换**为(全期 = rangeStart=cashFlowDays[0],签名保持 `(*float64, error)` 暂不变,Task 2 再扩):

```go
// portfolioTWR computes full-period TWR for the portfolio (base currency).
// Full period = rangeStart = first trade date (cashFlowDays[0]). Degrades to
// nil on insufficient data or missing prices.
func (s *Service) portfolioTWR(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, baseCurrency string) (*float64, error) {
	base := baseCurrency
	if base == "" {
		base = "CNY"
	}
	rateBase := s.rateForBase(ctx, base)
	trades, _ := s.allTradesForTenant(ctx, tenantID, accountID)
	if len(trades) == 0 {
		return nil, nil
	}
	cashFlowDays := uniqueSortedTradeDates(trades)
	if len(cashFlowDays) < 2 {
		return nil, nil
	}
	return s.computeTWR(ctx, tenantID, accountID, base, rateBase, trades, cashFlowDays, cashFlowDays[0])
}
```

- [ ] **Step 3: 运行,期望全期 TWR 回归不破(byte-identical)**

Run: `cd yucai/server && go test ./internal/holding/application/... -count=1`
Expected: 现有 portfolioTWR / GetPortfolioPerformance 测全绿(computeTWR 全期 == 原 portfolioTWR)。若 XIRR/TWR 现有测 FAIL,说明 computeTWR 全期不等价 —— 核对 effectiveDays/begin 与原 loop(i>0)。

- [ ] **Step 4: Commit**

```bash
git add yucai/server/internal/holding/application/service.go
git commit -m "refactor(holding/twr): computeTWR 通用化(全期=rangeStart=首笔 特例,零回归)"
```

---

### Task 2: portfolioTWR 返 (full, rng) + GetPortfolioPerformance + DTO

**Files:**
- Modify: `yucai/server/internal/holding/application/service.go`(portfolioTWR 签名 + GetPortfolioPerformance + PortfolioPerformance DTO)
- Test: `yucai/server/internal/holding/application/service_test.go`

**Interfaces:**
- Consumes: Task 1 的 `computeTWR`、`curveWindow(rangeName)`(现有 L466)、`PortfolioPerformance` DTO。
- Produces: `portfolioTWR(ctx, tenantID, accountID, base, rangeStart) (full, rng *float64, err)`;`PortfolioPerformance.RangeTwrAnnualizedPct *float64`。

- [ ] **Step 1: portfolioTWR 改签名返 (full, rng)**

替换 Task 1 的 portfolioTWR 为(镜像 portfolioXIRR):

```go
// portfolioTWR computes full-period + range TWR (base currency), mirroring
// portfolioXIRR(full, rng).
//   full: rangeStart = first trade date (cashFlowDays[0])
//   rng:  rangeStart = curveWindow(rangeName).from
// Returns (nil, nil, nil) on insufficient data.
func (s *Service) portfolioTWR(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, baseCurrency string, rangeStart time.Time) (full, rng *float64, err error) {
	base := baseCurrency
	if base == "" {
		base = "CNY"
	}
	rateBase := s.rateForBase(ctx, base)
	trades, _ := s.allTradesForTenant(ctx, tenantID, accountID)
	if len(trades) == 0 {
		return nil, nil, nil
	}
	cashFlowDays := uniqueSortedTradeDates(trades)
	if len(cashFlowDays) < 2 {
		return nil, nil, nil
	}
	full, _ = s.computeTWR(ctx, tenantID, accountID, base, rateBase, trades, cashFlowDays, cashFlowDays[0])
	rng, _ = s.computeTWR(ctx, tenantID, accountID, base, rateBase, trades, cashFlowDays, rangeStart)
	return full, rng, nil
}
```

- [ ] **Step 2: PortfolioPerformance DTO 加 RangeTwrAnnualizedPct**

找到 `PortfolioPerformance` struct(同文件,GetPortfolioPerformance 返回类型),加字段:

```go
type PortfolioPerformance struct {
	// ... 既有字段 ...
	TwrAnnualizedPct    *float64  // 全期 TWR(既有)
	RangeTwrAnnualizedPct *float64 // 区间 TWR(Task 2 新增,nil=降级/区间不足)
	// ...
}
```

- [ ] **Step 3: GetPortfolioPerformance 调 portfolioTWR(rangeStart) 填 DTO**

改 L679(`twr, _ := s.portfolioTWR(...)`)与 L682 DTO。先看 L676 前如何算 `from`(XIRR 已用):

```go
	// L679 原全期 → 改 full + rng(rangeStart = XIRR 同款 from):
	fullTwr, rangeTwr, _ := s.portfolioTWR(ctx, tenantID, accountID, base, from)
	// L682 DTO 加 RangeTwrAnnualizedPct:
	return &PortfolioPerformance{
		TotalCents: total, AnnualizedPct: fullXirr, RangeAnnualizedPct: rangeXirr,
		TwrAnnualizedPct: fullTwr, RangeTwrAnnualizedPct: rangeTwr, TotalPct: totalPct, Currency: base,
	}, nil
```

> 注:`from` 是 XIRR L676 已用的 rangeStart(curveWindow 算)。若 GetPortfolioPerformance 内变量名非 `from`,用实际名。

- [ ] **Step 4: 写 portfolioTWR range 测(known rangeStart 手算)**

在 service_test.go 加测:构造 3 笔 buy/sell trades(已知日期 + price history stub),手算区间 TWR(子区间连乘),断言 `rng` 匹配 + `full` 仍对齐。加降级测:区间 < 2 现金流日 → rng nil;区间初空仓(rangeStart < 首笔)→ rng nil。

- [ ] **Step 5: 运行 + Commit**

Run: `cd yucai/server && go test ./internal/holding/application/... -count=1`
Expected: portfolioTWR range 测绿 + 全期回归不破。

```bash
git add yucai/server/internal/holding/application/service.go yucai/server/internal/holding/application/service_test.go
git commit -m "feat(holding/twr): portfolioTWR 返 full+rng(对齐 XIRR)+ DTO RangeTwrAnnualizedPct"
```

---

### Task 3: proto range_twr_annualized_pct + regen + handler 组装

**Files:**
- Modify: `yucai/proto/holding/v1/holding.proto`(field 12)
- Modify: `yucai/server/internal/holding/adapter/driving/grpc/holding_handler.go`(DTO→proto range_twr)
- Regen: Go + Dart stub(protoc_plugin 25.0.0)

**Interfaces:**
- Consumes: Task 2 的 `PortfolioPerformance.RangeTwrAnnualizedPct`。
- Produces: proto `PortfolioPerformanceResponse.range_twr_annualized_pct`(field 12);client Task 4 用。

- [ ] **Step 1: proto 加 field 12**

`yucai/proto/holding/v1/holding.proto` 的 `PortfolioPerformanceResponse`(L185-197),在 `twr_annualized_pct = 11` 后加:

```proto
  optional double twr_annualized_pct = 11;        // TWR 时间加权年化%(全期,nil=降级)
  optional double range_twr_annualized_pct = 12;  // TWR 区间年化%(随 CurveRange,nil=降级/区间不足)  ← 新增
```

**HoldingPerformanceResponse 不改**(单标的 only full)。

- [ ] **Step 2: regen Go + Dart stub**

```bash
cd yucai/server && buf generate --template buf.gen.go.yaml   # 无网 fallback:本地 protoc + protoc-gen-go/-grpc,手修 package
cd yucai && make gen-dart                                       # protoc_plugin 必须 25.0.0
```

- [ ] **Step 3: handler DTO→proto 组装 range_twr**

`holding_handler.go` 的 GetPortfolioPerformance handler(把 PortfolioPerformance DTO → proto `PortfolioPerformanceResponse`),在 `TwrAnnualizedPct` 组装旁加 `RangeTwrAnnualizedPct`:

```go
	if p.TwrAnnualizedPct != nil {
		resp.TwrAnnualizedPct = *p.TwrAnnualizedPct
	}
	if p.RangeTwrAnnualizedPct != nil {           // ← 新增
		resp.RangeTwrAnnualizedPct = *p.RangeTwrAnnualizedPct
	}
```

> 对齐现有 TwrAnnualizedPct 组装模式(*float64 → optional double)。若现有用 helper,复用。

- [ ] **Step 4: 写 handler 测(range_twr 映射)**

mock portfolioTWR 返 full+rng,断言 proto resp 的 range_twr_annualized_pct 正确 + nil 时不设。

- [ ] **Step 5: 运行 + Commit**

Run: `cd yucai/server && go test ./internal/holding/... -count=1`
Expected: handler 测绿 + server build 绿(proto regen 无红)。

```bash
git add yucai/proto yucai/server/internal/holding/adapter/driving/grpc/holding_handler.go yucai/server/internal/holding/application/
git commit -m "feat(holding/proto): range_twr_annualized_pct field 12 + regen + handler 组装"
```

---

### Task 4: client entity/mapper + performance_page 区间 tab range TWR 行

**Files:**
- Modify: `yucai/client/lib/holding/domain/entities/performance_entity.dart`
- Modify: `yucai/client/lib/holding/data/mappers/holding_mapper.dart`(或对应 performance mapper)
- Modify: `yucai/client/lib/holding/presentation/pages/performance_page.dart`

**Interfaces:**
- Consumes: Task 3 的 proto `range_twr_annualized_pct`。
- Produces: client 区间 tab 显示 range TWR(与 range XIRR 并列)。

- [ ] **Step 1: entity 加 rangeTwrAnnualizedPct**

`performance_entity.dart` 的 `PortfolioPerformance` class,加字段:

```dart
final double? rangeTwrAnnualizedPct;  // 区间 TWR(nil=降级)
```

(构造器 + props 同步,对齐现有 twrAnnualizedPct 范式。)

- [ ] **Step 2: mapper optional→null**

performance mapper(proto → domain):

```dart
rangeTwrAnnualizedPct: dto.hasRangeTwrAnnualizedPct() ? dto.rangeTwrAnnualizedPct : null,
```

(对齐现有 twrAnnualizedPct 映射。)

- [ ] **Step 3: performance_page 区间 tab 加 range TWR 行**

找到组合级区间 tab 的 range XIRR 展示(现有 RangeAnnualizedPct 行),旁加 range TWR 行(标"时间加权",null→'—'):

```dart
// 对齐现有 range XIRR 行,加 range TWR 并列
_StatRow(
  label: '时间加权(区间)',
  value: perf.rangeTwrAnnualizedPct != null
      ? '${(perf.rangeTwrAnnualizedPct! * 100).toStringAsFixed(2)}%'
      : '—',
),
```

> 实际 widget 名/布局对齐现有 range XIRR / 全期 TWR 行(复用同一卡片/行组件)。

- [ ] **Step 4: 写 widget 测**

mock PortfolioPerformance rangeTwrAnnualizedPct=0.1 → 断言区间 tab 显示 "10.00%";null → 断言 '—'。

- [ ] **Step 5: 运行 + Commit**

Run: `cd yucai/client && flutter test test/holding/presentation/pages/performance_page_test.dart`
Expected: widget 测绿。

```bash
git add yucai/client/lib/holding/ yucai/client/test/holding/
git commit -m "feat(holding/client): range TWR 区间 tab 展示(entity+mapper+performance_page)"
```

---

## 全量回归(最后)

- [ ] **Run: `cd yucai/server && go test ./... `** —— 全绿(全期 TWR + XIRR 不破;handler 测绿)。
- [ ] **Run: `cd yucai/client && flutter test`** —— 除 4 预存 drift fail(account/app_shell/receivable,out-of-scope)外全绿;新增 client widget 测绿。
- [ ] **Run: `cd yucai/client && flutter analyze`** —— 22 error 基线全 `*.pbserver.dart`(range_twr regen 不增)。
