# XIRR 收益引擎修正 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 holding 模块的简单收益引擎替换为 XIRR(资金加权 IRR),支持全期 + 区间 × 组合 + 单标的,直击 P0 基础正确性。

**Architecture:** domain 纯函数(`XIRR` + `CashFlow` + `QtyAtDate`,自写 Newton-Raphson + bisection fallback,无依赖)+ application 编排(`buildCashFlows`/`portfolioXIRR`/`holdingXIRR`,复用现有 `convertTradeToBase`/`rateForCode`/`priceHistoryRepo`)。零 schema 改动。`annualized_pct` proto 字段语义升级为全期 XIRR + 新增 `range_annualized_pct`,proto3 `optional` 表达降级。

**Tech Stack:** Go(server,DDD + ent + 手改 wire)+ Flutter(client,flutter_bloc + injectable)+ proto3 + buf/protoc_plugin 25.0.0。

**Spec:** [docs/superpowers/specs/2026-07-12-xirr-design.md](../specs/2026-07-12-xirr-design.md)

## Global Constraints

- **英文结构化日志** — slog(`slog.Error("op", "key", val)`),无 CJK 在 log 串。
- **wire 不改** — 本任务 `Service` 签名不变,domain 纯函数无依赖,不碰 `wire_gen.go`。
- **proto regen** — Go:`cd yucai/proto && buf generate --template buf.gen.go.yaml`;Dart:`cd yucai && make gen-dart`(**protoc_plugin 25.0.0**)。改 proto 后两端都要 regen。
- **server test** — `cd yucai/server && go test ./internal/holding/... -count=1`。
- **DDD 边界** — domain 纯函数不 import application/infrastructure;application 编排 repo。
- **复用第一** — 多币种折算复用 `convertTradeToBase` / `currencydomain.ConvertToBase` / `rateForCode` / `rateForBase`,不重写。
- **降级不造假** — XIRR 无解/数据缺失 → `nil`(`*float64` / `double?`),UI 显示 `—`,绝不返回 0 或伪造值。

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `yucai/server/internal/holding/domain/xirr.go` | `CashFlow` + `XIRR` + sentinel error + `QtyAtDate` 纯函数 | Create |
| `yucai/server/internal/holding/domain/xirr_test.go` | XIRR 对比 Excel + 边界 + QtyAtDate replay | Create |
| `yucai/server/internal/holding/application/dto.go` | `PortfolioPerformance`/`HoldingPerformance` 加 nullable XIRR 字段 | Modify |
| `yucai/server/internal/holding/application/service.go` | `buildCashFlows`/`portfolioXIRR`/`holdingXIRR` + 改造两个 Get 方法 | Modify |
| `yucai/server/internal/holding/application/xirr_service_test.go` | application XIRR 编排测试(fake repos) | Create |
| `yucai/proto/holding/v1/holding.proto` | `annualized_pct` 改 optional + 新增 `range_annualized_pct` | Modify |
| `yucai/server/internal/holding/adapter/driving/grpc/holding_handler.go` | 两个 RPC 组装新字段 | Modify |
| `yucai/client/lib/holding/domain/entities/performance_entity.dart` | entity XIRR 字段 nullable | Modify |
| `yucai/client/lib/holding/data/mappers/holding_mapper.dart` | mapper 映射 optional→nullable | Modify |
| `yucai/client/lib/holding/presentation/pages/performance_page.dart` | 年化展示 + 区间副位 + `—` 降级 | Modify |
| `yucai/client/lib/holding/presentation/pages/holding_detail_page.dart` | 单标的 XIRR 展示 | Modify |

---

### Task 1: domain XIRR 纯函数 + CashFlow + 边界

**Files:**
- Create: `yucai/server/internal/holding/domain/xirr.go`
- Test: `yucai/server/internal/holding/domain/xirr_test.go`

**Interfaces:**
- Consumes: 无(domain 最底层,只依赖 stdlib)
- Produces: `CashFlow{Date time.Time, Amount float64}`、`XIRR(cashflows []CashFlow) (float64, error)`、`ErrInsufficientCashFlows`、`ErrNoSolution`。后续 Task 3 用 `XIRR`。

- [ ] **Step 1: Write the failing test**

Create `yucai/server/internal/holding/domain/xirr_test.go`:

```go
package domain

import (
	"math"
	"testing"
	"time"
)

func mustDate(s string) time.Time {
	t, err := time.Parse("2006-01-02", s)
	if err != nil {
		panic(err)
	}
	return t
}

// Excel =XIRR() 文档经典例:投入 -10000,分期收回 → 37.34%。
func TestXIRRMatchesExcel(t *testing.T) {
	cfs := []CashFlow{
		{Date: mustDate("2008-01-01"), Amount: -10000},
		{Date: mustDate("2008-03-01"), Amount: 2750},
		{Date: mustDate("2008-10-30"), Amount: 4250},
		{Date: mustDate("2009-02-15"), Amount: 3250},
		{Date: mustDate("2009-04-01"), Amount: 2750},
	}
	rate, err := XIRR(cfs)
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	// Excel XIRR = 0.373362535
	if math.Abs(rate-0.373362535) > 1e-6 {
		t.Errorf("XIRR = %.9f, want 0.373362535", rate)
	}
}

func TestXIRRInsufficientCashFlows(t *testing.T) {
	_, err := XIRR([]CashFlow{{Date: mustDate("2020-01-01"), Amount: -100}})
	if err != ErrInsufficientCashFlows {
		t.Errorf("err = %v, want ErrInsufficientCashFlows", err)
	}
}

// 全同号(无正负交叉)→ NPV 无零点。
func TestXIRRNoSolutionSameSign(t *testing.T) {
	_, err := XIRR([]CashFlow{
		{Date: mustDate("2020-01-01"), Amount: -100},
		{Date: mustDate("2020-06-01"), Amount: -50},
		{Date: mustDate("2021-01-01"), Amount: 0}, // 0 不算正
	})
	if err != ErrNoSolution {
		t.Errorf("err = %v, want ErrNoSolution", err)
	}
}

// 单笔 buy + 当前终值 → 正常收敛。
func TestXIRRSimpleGrowth(t *testing.T) {
	cfs := []CashFlow{
		{Date: mustDate("2020-01-01"), Amount: -10000},
		{Date: mustDate("2021-01-01"), Amount: 12000}, // 一年 20%
	}
	rate, err := XIRR(cfs)
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if math.Abs(rate-0.20) > 1e-4 {
		t.Errorf("XIRR = %.6f, want ~0.20", rate)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd yucai/server && go test ./internal/holding/domain/ -run TestXIRR -count=1 -v`
Expected: FAIL / 编译失败(`XIRR` / `CashFlow` undefined)。

- [ ] **Step 3: Write minimal implementation**

Create `yucai/server/internal/holding/domain/xirr.go`:

```go
package domain

import (
	"errors"
	"math"
	"sort"
	"time"
)

// XIRR 现金流计算错误。
var (
	ErrInsufficientCashFlows = errors.New("xirr: insufficient cash flows")
	ErrNoSolution            = errors.New("xirr: no solution")
)

// CashFlow 是一笔带日期的现金流。Amount 负=流出(投入),正=流入(收回)。
// 币种/单位无关 —— application 层负责把多币种折算到同币种再喂入。
type CashFlow struct {
	Date   time.Time
	Amount float64
}

// XIRR 计算现金流的年化内部收益率(actual/365,对齐 Excel XIRR)。
// 返回年化比率(如 0.085 = 8.5%)。现金流<2、全同号(无零点)、
// Newton+bisection 都失败时返回对应 sentinel error。
func XIRR(cashflows []CashFlow) (float64, error) {
	if len(cashflows) < 2 {
		return 0, ErrInsufficientCashFlows
	}
	hasPos, hasNeg := false, false
	for _, cf := range cashflows {
		if cf.Amount > 0 {
			hasPos = true
		}
		if cf.Amount < 0 {
			hasNeg = true
		}
	}
	if !hasPos || !hasNeg {
		return 0, ErrNoSolution
	}

	sorted := make([]CashFlow, len(cashflows))
	copy(sorted, cashflows)
	sort.Slice(sorted, func(i, j int) bool { return sorted[i].Date.Before(sorted[j].Date) })

	t0 := sorted[0].Date
	years := make([]float64, len(sorted))
	amounts := make([]float64, len(sorted))
	for i, cf := range sorted {
		years[i] = cf.Date.Sub(t0).Hours() / 24 / 365.0
		amounts[i] = cf.Amount
	}

	npv := func(rate float64) float64 {
		sum := 0.0
		for i := range sorted {
			sum += amounts[i] / math.Pow(1+rate, years[i])
		}
		return sum
	}
	npvPrime := func(rate float64) float64 {
		sum := 0.0
		for i := range sorted {
			sum += -years[i] * amounts[i] / math.Pow(1+rate, years[i]+1)
		}
		return sum
	}

	// Newton-Raphson 主迭代(guess=0.1)。
	rate := 0.1
	for iter := 0; iter < 100; iter++ {
		f := npv(rate)
		if math.Abs(f) < 1e-7 {
			return rate, nil
		}
		d := npvPrime(rate)
		if d == 0 {
			break
		}
		next := rate - f/d
		if math.Abs(next-rate) < 1e-9 {
			return next, nil
		}
		rate = next
		if rate <= -1 || rate > 1e4 {
			break // 越界,转 bisection
		}
	}

	// Bisection fallback 在 [-0.9999, 1000] 找根。
	lo, hi := -0.9999, 1000.0
	nLo := npv(lo)
	nHi := npv(hi)
	if nLo*nHi > 0 {
		return 0, ErrNoSolution
	}
	for iter := 0; iter < 200; iter++ {
		mid := (lo + hi) / 2
		f := npv(mid)
		if math.Abs(f) < 1e-7 || (hi-lo)/2 < 1e-9 {
			return mid, nil
		}
		if nLo*f < 0 {
			hi = mid
		} else {
			lo = mid
			nLo = f
		}
	}
	return 0, ErrNoSolution
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd yucai/server && go test ./internal/holding/domain/ -run TestXIRR -count=1 -v`
Expected: PASS(ok 4 tests)。

- [ ] **Step 5: Commit**

```bash
git add yucai/server/internal/holding/domain/xirr.go yucai/server/internal/holding/domain/xirr_test.go
git commit -m "feat(holding/xirr): domain XIRR 纯函数(Newton+bisection,对比 Excel)"
```

---

### Task 2: domain QtyAtDate replay 纯函数

**Files:**
- Modify: `yucai/server/internal/holding/domain/xirr.go`(追加 `QtyAtDate`)
- Test: `yucai/server/internal/holding/domain/xirr_test.go`(追加测试)

**Interfaces:**
- Consumes: `domain.HoldingTransaction`(entity.go)、`domain.TradeType` 常量(valueobject.go:TradeTypeBuy/Sell/Dividend/Split)。
- Produces: `QtyAtDate(trades []HoldingTransaction, date time.Time) float64`。Task 3 区间初值估值用它。

- [ ] **Step 1: Write the failing test**

追加到 `xirr_test.go`(在 `package domain` 内,复用 `mustDate`):

```go
func TestQtyAtDateReplay(t *testing.T) {
	trades := []HoldingTransaction{
		{TradeType: TradeTypeBuy, Quantity: 100, TradeDate: mustDate("2020-01-10")},
		{TradeType: TradeTypeBuy, Quantity: 50, TradeDate: mustDate("2020-03-01")},
		{TradeType: TradeTypeSell, Quantity: 30, TradeDate: mustDate("2020-06-01")},
		{TradeType: TradeTypeSplit, Quantity: 2, TradeDate: mustDate("2020-09-01")}, // ratio=2
	}
	tests := []struct {
		name string
		date string
		want float64
	}{
		{"before any buy", "2020-01-01", 0},
		{"after first buy (strict <)", "2020-01-10", 0}, // rangeStart 当天归入期间
		{"after two buys", "2020-04-01", 150},
		{"after sell", "2020-07-01", 120},
		{"after split (×2)", "2020-10-01", 240},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got := QtyAtDate(trades, mustDate(tt.date))
			if got != tt.want {
				t.Errorf("QtyAtDate(%s) = %v, want %v", tt.date, got, tt.want)
			}
		})
	}
}

// Dividend 不影响 qty。
func TestQtyAtDateDividendNoOp(t *testing.T) {
	trades := []HoldingTransaction{
		{TradeType: TradeTypeBuy, Quantity: 100, TradeDate: mustDate("2020-01-10")},
		{TradeType: TradeTypeDividend, Quantity: 100, TradeDate: mustDate("2020-06-01")},
	}
	got := QtyAtDate(trades, mustDate("2020-07-01"))
	if got != 100 {
		t.Errorf("QtyAtDate with dividend = %v, want 100", got)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd yucai/server && go test ./internal/holding/domain/ -run TestQtyAtDate -count=1 -v`
Expected: FAIL(`QtyAtDate` undefined)。

- [ ] **Step 3: Write minimal implementation**

追加到 `xirr.go` 末尾(`import` 已含 `sort`/`time`):

```go
// QtyAtDate 按 transaction 时间序回放,返回 date 开盘前持有的份额
// (严格 TradeDate < date —— date 当天的 trade 归入区间期间现金流)。
// Buy 加、Sell 减、Split 按 Quantity(=ratio)乘当前累计、Dividend 跳过
// (现金分红不碰持仓)。供区间 XIRR 的期初市值重建用。
func QtyAtDate(trades []HoldingTransaction, date time.Time) float64 {
	sorted := make([]HoldingTransaction, len(trades))
	copy(sorted, trades)
	sort.Slice(sorted, func(i, j int) bool { return sorted[i].TradeDate.Before(sorted[j].TradeDate) })
	qty := 0.0
	for _, t := range sorted {
		if !t.TradeDate.Before(date) {
			break
		}
		switch t.TradeType {
		case TradeTypeBuy:
			qty += t.Quantity
		case TradeTypeSell:
			qty -= t.Quantity
		case TradeTypeSplit:
			qty *= t.Quantity
		case TradeTypeDividend:
			// no-op
		}
	}
	return qty
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd yucai/server && go test ./internal/holding/domain/ -count=1 -v`
Expected: PASS(全部 XIRR + QtyAtDate 测试)。

- [ ] **Step 5: Commit**

```bash
git add yucai/server/internal/holding/domain/xirr.go yucai/server/internal/holding/domain/xirr_test.go
git commit -m "feat(holding/xirr): QtyAtDate transaction replay 重建历史持仓"
```

---

### Task 3: application XIRR 编排 + DTO 字段

**Files:**
- Modify: `yucai/server/internal/holding/application/dto.go`(`PortfolioPerformance`/`HoldingPerformance` 加 nullable 字段)
- Modify: `yucai/server/internal/holding/application/service.go`(新增 `buildCashFlows`/`portfolioXIRR`/`holdingXIRR`)
- Create: `yucai/server/internal/holding/application/xirr_service_test.go`

**Interfaces:**
- Consumes: Task 1 `domain.XIRR`、Task 2 `domain.QtyAtDate`;现有 `convertTradeToBase`([service.go:826](../../yucai/server/internal/holding/application/service.go#L826))、`rateForBase`、`rateForCode`、`tradeRepo.FindAll`、`priceHistoryRepo.FindBySecurity`、`holdingRepo.FindAll`、`securityRepo.FindByID`。
- Produces: `(*Service).portfolioXIRR(ctx, tenantID, accountID, baseCurrency, rangeStart time.Time) (full, rng *float64, err)`(组合全期+区间)、`(*Service).holdingXIRR(ctx, holdingID, baseCurrency) (full *float64, err)`(单标的全期;区间由 Task 4 `computeHoldingRangeXIRR` 单独算)。Task 4 两个 Get 方法调它们。

- [ ] **Step 1: Write the failing test**

Create `yucai/server/internal/holding/application/xirr_service_test.go`:

```go
package application

import (
	"context"
	"math"
	"testing"
	"time"

	"github.com/google/uuid"
	"github.com/yucai/server/internal/holding/domain"
)

// fakeTradeRepo serves in-memory trades (tenant/account/security-insensitive
// for test simplicity — XIRR only reads Items).
type fakeTradeRepo struct{ items []domain.HoldingTransaction }

func (r *fakeTradeRepo) Save(_ context.Context, _ *domain.HoldingTransaction) error {
	return nil
}
func (r *fakeTradeRepo) FindAll(_ context.Context, _ uuid.UUID, _, _ *uuid.UUID, _ domain.PageRequest) (*domain.PaginatedResult[domain.HoldingTransaction], error) {
	return &domain.PaginatedResult[domain.HoldingTransaction]{Items: r.items}, nil
}

// fakePriceRepo returns one fixed price for any (security,date ≤ query).
type fakePriceRepo struct{ priceCents int64 }

func (r *fakePriceRepo) FindBySecurity(_ context.Context, _ uuid.UUID, _, _ time.Time) ([]domain.SecurityPriceHistory, error) {
	return nil, nil
}
func (r *fakePriceRepo) FindAtOrBefore(_ context.Context, _ uuid.UUID, _ time.Time) (int64, bool) {
	return r.priceCents, true
}
func (r *fakePriceRepo) SaveAll(_ context.Context, _ []domain.SecurityPriceHistory) error { return nil }
func (r *fakePriceRepo) Save(_ context.Context, _ domain.SecurityPriceHistory) error      { return nil }
func (r *fakePriceRepo) Exists(_ context.Context, _ uuid.UUID) (bool, error)              { return true, nil }

func mustDate2(s string) time.Time {
	t, _ := time.Parse("2006-01-02", s)
	return t
}

// 全期 XIRR:两笔 buy(原币 CNY,rate=1)+ 当前终值 → 与 domain XIRR 一致。
func TestPortfolioXIRRFullPeriodCNY(t *testing.T) {
	secID := uuid.New()
	svc := &Service{
		securityRepo: &fakeSecurityRepoByID{sec: domain.Security{ID: secID, CurrencyCode: "CNY", CurrentPriceCents: 12000}},
		holdingRepo:  &fakeHoldingRepoSingle{h: domain.Holding{SecurityID: secID, Quantity: 100, AvgCostCents: 10000}},
		tradeRepo: &fakeTradeRepo{items: []domain.HoldingTransaction{
			{TradeType: domain.TradeTypeBuy, Quantity: 100, AmountCents: 1000000, FeeCents: 0, SecurityID: secID, TradeDate: mustDate2("2020-01-01")},
		}},
		priceHistoryRepo: &fakePriceRepo{priceCents: 12000},
		rateRepo:         &fakeRateRepo{rateByCode: map[string]float64{"CNY": 1.0}},
	}
	full, _, err := svc.portfolioXIRR(context.Background(), uuid.Nil, nil, "CNY", mustDate2("2020-06-01"))
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if full == nil {
		t.Fatal("full XIRR nil, want non-nil")
	}
	// 投入 10000(100×100元)→ 终值 12000(100×120元)≈ 1 年后;终值市值 12000,
	// 与 -10000 + 12000(同日近似)→ rate 接近 +20%(取决于 today 距 2020-01-01)。
	if *full <= 0 {
		t.Errorf("full XIRR = %v, want positive", *full)
	}
}

// 单标的 XIRR:buy + 终值(原币,不折算)。
func TestHoldingXIRROriginalCurrency(t *testing.T) {
	secID := uuid.New()
	holdID := uuid.New()
	accID := uuid.New()
	svc := &Service{
		securityRepo: &fakeSecurityRepoByID{sec: domain.Security{ID: secID, CurrencyCode: "CNY", CurrentPriceCents: 15000}},
		holdingRepo:  &fakeHoldingRepoSingle{h: domain.Holding{ID: holdID, AccountID: accID, SecurityID: secID, Quantity: 100, AvgCostCents: 10000}},
		tradeRepo: &fakeTradeRepo{items: []domain.HoldingTransaction{
			{TradeType: domain.TradeTypeBuy, Quantity: 100, AmountCents: 1000000, FeeCents: 0, SecurityID: secID, TradeDate: mustDate2("2020-01-01"), AccountID: accID},
		}},
		priceHistoryRepo: &fakePriceRepo{priceCents: 15000},
		rateRepo:         &fakeRateRepo{rateByCode: map[string]float64{"CNY": 1.0}},
	}
	full, err := svc.holdingXIRR(context.Background(), holdID, "CNY")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if full == nil || math.IsNaN(*full) {
		t.Fatalf("holding XIRR nil/NaN: %v", full)
	}
}

// 无 trade → nil(降级,不造假)。
func TestPortfolioXIRRNoTrades(t *testing.T) {
	svc := &Service{
		securityRepo:    &fakeSecurityRepoByID{},
		holdingRepo:     &fakeHoldingRepoSingle{},
		tradeRepo:       &fakeTradeRepo{items: nil},
		priceHistoryRepo: &fakePriceRepo{priceCents: 0},
		rateRepo:        &fakeRateRepo{rateByCode: map[string]float64{"CNY": 1.0}},
	}
	full, rng, err := svc.portfolioXIRR(context.Background(), uuid.Nil, nil, "CNY", mustDate2("2020-06-01"))
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if full != nil || rng != nil {
		t.Errorf("expected nil/nil for no trades, got full=%v rng=%v", full, rng)
	}
}
```

> **注:** `fakeSecurityRepoByID` / `fakeHoldingRepoSingle` 是测试辅助 —— 若 `service_test.go` 已有等价 fake(如 `fakeSecurityRepo`),复用之;否则在本文件追加最小实现。下面 Step 3 实现前先确认:用 `grep -n "type fakeSecurityRepo\|type fake.*Holding" yucai/server/internal/holding/application/service_test.go` 查现有 fake。若现有 `fakeSecurityRepo` 按 slice 存,改成"返回单个 byID"的包装即可。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd yucai/server && go test ./internal/holding/application/ -run TestPortfolioXIRR -count=1 -v`
Expected: FAIL / 编译失败(`portfolioXIRR` undefined、fake 类型缺失)。

- [ ] **Step 3: Add DTO nullable fields**

Modify `yucai/server/internal/holding/application/dto.go` —— `PortfolioPerformance`:

```go
type PortfolioPerformance struct {
	PortfolioPoints []CurvePointDTO
	BenchmarkPoints []CurvePointDTO
	BenchmarkName   string
	RealizedCents   int64
	UnrealizedCents int64
	TotalCents      int64
	AnnualizedPct   *float64 // 全期 XIRR 年化%(nil=降级)
	RangeAnnualizedPct *float64 // 区间 XIRR 年化%(随 CurveRange,nil=降级)
	TotalPct        float64
	Currency        string
}
```

`HoldingPerformance`:

```go
type HoldingPerformance struct {
	PricePoints     []CurvePointDTO
	RealizedCents   int64
	UnrealizedCents int64
	TotalCents      int64
	AnnualizedPct   *float64 // 全期 XIRR(原币,nil=降级)
	RangeAnnualizedPct *float64 // 区间 XIRR(原币,nil=降级)
	Currency        string
}
```

> ⚠️ 字段类型从 `float64` 改 `*float64` 后,`GetPortfolioPerformance` 现有 `AnnualizedPct: annualized` 会编译失败 —— Task 4 修复(改 `ptr(annualized)`)。本 task 先让 domain/application 编译过:GetPortfolioPerformance 暂改 `AnnualizedPct: nil`(Task 4 填真值)。在 service.go:672 把 `AnnualizedPct: annualized` 临时改 `AnnualizedPct: nil`。

- [ ] **Step 4: Write portfolioXIRR / holdingXIRR**

追加到 `service.go`(`import` 需含 `math`,已有):

```go
// ptrFloat 包装一个 float64 为 *float64(DTO nullable 字段用)。
func ptrFloat(v float64) *float64 { return &v }

// collectTradeCashFlows 把 trades 映射为 XIRR 现金流(base cents,已折算)。
// buy=−(amount+fee)、sell=+(amount−fee)、dividend=+amount、split 忽略。
// mode="full" 取全部 trades;"range" 只取 (rangeStart, today] 的 trade。
func (s *Service) collectTradeCashFlows(ctx context.Context, trades []domain.HoldingTransaction, mode string, rangeStart time.Time, rateBase float64, base string) []domain.CashFlow {
	cfs := make([]domain.CashFlow, 0, len(trades))
	for _, tr := range trades {
		if mode == "range" && !tr.TradeDate.After(rangeStart) {
			continue // 区间模式:只算 rangeStart 之后的 trade
		}
		var signed int64
		switch tr.TradeType {
		case domain.TradeTypeBuy:
			signed = -(tr.AmountCents + tr.FeeCents)
		case domain.TradeTypeSell:
			signed = tr.AmountCents - tr.FeeCents
		case domain.TradeTypeDividend:
			signed = tr.AmountCents
		case domain.TradeTypeSplit:
			continue
		default:
			continue
		}
		amount := s.convertTradeToBase(ctx, signed, tr.SecurityID, tr.TradeDate, rateBase, base)
		cfs = append(cfs, domain.CashFlow{Date: tr.TradeDate, Amount: float64(amount)})
	}
	return cfs
}

// currentMarketValueInBase 返回当前组合总市值(base cents)= Σ qty×currentPrice 折算。
// 复用 currentCostBasisInBase + currentUnrealizedInBase(两者皆 base,和=市值)。
func (s *Service) currentMarketValueInBase(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, base string) int64 {
	return s.currentCostBasisInBase(ctx, tenantID, accountID, base) +
		s.currentUnrealizedInBase(ctx, tenantID, accountID, base)
}

// marketValueAtDate 重建 date 开盘前的组合市值(base cents)。
// 任一 holding 的历史价缺失 → ok=false(调用方降级)。
func (s *Service) marketValueAtDate(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, date time.Time, rateBase float64, base string) (int64, bool) {
	var sum int64
	page := domain.PageRequest{PageSize: 100}
	for {
		res, err := s.holdingRepo.FindAll(ctx, tenantID, accountID, page)
		if err != nil {
			return 0, false
		}
		secTrades, _ := s.allTradesForTenant(ctx, tenantID, accountID)
		for _, h := range res.Items {
			sec, err := s.securityRepo.FindByID(ctx, h.SecurityID)
			if err != nil || sec == nil {
				return 0, false
			}
			qty := domain.QtyAtDate(filterTradesBySecurity(secTrades, h.SecurityID), date)
			if qty == 0 {
				continue
			}
			price, ok := s.priceAtOrBefore(ctx, h.SecurityID, date)
			if !ok {
				return 0, false // 历史价缺失(美股/OTC)→ 降级
			}
			mv := int64(math.Round(qty * float64(price)))
			rateFrom := s.rateForCode(ctx, sec.CurrencyCode, date)
			sum += currencydomain.ConvertToBase(mv, rateFrom, rateBase)
		}
		if res.NextPageToken == "" || len(res.Items) == 0 {
			break
		}
		page.PageToken = res.NextPageToken
	}
	return sum, true
}

// allTradesForTenant 分页拉全 trades(组合 XIRR 现金流 + qty 重建共用)。
func (s *Service) allTradesForTenant(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID) ([]domain.HoldingTransaction, error) {
	var all []domain.HoldingTransaction
	page := domain.PageRequest{PageSize: 500}
	for {
		res, err := s.tradeRepo.FindAll(ctx, tenantID, accountID, nil, page)
		if err != nil {
			return all, nil
		}
		all = append(all, res.Items...)
		if res.NextPageToken == "" || len(res.Items) == 0 {
			break
		}
		page.PageToken = res.NextPageToken
	}
	return all, nil
}

// portfolioXIRR 算组合级全期 + 区间 XIRR(base 折算)。rangeStart 是区间窗口起点
// (CurveRange 的 from,由 GetPortfolioPerformance 经 curveWindow 算出)。降级返回 nil;
// 区间历史价缺失(美股/OTC)时只返回 full(全期不受影响)。
func (s *Service) portfolioXIRR(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, baseCurrency string, rangeStart time.Time) (full, rng *float64, err error) {
	base := baseCurrency
	if base == "" {
		base = "CNY"
	}
	rateBase := s.rateForBase(ctx, base)
	trades, _ := s.allTradesForTenant(ctx, tenantID, accountID)
	if len(trades) == 0 {
		return nil, nil, nil
	}
	terminal := s.currentMarketValueInBase(ctx, tenantID, accountID, base)
	// 全期:全部 trade + 当前终值。
	fullCfs := append(s.collectTradeCashFlows(ctx, trades, "full", time.Time{}, rateBase, base),
		domain.CashFlow{Date: time.Now(), Amount: float64(terminal)})
	if r, e := domain.XIRR(fullCfs); e == nil {
		full = ptrFloat(r)
	}
	// 区间:期初市值作投入(−)+ 区间内 trade + 终值。
	startMV, ok := s.marketValueAtDate(ctx, tenantID, accountID, rangeStart, rateBase, base)
	if !ok {
		return full, nil // 区间历史价缺失 → 降级,全期仍返回
	}
	rangeCfs := []domain.CashFlow{{Date: rangeStart, Amount: -float64(startMV)}}
	rangeCfs = append(rangeCfs, s.collectTradeCashFlows(ctx, trades, "range", rangeStart, rateBase, base)...)
	rangeCfs = append(rangeCfs, domain.CashFlow{Date: time.Now(), Amount: float64(terminal)})
	if r, e := domain.XIRR(rangeCfs); e == nil {
		rng = ptrFloat(r)
	}
	return full, rng, nil
}

// holdingXIRR 算单标的级全期 XIRR(原币,不折算)。降级返回 nil。
// 区间 XIRR 由 Task 4 computeHoldingRangeXIRR 单独算(需 price_history 重建端点)。
func (s *Service) holdingXIRR(ctx context.Context, holdingID uuid.UUID, baseCurrency string) (full *float64, err error) {
	h, err := s.holdingRepo.FindByID(ctx, holdingID)
	if err != nil {
		return nil, nil, fmt.Errorf("holding xirr: find holding: %w", err)
	}
	sec, err := s.securityRepo.FindByID(ctx, h.SecurityID)
	if err != nil || sec == nil {
		return nil, nil, nil
	}
	trades, _ := s.tradeRepo.FindAll(ctx, h.TenantID, &h.AccountID, &h.SecurityID, domain.PageRequest{PageSize: 500})
	if len(trades) == 0 {
		return nil, nil, nil
	}
	// 原币现金流(不折算):直接用 trade amount ± fee。
	cfs := make([]domain.CashFlow, 0, len(trades)+1)
	for _, tr := range trades {
		var signed int64
		switch tr.TradeType {
		case domain.TradeTypeBuy:
			signed = -(tr.AmountCents + tr.FeeCents)
		case domain.TradeTypeSell:
			signed = tr.AmountCents - tr.FeeCents
		case domain.TradeTypeDividend:
			signed = tr.AmountCents
		case domain.TradeTypeSplit:
			continue
		default:
			continue
		}
		cfs = append(cfs, domain.CashFlow{Date: tr.TradeDate, Amount: float64(signed)})
	}
	terminal := h.MarketValue(sec.CurrentPriceCents) // 原币市值
	cfs = append(cfs, domain.CashFlow{Date: time.Now(), Amount: float64(terminal)})
	if r, e := domain.XIRR(cfs); e == nil {
		full = ptrFloat(r)
	}
	return full, nil
}

// priceAtOrBefore 取 security 在 date 当天或之前最近的 price_history 价(向前填充)。
// 复用 FindBySecurity(securityID, epoch, date) 取首条。
func (s *Service) priceAtOrBefore(ctx context.Context, securityID uuid.UUID, date time.Time) (int64, bool) {
	if s.priceHistoryRepo == nil {
		return 0, false
	}
	ph, err := s.priceHistoryRepo.FindBySecurity(ctx, securityID, time.Unix(0, 0), date)
	if err != nil || len(ph) == 0 {
		return 0, false
	}
	// 取 ≤ date 的最近一条(FindBySecurity 通常已按日期升序,取末条)。
	latest := ph[0]
	for _, p := range ph {
		if !p.PriceDate.After(date) && p.PriceDate.After(latest.PriceDate) {
			latest = p
		}
	}
	return latest.PriceCents, true
}

// filterTradesBySecurity 筛出某 security 的 trades(qty 重建用)。
func filterTradesBySecurity(trades []domain.HoldingTransaction, securityID uuid.UUID) []domain.HoldingTransaction {
	out := make([]domain.HoldingTransaction, 0, len(trades))
	for _, t := range trades {
		if t.SecurityID == securityID {
			out = append(out, t)
		}
	}
	return out
}
```

> `portfolioXIRR` 已含全期 + 区间(范围由 `rangeStart` 参数控制);Task 4 的 `GetPortfolioPerformance` 经 `curveWindow(rangeName)` 算出 `from` 后直接调用,无需另写组合区间 helper。

- [ ] **Step 5: Run test to verify it passes**

Run: `cd yucai/server && go test ./internal/holding/application/ -run TestPortfolioXIRR -count=1 -v && go test ./internal/holding/application/ -run TestHoldingXIRR -count=1 -v`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add yucai/server/internal/holding/application/dto.go yucai/server/internal/holding/application/service.go yucai/server/internal/holding/application/xirr_service_test.go
git commit -m "feat(holding/xirr): application portfolioXIRR/holdingXIRR 编排 + DTO nullable"
```

---

### Task 4: 改造 Get 方法填 XIRR + 区间 + 降级

**Files:**
- Modify: `yucai/server/internal/holding/application/service.go`(`GetPortfolioPerformance`:644-680、`GetHoldingPerformance`:962-1001)

**Interfaces:**
- Consumes: Task 3 `portfolioXIRR`/`holdingXIRR`/`marketValueAtDate`/`collectTradeCashFlows`、现有 `curveWindow(rangeName)`。
- Produces: 两个 Get 方法返回值带 `AnnualizedPct`/`RangeAnnualizedPct`(`*float64`)。Task 5 handler 组装。

- [ ] **Step 1: Write the failing test**

追加到 `xirr_service_test.go`:

```go
// 区间 XIRR:rangeStart 有历史价 → 非 nil;无 trade → nil。
func TestGetPortfolioPerformanceFillsXIRR(t *testing.T) {
	secID := uuid.New()
	tenant := uuid.New()
	svc := &Service{
		securityRepo: &fakeSecurityRepoByID{sec: domain.Security{ID: secID, CurrencyCode: "CNY", CurrentPriceCents: 12000}},
		holdingRepo:  &fakeHoldingRepoSingle{h: domain.Holding{TenantID: tenant, SecurityID: secID, Quantity: 100, AvgCostCents: 10000}},
		tradeRepo: &fakeTradeRepo{items: []domain.HoldingTransaction{
			{TradeType: domain.TradeTypeBuy, Quantity: 100, AmountCents: 1000000, SecurityID: secID, TradeDate: mustDate2("2020-01-01")},
		}},
		priceHistoryRepo: &fakePriceRepo{priceCents: 10000},
		rateRepo:         &fakeRateRepo{rateByCode: map[string]float64{"CNY": 1.0}},
	}
	out, err := svc.GetPortfolioPerformance(context.Background(), tenant, nil, "MONTH", false, "CNY")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if out.AnnualizedPct == nil {
		t.Error("AnnualizedPct nil, want non-nil (full XIRR)")
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd yucai/server && go test ./internal/holding/application/ -run TestGetPortfolioPerformanceFillsXIRR -count=1 -v`
Expected: FAIL(`AnnualizedPct` 是 Task 3 临时设的 nil)。

- [ ] **Step 3: Wire XIRR into GetPortfolioPerformance**

Modify `service.go` `GetPortfolioPerformance`([:644-680](../../yucai/server/internal/holding/application/service.go#L644))—— 在 `annualized := s.annualizedPct(...)` 那行附近替换。把:

```go
	annualized := s.annualizedPct(ctx, total, costBasis, tenantID)
	out := &PortfolioPerformance{
		PortfolioPoints: portPts, RealizedCents: realized, UnrealizedCents: unrealized,
		TotalCents: total, AnnualizedPct: nil, TotalPct: totalPct, Currency: base,
	}
```

替换为:

```go
	// XIRR:全期(资金加权,考虑现金流时点)+ 区间(随 range)。
	fullXirr, rangeXirr, _ := s.portfolioXIRR(ctx, tenantID, accountID, base, from)
	out := &PortfolioPerformance{
		PortfolioPoints: portPts, RealizedCents: realized, UnrealizedCents: unrealized,
		TotalCents: total, AnnualizedPct: fullXirr, RangeAnnualizedPct: rangeXirr,
		TotalPct: totalPct, Currency: base,
	}
```

> `from` 来自现有 `from, to, granularity := curveWindow(rangeName)`(service.go:652)。`portfolioXIRR` 已在 Task 3 实现(含区间),此处直接调用。

- [ ] **Step 4: (无需新增组合 helper)**

`portfolioXIRR`(含全期+区间)已在 Task 3 Step 4 实现。本 task 直接在 `GetPortfolioPerformance`(Step 3)调用,不重复定义组合 helper。`GetHoldingPerformance` 的区间由 Step 6 `computeHoldingRangeXIRR` 处理。

- [ ] **Step 5: Wire XIRR into GetHoldingPerformance**

Modify `GetHoldingPerformance`([:997-1000](../../yucai/server/internal/holding/application/service.go#L997))—— 把返回构造改为:

```go
	fullXirr, _ := s.holdingXIRR(ctx, holdingID, base)
	// 区间 XIRR(单标的):用 price_history 重建 rangeStart 端点。
	rangeStart, _, _ := curveWindow(rangeName)
	rng := s.computeHoldingRangeXIRR(ctx, h, sec, tradesForHolding(ctx, s, h), rangeStart, fullXirr)
	return &HoldingPerformance{
		PricePoints: pts, RealizedCents: realized, UnrealizedCents: unrealized,
		TotalCents: realized + unrealized, AnnualizedPct: fullXirr, RangeAnnualizedPct: rng,
		Currency: base,
	}, nil
```

> `computeHoldingRangeXIRR` + `tradesForHolding` 见 Step 6。rangeStart 早于首笔 buy(qty=0)→ 退化返回 `fullXirr`(fallback);历史价缺失 → nil(降级)。

- [ ] **Step 6: Add holding range XIRR helper**

追加到 `service.go`:

```go
// tradesForHolding 拉单 holding(account+security)的全部 trade。
func tradesForHolding(ctx context.Context, s *Service, h domain.Holding) []domain.HoldingTransaction {
	res, _ := s.tradeRepo.FindAll(ctx, h.TenantID, &h.AccountID, &h.SecurityID, domain.PageRequest{PageSize: 500})
	return res.Items
}

// computeHoldingRangeXIRR 算单标的区间 XIRR(原币)。rangeStart 价缺失 → nil。
func (s *Service) computeHoldingRangeXIRR(ctx context.Context, h domain.Holding, sec domain.Security, trades []domain.HoldingTransaction, rangeStart time.Time, fallback *float64) *float64 {
	qty := domain.QtyAtDate(trades, rangeStart)
	if qty == 0 {
		return fallback // rangeStart 早于首笔 buy → 退化全期
	}
	price, ok := s.priceAtOrBefore(ctx, h.SecurityID, rangeStart)
	if !ok {
		return nil // 历史价缺失 → 降级
	}
	startMV := int64(math.Round(qty * float64(price)))
	terminal := h.MarketValue(sec.CurrentPriceCents)
	cfs := []domain.CashFlow{{Date: rangeStart, Amount: -float64(startMV)}}
	for _, tr := range trades {
		if !tr.TradeDate.After(rangeStart) {
			continue
		}
		var signed int64
		switch tr.TradeType {
		case domain.TradeTypeBuy:
			signed = -(tr.AmountCents + tr.FeeCents)
		case domain.TradeTypeSell:
			signed = tr.AmountCents - tr.FeeCents
		case domain.TradeTypeDividend:
			signed = tr.AmountCents
		case domain.TradeTypeSplit:
			continue
		default:
			continue
		}
		cfs = append(cfs, domain.CashFlow{Date: tr.TradeDate, Amount: float64(signed)})
	}
	cfs = append(cfs, domain.CashFlow{Date: time.Now(), Amount: float64(terminal)})
	if r, e := domain.XIRR(cfs); e == nil {
		return ptrFloat(r)
	}
	return nil
}
```

- [ ] **Step 7: Run test to verify it passes + 全包回归**

Run: `cd yucai/server && go test ./internal/holding/application/ -count=1 -v`
Expected: PASS(全部,XIRR + 现有 GetPortfolioPerformance/GetHoldingPerformance 测不破坏)。

> ⚠️ 若现有测试断言 `out.AnnualizedPct == 0` 或 `AnnualizedPct` 为 float64,需同步改断言为 `nil`/`*float64`。用 `go test` 报错定位,逐个修。

- [ ] **Step 8: Commit**

```bash
git add yucai/server/internal/holding/application/service.go yucai/server/internal/holding/application/xirr_service_test.go
git commit -m "feat(holding/xirr): GetPortfolio/GetHoldingPerformance 填全期+区间 XIRR + 降级"
```

---

### Task 5: proto 字段 + Go regen + handler 组装

**Files:**
- Modify: `yucai/proto/holding/v1/holding.proto`(:185-211)
- Regen: Go stub(`yucai/server/internal/proto/holding/v1/`)
- Modify: `yucai/server/internal/holding/adapter/driving/grpc/holding_handler.go`(两个 RPC handler)

**Interfaces:**
- Consumes: Task 4 的 DTO `*float64` 字段。
- Produces: proto `optional double` 字段 + handler 填充。Task 6 client stub 依赖此 proto。

- [ ] **Step 1: Edit proto**

Modify `holding.proto` `PortfolioPerformanceResponse`(:185-195)—— `annualized_pct` 改 optional + 加 `range_annualized_pct`:

```proto
message PortfolioPerformanceResponse {
  repeated CurvePoint portfolio_points = 1;
  repeated CurvePoint benchmark_points = 2;
  string benchmark_name = 3;
  int64 realized_cents = 4;
  int64 unrealized_cents = 5;
  int64 total_cents = 6;
  optional double annualized_pct = 7;       // 全期 XIRR 年化%(nil=降级)
  double total_pct = 8;
  string currency = 9;
  optional double range_annualized_pct = 10; // 区间 XIRR 年化%(随 CurveRange)
}
```

`HoldingPerformanceResponse`(:205-211):

```proto
message HoldingPerformanceResponse {
  repeated CurvePoint price_points = 1;
  int64 realized_cents = 2;
  int64 unrealized_cents = 3;
  int64 total_cents = 4;
  string currency = 5;
  optional double annualized_pct = 6;        // 全期 XIRR(原币)
  optional double range_annualized_pct = 7;  // 区间 XIRR(原币)
}
```

- [ ] **Step 2: Regenerate Go stub**

Run: `cd yucai/proto && buf generate --template buf.gen.go.yaml`
Expected: 无错;`yucai/server/internal/proto/holding/v1/holding.pb.go` 重新生成(`AnnualizedPct *float64` + `RangeAnnualizedPct *float64` + `GetAnnualizedPct()` helper)。

> 无网 fallback:本地 protoc + protoc-gen-go/-grpc,手修 `package holdingv1`(见 CLAUDE.md)。

- [ ] **Step 3: Wire handler fields**

Modify `holding_handler.go` `GetPortfolioPerformance` handler —— 找到组装 `PortfolioPerformanceResponse{...}` 的地方(类似 `GetHoldingPerformance` handler :324-330),加 XIRR 字段:

```go
	return &pb.PortfolioPerformanceResponse{
		PortfolioPoints:     curvePointsToProto(perf.PortfolioPoints),
		BenchmarkPoints:     curvePointsToProto(perf.BenchmarkPoints),
		BenchmarkName:       perf.BenchmarkName,
		RealizedCents:       perf.RealizedCents,
		UnrealizedCents:     perf.UnrealizedCents,
		TotalCents:          perf.TotalCents,
		AnnualizedPct:       perf.AnnualizedPct,       // *float64 → optional
		RangeAnnualizedPct:  perf.RangeAnnualizedPct,  // *float64 → optional
		TotalPct:            perf.TotalPct,
		Currency:            perf.Currency,
	}, nil
```

`GetHoldingPerformance` handler(:324-330):

```go
	return &pb.HoldingPerformanceResponse{
		PricePoints:        curvePointsToProto(perf.PricePoints),
		RealizedCents:      perf.RealizedCents,
		UnrealizedCents:    perf.UnrealizedCents,
		TotalCents:         perf.TotalCents,
		Currency:           perf.Currency,
		AnnualizedPct:      perf.AnnualizedPct,
		RangeAnnualizedPct: perf.RangeAnnualizedPct,
	}, nil
```

- [ ] **Step 4: Build + handler test**

Run: `cd yucai/server && go build ./... && go test ./internal/holding/adapter/driving/grpc/ -count=1 -v`
Expected: build 绿;handler 测 PASS。若 `holding_handler_test.go` 的 response 断言引用旧字段,同步修。

- [ ] **Step 5: Commit**

```bash
git add yucai/proto/holding/v1/holding.proto yucai/server/internal/proto/holding/v1/ yucai/server/internal/holding/adapter/driving/grpc/holding_handler.go
git commit -m "feat(holding/proto): annualized_pct optional(XIRR 语义)+ range_annualized_pct"
```

---

### Task 6: client Dart stub regen + entity + mapper

**Files:**
- Regen: Dart stub(`yucai/client/lib/proto/holding/v1/holding.pb.dart`)
- Modify: `yucai/client/lib/holding/domain/entities/performance_entity.dart`
- Modify: `yucai/client/lib/holding/data/mappers/holding_mapper.dart`

- [ ] **Step 1: Regenerate Dart stub**

Run: `cd yucai && make gen-dart`
Expected: 无错;`PortfolioPerformanceResponse` 生成 `hasAnnualizedPct()` + `annualizedPct`($core.double)+ `hasRangeAnnualizedPct()` + `rangeAnnualizedPct`。

> ⚠️ **protoc_plugin 必须 25.0.0**(21.x 生成 protobuf 4.x 旧 API → analyze 暴增)。`make gen-dart` 内已固定。

- [ ] **Step 2: Edit entity nullable fields**

Modify `performance_entity.dart` `PortfolioPerformance`(:21-56)—— `annualizedPct` 改 `double?` + 加 `rangeAnnualizedPct`:

```dart
class PortfolioPerformance extends Equatable {
  const PortfolioPerformance({
    this.portfolioPoints = const [],
    this.benchmarkPoints = const [],
    this.benchmarkName = '',
    required this.realizedCents,
    required this.unrealizedCents,
    required this.totalCents,
    this.annualizedPct,            // double? (全期 XIRR,null=降级)
    this.rangeAnnualizedPct,       // double? (区间 XIRR)
    this.totalPct = 0,
    this.currency = 'CNY',
  });

  final List<PerfPoint> portfolioPoints;
  final List<PerfPoint> benchmarkPoints;
  final String benchmarkName;
  final int realizedCents;
  final int unrealizedCents;
  final int totalCents;
  final double? annualizedPct;
  final double? rangeAnnualizedPct;
  final double totalPct;
  final String currency;

  @override
  List<Object?> get props => [
        portfolioPoints, benchmarkPoints, benchmarkName,
        realizedCents, unrealizedCents, totalCents,
        annualizedPct, rangeAnnualizedPct, totalPct, currency,
      ];
}
```

`HoldingPerformance`(:62-85)—— 加 `annualizedPct` + `rangeAnnualizedPct`(`double?`):

```dart
class HoldingPerformance extends Equatable {
  const HoldingPerformance({
    this.pricePoints = const [],
    required this.realizedCents,
    required this.unrealizedCents,
    required this.totalCents,
    this.annualizedPct,
    this.rangeAnnualizedPct,
    this.currency = 'CNY',
  });

  final List<PerfPoint> pricePoints;
  final int realizedCents;
  final int unrealizedCents;
  final int totalCents;
  final double? annualizedPct;
  final double? rangeAnnualizedPct;
  final String currency;

  @override
  List<Object?> get props => [
        pricePoints, realizedCents, unrealizedCents, totalCents,
        annualizedPct, rangeAnnualizedPct, currency,
      ];
}
```

- [ ] **Step 3: Edit mapper**

Modify `holding_mapper.dart` `portfolioResponseToEntity`(:193-205)—— 用 `hasAnnualizedPct()`:

```dart
PortfolioPerformance portfolioResponseToEntity(
        pb.PortfolioPerformanceResponse r) =>
    PortfolioPerformance(
      portfolioPoints: r.portfolioPoints.map(curvePointToPerfPoint).toList(),
      benchmarkPoints: r.benchmarkPoints.map(curvePointToPerfPoint).toList(),
      benchmarkName: r.benchmarkName,
      realizedCents: r.realizedCents.toInt(),
      unrealizedCents: r.unrealizedCents.toInt(),
      totalCents: r.totalCents.toInt(),
      annualizedPct: r.hasAnnualizedPct() ? r.annualizedPct : null,
      rangeAnnualizedPct: r.hasRangeAnnualizedPct() ? r.rangeAnnualizedPct : null,
      totalPct: r.totalPct,
      currency: r.currency,
    );
```

`holdingResponseToEntity`(:208-215):

```dart
HoldingPerformance holdingResponseToEntity(pb.HoldingPerformanceResponse r) =>
    HoldingPerformance(
      pricePoints: r.pricePoints.map(curvePointToPerfPoint).toList(),
      realizedCents: r.realizedCents.toInt(),
      unrealizedCents: r.unrealizedCents.toInt(),
      totalCents: r.totalCents.toInt(),
      annualizedPct: r.hasAnnualizedPct() ? r.annualizedPct : null,
      rangeAnnualizedPct: r.hasRangeAnnualizedPct() ? r.rangeAnnualizedPct : null,
      currency: r.currency,
    );
```

- [ ] **Step 4: analyze + mapper test**

Run: `cd yucai/client && flutter analyze lib/holding/domain/entities/performance_entity.dart lib/holding/data/mappers/holding_mapper.dart && flutter test test/holding/data/holding_mapper_test.dart`
Expected: analyze 无新 error;mapper 测 PASS。若 mapper_test 断言旧 `annualizedPct == 0`,改 `== null`。

- [ ] **Step 5: Commit**

```bash
git add yucai/client/lib/proto/holding/v1/holding.pb.dart yucai/client/lib/holding/domain/entities/performance_entity.dart yucai/client/lib/holding/data/mappers/holding_mapper.dart
git commit -m "feat(holding/client): XIRR entity nullable + mapper optional→null"
```

---

### Task 7: client presentation 展示 XIRR + 降级

**Files:**
- Modify: `yucai/client/lib/holding/presentation/pages/performance_page.dart`(:282、:662-665、:697-705、:719-721)
- Modify: `yucai/client/lib/holding/presentation/pages/holding_detail_page.dart`(详情页 XIRR 卡)
- Test: `yucai/client/test/holding/presentation/pages/performance_page_test.dart`

> **状态层透传:** `PerformanceBloc` emit `PerformanceLoaded(perf)`,perf 是 `PortfolioPerformance` —— entity 字段已 nullable,bloc **不用改**。

- [ ] **Step 1: Write the failing widget test**

追加到 `performance_page_test.dart`(参照现有模式 —— pump `PerformanceLoaded` 带非 null annualizedPct):

```dart
testWidgets('renders XIRR value when annualizedPct non-null', (tester) async {
  final perf = PortfolioPerformance(
    realizedCents: 0, unrealizedCents: 0, totalCents: 0,
    annualizedPct: 8.5, rangeAnnualizedPct: 12.3,
  );
  // ... pump 页面 with PerformanceLoaded(perf) + HoldingLoaded(空 holdings)
  // expect find text containing '8.5%' (全期) + '12.3%' (区间)
});

testWidgets('renders — when annualizedPct null (degraded)', (tester) async {
  final perf = PortfolioPerformance(
    realizedCents: 0, unrealizedCents: 0, totalCents: 0,
    annualizedPct: null,
  );
  // ... pump;expect find '—'(不显数字)
});
```

> 具体 pump harness 参照 `performance_page_test.dart` 现有用例(它已 pump `PerformanceLoaded`)。

- [ ] **Step 2: Run test to verify it fails**

Run: `cd yucai/client && flutter test test/holding/presentation/pages/performance_page_test.dart`
Expected: FAIL(XIRR 文案未渲染 / `—` 未出现)。

- [ ] **Step 3: Fix hasAnnualized checks (null-aware)**

`performance_page.dart` 三处 `loaded.annualizedPct != 0` → `loaded.annualizedPct != null`:

- :282: `final hasAnnualized = loaded != null && loaded.annualizedPct != null;`
- :662: `final hasAnnualized = loaded != null && loaded.annualizedPct != null;`
- :719: `if (loaded != null && loaded.annualizedPct != null)`

`:663-665` 展示值(注意 `loaded.annualizedPct` 现在是 `double?`,非 null 解包):

```dart
    final annualValue = hasAnnualized
        ? '${loaded!.annualizedPct! >= 0 ? '+' : ''}${loaded.annualizedPct!.toStringAsFixed(1)}%'
        : '—';
```

`:721` `_BenchmarkMiniBar.myAnnualized` 同理解包(`loaded!.annualizedPct!`)。

- [ ] **Step 4: Add range XIRR display**

在 `_annualCard`(:656+)或 `_annualRow` 调用处,加区间 XIRR 副标注(随 range tab):

```dart
          _annualRow(
            icon: LucideIcons.percent,
            label: '年化收益',
            value: annualValue,
            valueColor: hasAnnualized
                ? (loaded!.annualizedPct! >= 0 ? AppColors.positive : AppColors.negative)
                : AppColors.muted,
            sub: loaded?.rangeAnnualizedPct != null
                ? '区间 ${loaded!.rangeAnnualizedPct! >= 0 ? '+' : ''}${loaded!.rangeAnnualizedPct!.toStringAsFixed(1)}%'
                : null,
          ),
```

> `_annualRow` 若无 `sub` 参数,加一个可选 `String? sub` 副标注(小字 muted,在 value 下方)。对齐 OD annual-card 布局;具体 defer 到实现看格子(可走 visual companion 对齐)。

- [ ] **Step 5: Add holding_detail XIRR display**

`holding_detail_page.dart` —— 在 foot(realized/unrealized/total)区附近加一个 XIRR 行,接 `HoldingPerformance.annualizedPct`(全期)+ `rangeAnnualizedPct`(区间),`null → '—'`。模式同 performance_page `_annualRow`。

> 详情页 HoldingBloc 已带 `HoldingPerformance`?若否,加 `LoadHoldingCurveRequested` 子 event(C snapshot spec Task 12 已设计)—— **核查现状**:`grep -n "HoldingPerformance\|holdingCurveFoot\|holdingCurve" yucai/client/lib/holding/presentation/bloc/holding_bloc.dart`。若详情页尚未接 GetHoldingPerformance,本 task 加 bloc 子 event + state 字段(参照 C spec §7.2)。

- [ ] **Step 6: Run test + analyze**

Run: `cd yucai/client && flutter test test/holding/presentation/pages/performance_page_test.dart && flutter analyze lib/holding/presentation`
Expected: 测 PASS;analyze 无新 error(基线 22 error 全 `*.pbserver.dart`)。

- [ ] **Step 7: Commit**

```bash
git add yucai/client/lib/holding/presentation/pages/performance_page.dart yucai/client/lib/holding/presentation/pages/holding_detail_page.dart yucai/client/test/holding/presentation/pages/performance_page_test.dart
git commit -m "feat(holding/client): performance/holding_detail 展示 XIRR 全期+区间 + — 降级"
```

---

### Task 8: e2e 验证(vs Excel)+ final review

**Files:** 无新文件(验证 + review)。

- [ ] **Step 1: server 全量测**

Run: `cd yucai/server && go test ./... -count=1`
Expected: PASS(含 XIRR 新测 + 现有 holding 测不破坏;account/debt/transaction_detail_page 预存 fail 跳过)。

- [ ] **Step 2: client 全量测**

Run: `cd yucai/client && flutter test`
Expected: PASS(基线 3 预存 fail 跳过:account/debt/transaction_detail_page_test)。

- [ ] **Step 3: P0 验证基准 —— 真实数据 vs Excel**

启 dev 环境(podman `yucai-pg` + server `bin/server.exe`,见 memory `yucai-dev-env`),用真实 holding 交易数据:
1. grpcurl 调 `GetPortfolioPerformance`(range=YEAR),记录 `annualized_pct`(全期 XIRR)。
2. 同数据手动输入 Excel `=XIRR(...)`(buy 负、sell 正、dividend 正、当前市值正)。
3. 比对:误差应 < 0.01%(年化)。

Expected: server XIRR ≈ Excel XIRR。若偏差大,检查现金流符号(`collectTradeCashFlows`)或折算(`convertTradeToBase` 交易日汇率)。

- [ ] **Step 4: flutter run 看展示**

Run: `cd yucai/client && flutter run -d windows`(JIT debug)
Expected: performance_page 年化位显 XIRR 全期值 + 区间副位随 range tab 变;美股 holding 区间 `—`(全期有值);holding_detail 显 XIRR。

- [ ] **Step 5: final whole-branch review**

```bash
git log --oneline main..HEAD | cat   # 本分支 XIRR commits
```
用 `/code-review`(high effort)过整 diff,确认:现金流符号、边界降级、proto optional 一致、client nullable 解包无 null 断言崩溃。

- [ ] **Step 6: Commit ledger / 更新 memory**

更新 memory `holding-asset-management-todo`:XIRR 修正完成(全期+区间,组合+单标的);记 P0 验证结果(server vs Excel 误差)。若有 defer 项(TWR、美股历史价回填、缓存)记 follow-up。

---

## 实施顺序依赖图

```
Task 1 (domain XIRR) ──┐
Task 2 (domain QtyAtDate) ─┤
                          ├─→ Task 3 (app XIRR 编排 + DTO) ─→ Task 4 (Get 方法填 XIRR)
                          │                                          │
                          │                                          ↓
                          │                  Task 5 (proto + Go regen + handler)
                          │                                          │
                          │                                          ↓
                          │                  Task 6 (client stub + entity + mapper)
                          │                                          │
                          │                                          ↓
                          │                  Task 7 (client presentation)
                          │                                          │
                          └──────────────────────────────────────────┴─→ Task 8 (e2e + review)
```

Task 1/2 可并行(独立 domain 纯函数)。Task 5 依赖 Task 4(dto 字段)。Task 6 依赖 Task 5(proto)。Task 7 依赖 Task 6。Task 8 最后。
