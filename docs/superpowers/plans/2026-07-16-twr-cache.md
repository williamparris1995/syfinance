# request-scoped TWR cache Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** portfolioTWR 的 full+range computeTWR 共享 request-scoped `mvCache`,消除 range ⊂ full 的 BV(day) 重复。兑现 [`2026-07-16-twr-cache-design.md`](../specs/2026-07-16-twr-cache-design.md)。

**Architecture:** `mvCacheKey`/`mvCache`/`cachedMV`(memoize `marketValueAtDateAsOfWithTrades`)→ `computeTWR` 加 cache 参数(3 处 marketValue → cachedMV)→ `portfolioTWR` 创建 cache 传 full+range。**透明(byte-identical),零 domain/proto/client/handler 改动**。

**Tech Stack:** Go(server DDD application 层)。

## Global Constraints

- **透明(byte-identical)** —— cache 只 memoize(miss→算+存,hit→返),不改算法。现有 range TWR Task 1/2 测必须全绿(全期+range TWR 结果不变)。
- **零 domain/proto/client/handler 改动** —— cache 纯 application 内。
- **request-scoped** —— `portfolioTWR` 内 `cache := mvCache{}`,每 request 重建,无失效。
- **holdingTWR 不动** —— 单标的独立(L1369),不调 computeTWR,不受 cache 参数影响。
- **英文结构化日志**(slog)。
- **运行目录**: server `yucai/server/`。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `yucai/server/internal/holding/application/service.go` | mvCacheKey/mvCache/cachedMV + computeTWR cache 参数 + portfolioTWR 创建 cache | Modify |
| `yucai/server/internal/holding/application/service_test.go`(或 twr_service_test.go) | cache 正确性 + 回归 + benchmark | Modify/Create |
| **不改** | domain/twr.go、proto、client、handler、holdingTWR | — |

---

### Task 1: mvCache + cachedMV + computeTWR cache 参数 + portfolioTWR 创建 cache

**Files:**
- Modify: `yucai/server/internal/holding/application/service.go`
- Test: `yucai/server/internal/holding/application/twr_service_test.go`(或 service_test.go)

**Interfaces:**
- Consumes: `marketValueAtDateAsOfWithTrades`(现有 L1132)、`computeTWR`(现有 L1239)、`portfolioTWR`(现有 L1289)。
- Produces: `mvCacheKey`/`mvCache`/`cachedMV`;`computeTWR` 加 `cache mvCache` 参数(仅 portfolioTWR 调)。

- [ ] **Step 1: 加 mvCacheKey/mvCache/cachedMV(spec §6 verbatim)**

在 `service.go` 的 `computeTWR` 前(L1239 前)新增:

```go
// mvCacheKey identifies a cached market-value computation within one request.
// tenantID/rateBase/base are fixed inside portfolioTWR (single base per request),
// so they are not part of the key.
type mvCacheKey struct {
	accountID uuid.UUID // nil (portfolio-level) → uuid.Nil; specific account → *accountID
	qtyAsOf   time.Time
	priceAsOf time.Time
}

// mvCache memoizes marketValueAtDateAsOfWithTrades results within one portfolioTWR
// call so full + range computeTWR share BV(day) (range ⊂ full). Both val and ok
// (ok=false = price missing → degrade) are cached to avoid recomputing misses.
type mvCache map[mvCacheKey]struct {
	val int64
	ok  bool
}

// cachedMV is a memoizing wrapper around marketValueAtDateAsOfWithTrades. On cache
// hit it returns the cached (val, ok); on miss it computes, stores, and returns.
// Transparent: identical results to a direct marketValueAtDateAsOfWithTrades call.
func (s *Service) cachedMV(ctx context.Context, cache mvCache, trades []domain.HoldingTransaction,
	tenantID uuid.UUID, accountID *uuid.UUID, qtyAsOf, priceAsOf time.Time, rateBase float64, base string) (int64, bool) {
	ac := uuid.Nil
	if accountID != nil {
		ac = *accountID
	}
	key := mvCacheKey{accountID: ac, qtyAsOf: qtyAsOf, priceAsOf: priceAsOf}
	if v, hit := cache[key]; hit {
		return v.val, v.ok
	}
	val, ok := s.marketValueAtDateAsOfWithTrades(ctx, trades, tenantID, accountID, qtyAsOf, priceAsOf, rateBase, base)
	cache[key] = struct {
		val int64
		ok  bool
	}{val, ok}
	return val, ok
}
```

- [ ] **Step 2: computeTWR 加 cache 参数,3 处 marketValueAtDateAsOfWithTrades → cachedMV**

`computeTWR`(L1239)签名加 `cache mvCache` 参数(末尾),3 处调用改 `cachedMV`:

```go
func (s *Service) computeTWR(ctx context.Context, tenantID uuid.UUID, accountID *uuid.UUID, base string, rateBase float64, trades []domain.HoldingTransaction, cashFlowDays []time.Time, rangeStart time.Time, cache mvCache) (*float64, error) {
	// ... effectiveDays (unchanged) ...
	beginAfter, ok := s.cachedMV(ctx, cache, trades, tenantID, accountID, rangeStart.AddDate(0, 0, 1), rangeStart, rateBase, base)
	// ...
	for _, day := range effectiveDays {
		bvBefore, ok := s.cachedMV(ctx, cache, trades, tenantID, accountID, day, day, rateBase, base)
		// ... subPeriods append ...
		bvAfter, ok := s.cachedMV(ctx, cache, trades, tenantID, accountID, day.AddDate(0, 0, 1), day, rateBase, base)
		// ...
	}
	// ... finalValue/totalDays/domain.TWR (unchanged) ...
}
```

> 注:beginAfter/bvBefore/bvAfter 三处的 `marketValueAtDateAsOfWithTrades` 全换 `cachedMV(cache, ...)`,其余(effectiveDays 构建、subPeriods、finalValue、totalDays、TWR 调用)byte-identical 不变。

- [ ] **Step 3: portfolioTWR 创建 cache 传 computeTWR(full + range 共享)**

`portfolioTWR`(L1289-1306)在两次 computeTWR 调用前创建 cache:

```go
	cache := mvCache{}
	full, _ = s.computeTWR(ctx, tenantID, accountID, base, rateBase, trades, cashFlowDays, cashFlowDays[0], cache)
	rng, _ = s.computeTWR(ctx, tenantID, accountID, base, rateBase, trades, cashFlowDays, rangeStart, cache)
	return full, rng, nil
```

> holdingTWR(L1369)不调 computeTWR(独立),不动。

- [ ] **Step 4: 写 cache 正确性测(hit 不重算)**

在 twr_service_test.go 加测:构造 portfolioTWR test(double cashFlowDays in [rangeStart, now]),用 mock `securityRepo.FindByID` 或 `priceAtOrBefore` 计数(per marketValue call),验证 cache 后调用数 < 无 cache(range BV hit)。或加 cache hit counter 测试 hook。

简化方案:断言 portfolioTWR 结果 byte-identical 现有 range TWR 测(回归)+ 新测验证同 (accountID, qtyAsOf, priceAsOf) 第二次 computeTWR(range)的 marketValue 调用走 cache(mock 计数减半)。

- [ ] **Step 5: 运行 + 回归验证(byte-identical)**

Run: `cd yucai/server && go test ./internal/holding/application/... -count=1`
Expected: 现有 range TWR Task 1/2 测全绿(全期 + range byte-identical,cache 透明)+ 新 cache 测绿。若 range TWR 测 FAIL,cache 不透明 —— 核对 cachedMV 返回值与 marketValueAtDateAsOfWithTrades 一致。

- [ ] **Step 6: Commit**

```bash
git add yucai/server/internal/holding/application/service.go yucai/server/internal/holding/application/twr_service_test.go
git commit -m "perf(holding/twr): request-scoped mvCache,full+range computeTWR 共享 BV(透明 byte-identical)"
```

---

### Task 2: benchmark 收益验证

**Files:**
- Test: `yucai/server/internal/holding/application/twr_service_test.go`(加 benchmark)

**Interfaces:**
- Consumes: Task 1 的 cache(portfolioTWR)。

- [ ] **Step 1: 写 benchmark(N cashFlowDays × M holdings,marketValue 调用数 before/after)**

构造 N=10 cashFlowDays × M=5 holdings 的 fixture(stub price/qty),mock `securityRepo.FindByID`(或 `holdingRepo.FindAll`)计数,对比 portfolioTWR 的 marketValue 调用数:
- 无 cache 假设(理论):full N×2 + range |rangeDays|×2
- 有 cache(实际):full N×2(range BV 全 hit)

断言:有 cache 时 marketValue 调用数 ≈ full only(range 的 overlap 部分全 cache hit)。

> 若 mock 计数难(市场值 method 是 Service 内部),fallback:cache hit counter(cachedMV 内 testing hook)或 timing benchmark(`go test -bench`)。核心是证明 range 的 BV 不重算。

- [ ] **Step 2: 运行 benchmark + 验证收益**

Run: `cd yucai/server && go test ./internal/holding/application/... -count=1 -run TestPortfolioTWRCacheBenchmark -v`
Expected: cache 后 marketValue 调用数显著少于无 cache(range overlap 全 hit)。

- [ ] **Step 3: Commit**

```bash
git add yucai/server/internal/holding/application/twr_service_test.go
git commit -m "test(holding/twr): cache 收益 benchmark(range BV hit full,marketValue 调用数验证)"
```

---

## 全量回归(最后)

- [ ] **Run: `cd yucai/server && go test ./... `** —— 全绿(全期 + range TWR byte-identical;XIRR/holdingTWR 不破;cache 测绿)。
- [ ] **Run: `cd yucai/client && flutter analyze`** —— 22 error 基线全 `*.pbserver.dart`(零 client 改动,不受影响)。
