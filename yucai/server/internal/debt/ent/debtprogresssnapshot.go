// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/debt/ent/debtprogresssnapshot"
)

// DebtProgressSnapshot is the model entity for the DebtProgressSnapshot schema.
type DebtProgressSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// FK to DebtDetails
	DebtID uuid.UUID `json:"debt_id,omitempty"`
	// SnapshotDate holds the value of the "snapshot_date" field.
	SnapshotDate time.Time `json:"snapshot_date,omitempty"`
	// debt total principal at snapshot_date, original currency
	TotalPrincipalCents int64 `json:"total_principal_cents,omitempty"`
	// remaining principal at snapshot_date, original currency
	RemainingPrincipalCents int64 `json:"remaining_principal_cents,omitempty"`
	// cumulative paid (principal+interest) at snapshot_date, original currency
	PaidTotalCents int64 `json:"paid_total_cents,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DebtProgressSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case debtprogresssnapshot.FieldTotalPrincipalCents, debtprogresssnapshot.FieldRemainingPrincipalCents, debtprogresssnapshot.FieldPaidTotalCents:
			values[i] = new(sql.NullInt64)
		case debtprogresssnapshot.FieldSnapshotDate, debtprogresssnapshot.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case debtprogresssnapshot.FieldID, debtprogresssnapshot.FieldTenantID, debtprogresssnapshot.FieldDebtID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DebtProgressSnapshot fields.
func (dps *DebtProgressSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case debtprogresssnapshot.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				dps.ID = *value
			}
		case debtprogresssnapshot.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				dps.TenantID = *value
			}
		case debtprogresssnapshot.FieldDebtID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field debt_id", values[i])
			} else if value != nil {
				dps.DebtID = *value
			}
		case debtprogresssnapshot.FieldSnapshotDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field snapshot_date", values[i])
			} else if value.Valid {
				dps.SnapshotDate = value.Time
			}
		case debtprogresssnapshot.FieldTotalPrincipalCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_principal_cents", values[i])
			} else if value.Valid {
				dps.TotalPrincipalCents = value.Int64
			}
		case debtprogresssnapshot.FieldRemainingPrincipalCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field remaining_principal_cents", values[i])
			} else if value.Valid {
				dps.RemainingPrincipalCents = value.Int64
			}
		case debtprogresssnapshot.FieldPaidTotalCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field paid_total_cents", values[i])
			} else if value.Valid {
				dps.PaidTotalCents = value.Int64
			}
		case debtprogresssnapshot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dps.CreatedAt = value.Time
			}
		default:
			dps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DebtProgressSnapshot.
// This includes values selected through modifiers, order, etc.
func (dps *DebtProgressSnapshot) Value(name string) (ent.Value, error) {
	return dps.selectValues.Get(name)
}

// Update returns a builder for updating this DebtProgressSnapshot.
// Note that you need to call DebtProgressSnapshot.Unwrap() before calling this method if this DebtProgressSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (dps *DebtProgressSnapshot) Update() *DebtProgressSnapshotUpdateOne {
	return NewDebtProgressSnapshotClient(dps.config).UpdateOne(dps)
}

// Unwrap unwraps the DebtProgressSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dps *DebtProgressSnapshot) Unwrap() *DebtProgressSnapshot {
	_tx, ok := dps.config.driver.(*txDriver)
	if !ok {
		panic("ent: DebtProgressSnapshot is not a transactional entity")
	}
	dps.config.driver = _tx.drv
	return dps
}

// String implements the fmt.Stringer.
func (dps *DebtProgressSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("DebtProgressSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dps.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", dps.TenantID))
	builder.WriteString(", ")
	builder.WriteString("debt_id=")
	builder.WriteString(fmt.Sprintf("%v", dps.DebtID))
	builder.WriteString(", ")
	builder.WriteString("snapshot_date=")
	builder.WriteString(dps.SnapshotDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("total_principal_cents=")
	builder.WriteString(fmt.Sprintf("%v", dps.TotalPrincipalCents))
	builder.WriteString(", ")
	builder.WriteString("remaining_principal_cents=")
	builder.WriteString(fmt.Sprintf("%v", dps.RemainingPrincipalCents))
	builder.WriteString(", ")
	builder.WriteString("paid_total_cents=")
	builder.WriteString(fmt.Sprintf("%v", dps.PaidTotalCents))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dps.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DebtProgressSnapshots is a parsable slice of DebtProgressSnapshot.
type DebtProgressSnapshots []*DebtProgressSnapshot
