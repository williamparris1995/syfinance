// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/debt/ent/debtprogresssnapshot"
	"github.com/yucai/server/internal/debt/ent/predicate"
)

// DebtProgressSnapshotQuery is the builder for querying DebtProgressSnapshot entities.
type DebtProgressSnapshotQuery struct {
	config
	ctx        *QueryContext
	order      []debtprogresssnapshot.OrderOption
	inters     []Interceptor
	predicates []predicate.DebtProgressSnapshot
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DebtProgressSnapshotQuery builder.
func (dpsq *DebtProgressSnapshotQuery) Where(ps ...predicate.DebtProgressSnapshot) *DebtProgressSnapshotQuery {
	dpsq.predicates = append(dpsq.predicates, ps...)
	return dpsq
}

// Limit the number of records to be returned by this query.
func (dpsq *DebtProgressSnapshotQuery) Limit(limit int) *DebtProgressSnapshotQuery {
	dpsq.ctx.Limit = &limit
	return dpsq
}

// Offset to start from.
func (dpsq *DebtProgressSnapshotQuery) Offset(offset int) *DebtProgressSnapshotQuery {
	dpsq.ctx.Offset = &offset
	return dpsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dpsq *DebtProgressSnapshotQuery) Unique(unique bool) *DebtProgressSnapshotQuery {
	dpsq.ctx.Unique = &unique
	return dpsq
}

// Order specifies how the records should be ordered.
func (dpsq *DebtProgressSnapshotQuery) Order(o ...debtprogresssnapshot.OrderOption) *DebtProgressSnapshotQuery {
	dpsq.order = append(dpsq.order, o...)
	return dpsq
}

// First returns the first DebtProgressSnapshot entity from the query.
// Returns a *NotFoundError when no DebtProgressSnapshot was found.
func (dpsq *DebtProgressSnapshotQuery) First(ctx context.Context) (*DebtProgressSnapshot, error) {
	nodes, err := dpsq.Limit(1).All(setContextOp(ctx, dpsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{debtprogresssnapshot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) FirstX(ctx context.Context) *DebtProgressSnapshot {
	node, err := dpsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DebtProgressSnapshot ID from the query.
// Returns a *NotFoundError when no DebtProgressSnapshot ID was found.
func (dpsq *DebtProgressSnapshotQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dpsq.Limit(1).IDs(setContextOp(ctx, dpsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{debtprogresssnapshot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := dpsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DebtProgressSnapshot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DebtProgressSnapshot entity is found.
// Returns a *NotFoundError when no DebtProgressSnapshot entities are found.
func (dpsq *DebtProgressSnapshotQuery) Only(ctx context.Context) (*DebtProgressSnapshot, error) {
	nodes, err := dpsq.Limit(2).All(setContextOp(ctx, dpsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{debtprogresssnapshot.Label}
	default:
		return nil, &NotSingularError{debtprogresssnapshot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) OnlyX(ctx context.Context) *DebtProgressSnapshot {
	node, err := dpsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DebtProgressSnapshot ID in the query.
// Returns a *NotSingularError when more than one DebtProgressSnapshot ID is found.
// Returns a *NotFoundError when no entities are found.
func (dpsq *DebtProgressSnapshotQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dpsq.Limit(2).IDs(setContextOp(ctx, dpsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{debtprogresssnapshot.Label}
	default:
		err = &NotSingularError{debtprogresssnapshot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := dpsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DebtProgressSnapshots.
func (dpsq *DebtProgressSnapshotQuery) All(ctx context.Context) ([]*DebtProgressSnapshot, error) {
	ctx = setContextOp(ctx, dpsq.ctx, ent.OpQueryAll)
	if err := dpsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DebtProgressSnapshot, *DebtProgressSnapshotQuery]()
	return withInterceptors[[]*DebtProgressSnapshot](ctx, dpsq, qr, dpsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) AllX(ctx context.Context) []*DebtProgressSnapshot {
	nodes, err := dpsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DebtProgressSnapshot IDs.
func (dpsq *DebtProgressSnapshotQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if dpsq.ctx.Unique == nil && dpsq.path != nil {
		dpsq.Unique(true)
	}
	ctx = setContextOp(ctx, dpsq.ctx, ent.OpQueryIDs)
	if err = dpsq.Select(debtprogresssnapshot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := dpsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dpsq *DebtProgressSnapshotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dpsq.ctx, ent.OpQueryCount)
	if err := dpsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dpsq, querierCount[*DebtProgressSnapshotQuery](), dpsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) CountX(ctx context.Context) int {
	count, err := dpsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dpsq *DebtProgressSnapshotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dpsq.ctx, ent.OpQueryExist)
	switch _, err := dpsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dpsq *DebtProgressSnapshotQuery) ExistX(ctx context.Context) bool {
	exist, err := dpsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DebtProgressSnapshotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dpsq *DebtProgressSnapshotQuery) Clone() *DebtProgressSnapshotQuery {
	if dpsq == nil {
		return nil
	}
	return &DebtProgressSnapshotQuery{
		config:     dpsq.config,
		ctx:        dpsq.ctx.Clone(),
		order:      append([]debtprogresssnapshot.OrderOption{}, dpsq.order...),
		inters:     append([]Interceptor{}, dpsq.inters...),
		predicates: append([]predicate.DebtProgressSnapshot{}, dpsq.predicates...),
		// clone intermediate query.
		sql:  dpsq.sql.Clone(),
		path: dpsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DebtProgressSnapshot.Query().
//		GroupBy(debtprogresssnapshot.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dpsq *DebtProgressSnapshotQuery) GroupBy(field string, fields ...string) *DebtProgressSnapshotGroupBy {
	dpsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DebtProgressSnapshotGroupBy{build: dpsq}
	grbuild.flds = &dpsq.ctx.Fields
	grbuild.label = debtprogresssnapshot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.DebtProgressSnapshot.Query().
//		Select(debtprogresssnapshot.FieldTenantID).
//		Scan(ctx, &v)
func (dpsq *DebtProgressSnapshotQuery) Select(fields ...string) *DebtProgressSnapshotSelect {
	dpsq.ctx.Fields = append(dpsq.ctx.Fields, fields...)
	sbuild := &DebtProgressSnapshotSelect{DebtProgressSnapshotQuery: dpsq}
	sbuild.label = debtprogresssnapshot.Label
	sbuild.flds, sbuild.scan = &dpsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DebtProgressSnapshotSelect configured with the given aggregations.
func (dpsq *DebtProgressSnapshotQuery) Aggregate(fns ...AggregateFunc) *DebtProgressSnapshotSelect {
	return dpsq.Select().Aggregate(fns...)
}

func (dpsq *DebtProgressSnapshotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dpsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dpsq); err != nil {
				return err
			}
		}
	}
	for _, f := range dpsq.ctx.Fields {
		if !debtprogresssnapshot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dpsq.path != nil {
		prev, err := dpsq.path(ctx)
		if err != nil {
			return err
		}
		dpsq.sql = prev
	}
	return nil
}

func (dpsq *DebtProgressSnapshotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DebtProgressSnapshot, error) {
	var (
		nodes = []*DebtProgressSnapshot{}
		_spec = dpsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DebtProgressSnapshot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DebtProgressSnapshot{config: dpsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dpsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dpsq *DebtProgressSnapshotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dpsq.querySpec()
	_spec.Node.Columns = dpsq.ctx.Fields
	if len(dpsq.ctx.Fields) > 0 {
		_spec.Unique = dpsq.ctx.Unique != nil && *dpsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dpsq.driver, _spec)
}

func (dpsq *DebtProgressSnapshotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(debtprogresssnapshot.Table, debtprogresssnapshot.Columns, sqlgraph.NewFieldSpec(debtprogresssnapshot.FieldID, field.TypeUUID))
	_spec.From = dpsq.sql
	if unique := dpsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dpsq.path != nil {
		_spec.Unique = true
	}
	if fields := dpsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, debtprogresssnapshot.FieldID)
		for i := range fields {
			if fields[i] != debtprogresssnapshot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dpsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dpsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dpsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dpsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dpsq *DebtProgressSnapshotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dpsq.driver.Dialect())
	t1 := builder.Table(debtprogresssnapshot.Table)
	columns := dpsq.ctx.Fields
	if len(columns) == 0 {
		columns = debtprogresssnapshot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dpsq.sql != nil {
		selector = dpsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dpsq.ctx.Unique != nil && *dpsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dpsq.predicates {
		p(selector)
	}
	for _, p := range dpsq.order {
		p(selector)
	}
	if offset := dpsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dpsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DebtProgressSnapshotGroupBy is the group-by builder for DebtProgressSnapshot entities.
type DebtProgressSnapshotGroupBy struct {
	selector
	build *DebtProgressSnapshotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dpsgb *DebtProgressSnapshotGroupBy) Aggregate(fns ...AggregateFunc) *DebtProgressSnapshotGroupBy {
	dpsgb.fns = append(dpsgb.fns, fns...)
	return dpsgb
}

// Scan applies the selector query and scans the result into the given value.
func (dpsgb *DebtProgressSnapshotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dpsgb.build.ctx, ent.OpQueryGroupBy)
	if err := dpsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DebtProgressSnapshotQuery, *DebtProgressSnapshotGroupBy](ctx, dpsgb.build, dpsgb, dpsgb.build.inters, v)
}

func (dpsgb *DebtProgressSnapshotGroupBy) sqlScan(ctx context.Context, root *DebtProgressSnapshotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dpsgb.fns))
	for _, fn := range dpsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dpsgb.flds)+len(dpsgb.fns))
		for _, f := range *dpsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dpsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dpsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DebtProgressSnapshotSelect is the builder for selecting fields of DebtProgressSnapshot entities.
type DebtProgressSnapshotSelect struct {
	*DebtProgressSnapshotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dpss *DebtProgressSnapshotSelect) Aggregate(fns ...AggregateFunc) *DebtProgressSnapshotSelect {
	dpss.fns = append(dpss.fns, fns...)
	return dpss
}

// Scan applies the selector query and scans the result into the given value.
func (dpss *DebtProgressSnapshotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dpss.ctx, ent.OpQuerySelect)
	if err := dpss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DebtProgressSnapshotQuery, *DebtProgressSnapshotSelect](ctx, dpss.DebtProgressSnapshotQuery, dpss, dpss.inters, v)
}

func (dpss *DebtProgressSnapshotSelect) sqlScan(ctx context.Context, root *DebtProgressSnapshotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dpss.fns))
	for _, fn := range dpss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dpss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dpss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
