// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/template/ent/predicate"
	"github.com/yucai/server/internal/template/ent/templaterecordlog"
)

// TemplateRecordLogQuery is the builder for querying TemplateRecordLog entities.
type TemplateRecordLogQuery struct {
	config
	ctx        *QueryContext
	order      []templaterecordlog.OrderOption
	inters     []Interceptor
	predicates []predicate.TemplateRecordLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TemplateRecordLogQuery builder.
func (trlq *TemplateRecordLogQuery) Where(ps ...predicate.TemplateRecordLog) *TemplateRecordLogQuery {
	trlq.predicates = append(trlq.predicates, ps...)
	return trlq
}

// Limit the number of records to be returned by this query.
func (trlq *TemplateRecordLogQuery) Limit(limit int) *TemplateRecordLogQuery {
	trlq.ctx.Limit = &limit
	return trlq
}

// Offset to start from.
func (trlq *TemplateRecordLogQuery) Offset(offset int) *TemplateRecordLogQuery {
	trlq.ctx.Offset = &offset
	return trlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (trlq *TemplateRecordLogQuery) Unique(unique bool) *TemplateRecordLogQuery {
	trlq.ctx.Unique = &unique
	return trlq
}

// Order specifies how the records should be ordered.
func (trlq *TemplateRecordLogQuery) Order(o ...templaterecordlog.OrderOption) *TemplateRecordLogQuery {
	trlq.order = append(trlq.order, o...)
	return trlq
}

// First returns the first TemplateRecordLog entity from the query.
// Returns a *NotFoundError when no TemplateRecordLog was found.
func (trlq *TemplateRecordLogQuery) First(ctx context.Context) (*TemplateRecordLog, error) {
	nodes, err := trlq.Limit(1).All(setContextOp(ctx, trlq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{templaterecordlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) FirstX(ctx context.Context) *TemplateRecordLog {
	node, err := trlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TemplateRecordLog ID from the query.
// Returns a *NotFoundError when no TemplateRecordLog ID was found.
func (trlq *TemplateRecordLogQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = trlq.Limit(1).IDs(setContextOp(ctx, trlq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{templaterecordlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := trlq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TemplateRecordLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TemplateRecordLog entity is found.
// Returns a *NotFoundError when no TemplateRecordLog entities are found.
func (trlq *TemplateRecordLogQuery) Only(ctx context.Context) (*TemplateRecordLog, error) {
	nodes, err := trlq.Limit(2).All(setContextOp(ctx, trlq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{templaterecordlog.Label}
	default:
		return nil, &NotSingularError{templaterecordlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) OnlyX(ctx context.Context) *TemplateRecordLog {
	node, err := trlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TemplateRecordLog ID in the query.
// Returns a *NotSingularError when more than one TemplateRecordLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (trlq *TemplateRecordLogQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = trlq.Limit(2).IDs(setContextOp(ctx, trlq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{templaterecordlog.Label}
	default:
		err = &NotSingularError{templaterecordlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := trlq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TemplateRecordLogs.
func (trlq *TemplateRecordLogQuery) All(ctx context.Context) ([]*TemplateRecordLog, error) {
	ctx = setContextOp(ctx, trlq.ctx, ent.OpQueryAll)
	if err := trlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TemplateRecordLog, *TemplateRecordLogQuery]()
	return withInterceptors[[]*TemplateRecordLog](ctx, trlq, qr, trlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) AllX(ctx context.Context) []*TemplateRecordLog {
	nodes, err := trlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TemplateRecordLog IDs.
func (trlq *TemplateRecordLogQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if trlq.ctx.Unique == nil && trlq.path != nil {
		trlq.Unique(true)
	}
	ctx = setContextOp(ctx, trlq.ctx, ent.OpQueryIDs)
	if err = trlq.Select(templaterecordlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := trlq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (trlq *TemplateRecordLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, trlq.ctx, ent.OpQueryCount)
	if err := trlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, trlq, querierCount[*TemplateRecordLogQuery](), trlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) CountX(ctx context.Context) int {
	count, err := trlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (trlq *TemplateRecordLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, trlq.ctx, ent.OpQueryExist)
	switch _, err := trlq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (trlq *TemplateRecordLogQuery) ExistX(ctx context.Context) bool {
	exist, err := trlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TemplateRecordLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (trlq *TemplateRecordLogQuery) Clone() *TemplateRecordLogQuery {
	if trlq == nil {
		return nil
	}
	return &TemplateRecordLogQuery{
		config:     trlq.config,
		ctx:        trlq.ctx.Clone(),
		order:      append([]templaterecordlog.OrderOption{}, trlq.order...),
		inters:     append([]Interceptor{}, trlq.inters...),
		predicates: append([]predicate.TemplateRecordLog{}, trlq.predicates...),
		// clone intermediate query.
		sql:  trlq.sql.Clone(),
		path: trlq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TemplateRecordLog.Query().
//		GroupBy(templaterecordlog.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (trlq *TemplateRecordLogQuery) GroupBy(field string, fields ...string) *TemplateRecordLogGroupBy {
	trlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TemplateRecordLogGroupBy{build: trlq}
	grbuild.flds = &trlq.ctx.Fields
	grbuild.label = templaterecordlog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.TemplateRecordLog.Query().
//		Select(templaterecordlog.FieldTenantID).
//		Scan(ctx, &v)
func (trlq *TemplateRecordLogQuery) Select(fields ...string) *TemplateRecordLogSelect {
	trlq.ctx.Fields = append(trlq.ctx.Fields, fields...)
	sbuild := &TemplateRecordLogSelect{TemplateRecordLogQuery: trlq}
	sbuild.label = templaterecordlog.Label
	sbuild.flds, sbuild.scan = &trlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TemplateRecordLogSelect configured with the given aggregations.
func (trlq *TemplateRecordLogQuery) Aggregate(fns ...AggregateFunc) *TemplateRecordLogSelect {
	return trlq.Select().Aggregate(fns...)
}

func (trlq *TemplateRecordLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range trlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, trlq); err != nil {
				return err
			}
		}
	}
	for _, f := range trlq.ctx.Fields {
		if !templaterecordlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if trlq.path != nil {
		prev, err := trlq.path(ctx)
		if err != nil {
			return err
		}
		trlq.sql = prev
	}
	return nil
}

func (trlq *TemplateRecordLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TemplateRecordLog, error) {
	var (
		nodes = []*TemplateRecordLog{}
		_spec = trlq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TemplateRecordLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TemplateRecordLog{config: trlq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, trlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (trlq *TemplateRecordLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := trlq.querySpec()
	_spec.Node.Columns = trlq.ctx.Fields
	if len(trlq.ctx.Fields) > 0 {
		_spec.Unique = trlq.ctx.Unique != nil && *trlq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, trlq.driver, _spec)
}

func (trlq *TemplateRecordLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(templaterecordlog.Table, templaterecordlog.Columns, sqlgraph.NewFieldSpec(templaterecordlog.FieldID, field.TypeUUID))
	_spec.From = trlq.sql
	if unique := trlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if trlq.path != nil {
		_spec.Unique = true
	}
	if fields := trlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, templaterecordlog.FieldID)
		for i := range fields {
			if fields[i] != templaterecordlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := trlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := trlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := trlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := trlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (trlq *TemplateRecordLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(trlq.driver.Dialect())
	t1 := builder.Table(templaterecordlog.Table)
	columns := trlq.ctx.Fields
	if len(columns) == 0 {
		columns = templaterecordlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if trlq.sql != nil {
		selector = trlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if trlq.ctx.Unique != nil && *trlq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range trlq.predicates {
		p(selector)
	}
	for _, p := range trlq.order {
		p(selector)
	}
	if offset := trlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := trlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TemplateRecordLogGroupBy is the group-by builder for TemplateRecordLog entities.
type TemplateRecordLogGroupBy struct {
	selector
	build *TemplateRecordLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (trlgb *TemplateRecordLogGroupBy) Aggregate(fns ...AggregateFunc) *TemplateRecordLogGroupBy {
	trlgb.fns = append(trlgb.fns, fns...)
	return trlgb
}

// Scan applies the selector query and scans the result into the given value.
func (trlgb *TemplateRecordLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, trlgb.build.ctx, ent.OpQueryGroupBy)
	if err := trlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TemplateRecordLogQuery, *TemplateRecordLogGroupBy](ctx, trlgb.build, trlgb, trlgb.build.inters, v)
}

func (trlgb *TemplateRecordLogGroupBy) sqlScan(ctx context.Context, root *TemplateRecordLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(trlgb.fns))
	for _, fn := range trlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*trlgb.flds)+len(trlgb.fns))
		for _, f := range *trlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*trlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := trlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TemplateRecordLogSelect is the builder for selecting fields of TemplateRecordLog entities.
type TemplateRecordLogSelect struct {
	*TemplateRecordLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (trls *TemplateRecordLogSelect) Aggregate(fns ...AggregateFunc) *TemplateRecordLogSelect {
	trls.fns = append(trls.fns, fns...)
	return trls
}

// Scan applies the selector query and scans the result into the given value.
func (trls *TemplateRecordLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, trls.ctx, ent.OpQuerySelect)
	if err := trls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TemplateRecordLogQuery, *TemplateRecordLogSelect](ctx, trls.TemplateRecordLogQuery, trls, trls.inters, v)
}

func (trls *TemplateRecordLogSelect) sqlScan(ctx context.Context, root *TemplateRecordLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(trls.fns))
	for _, fn := range trls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*trls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := trls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
