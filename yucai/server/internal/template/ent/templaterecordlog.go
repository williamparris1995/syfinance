// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/template/ent/templaterecordlog"
	"github.com/yucai/server/internal/template/ent/transactiontemplate"
)

// TemplateRecordLog is the model entity for the TemplateRecordLog schema.
type TemplateRecordLog struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// FK to transaction_template — which template was recorded
	TemplateID uuid.UUID `json:"template_id,omitempty"`
	// The template.NextDate that was recorded (idempotency key component)
	RecordDate time.Time `json:"record_date,omitempty"`
	// FK to the recorded transaction; back-filled after recorder.Record
	TransactionID *uuid.UUID `json:"transaction_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TemplateRecordLogQuery when eager-loading is set.
	Edges        TemplateRecordLogEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TemplateRecordLogEdges holds the relations/edges for other nodes in the graph.
type TemplateRecordLogEdges struct {
	// Template holds the value of the template edge.
	Template *TransactionTemplate `json:"template,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TemplateOrErr returns the Template value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TemplateRecordLogEdges) TemplateOrErr() (*TransactionTemplate, error) {
	if e.Template != nil {
		return e.Template, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: transactiontemplate.Label}
	}
	return nil, &NotLoadedError{edge: "template"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TemplateRecordLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case templaterecordlog.FieldTransactionID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case templaterecordlog.FieldRecordDate, templaterecordlog.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case templaterecordlog.FieldID, templaterecordlog.FieldTenantID, templaterecordlog.FieldTemplateID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TemplateRecordLog fields.
func (trl *TemplateRecordLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case templaterecordlog.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				trl.ID = *value
			}
		case templaterecordlog.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				trl.TenantID = *value
			}
		case templaterecordlog.FieldTemplateID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field template_id", values[i])
			} else if value != nil {
				trl.TemplateID = *value
			}
		case templaterecordlog.FieldRecordDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field record_date", values[i])
			} else if value.Valid {
				trl.RecordDate = value.Time
			}
		case templaterecordlog.FieldTransactionID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field transaction_id", values[i])
			} else if value.Valid {
				trl.TransactionID = new(uuid.UUID)
				*trl.TransactionID = *value.S.(*uuid.UUID)
			}
		case templaterecordlog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				trl.CreatedAt = value.Time
			}
		default:
			trl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TemplateRecordLog.
// This includes values selected through modifiers, order, etc.
func (trl *TemplateRecordLog) Value(name string) (ent.Value, error) {
	return trl.selectValues.Get(name)
}

// QueryTemplate queries the "template" edge of the TemplateRecordLog entity.
func (trl *TemplateRecordLog) QueryTemplate() *TransactionTemplateQuery {
	return NewTemplateRecordLogClient(trl.config).QueryTemplate(trl)
}

// Update returns a builder for updating this TemplateRecordLog.
// Note that you need to call TemplateRecordLog.Unwrap() before calling this method if this TemplateRecordLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (trl *TemplateRecordLog) Update() *TemplateRecordLogUpdateOne {
	return NewTemplateRecordLogClient(trl.config).UpdateOne(trl)
}

// Unwrap unwraps the TemplateRecordLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (trl *TemplateRecordLog) Unwrap() *TemplateRecordLog {
	_tx, ok := trl.config.driver.(*txDriver)
	if !ok {
		panic("ent: TemplateRecordLog is not a transactional entity")
	}
	trl.config.driver = _tx.drv
	return trl
}

// String implements the fmt.Stringer.
func (trl *TemplateRecordLog) String() string {
	var builder strings.Builder
	builder.WriteString("TemplateRecordLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", trl.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", trl.TenantID))
	builder.WriteString(", ")
	builder.WriteString("template_id=")
	builder.WriteString(fmt.Sprintf("%v", trl.TemplateID))
	builder.WriteString(", ")
	builder.WriteString("record_date=")
	builder.WriteString(trl.RecordDate.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := trl.TransactionID; v != nil {
		builder.WriteString("transaction_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(trl.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TemplateRecordLogs is a parsable slice of TemplateRecordLog.
type TemplateRecordLogs []*TemplateRecordLog
