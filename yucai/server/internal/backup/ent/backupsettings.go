// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/backup/ent/backupsettings"
)

// BackupSettings is the model entity for the BackupSettings schema.
type BackupSettings struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// AutoBackup holds the value of the "auto_backup" field.
	AutoBackup bool `json:"auto_backup,omitempty"`
	// AutoBackupIntervalHours holds the value of the "auto_backup_interval_hours" field.
	AutoBackupIntervalHours int32 `json:"auto_backup_interval_hours,omitempty"`
	selectValues            sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BackupSettings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case backupsettings.FieldAutoBackup:
			values[i] = new(sql.NullBool)
		case backupsettings.FieldAutoBackupIntervalHours:
			values[i] = new(sql.NullInt64)
		case backupsettings.FieldID, backupsettings.FieldTenantID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BackupSettings fields.
func (bs *BackupSettings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case backupsettings.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				bs.ID = *value
			}
		case backupsettings.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				bs.TenantID = *value
			}
		case backupsettings.FieldAutoBackup:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field auto_backup", values[i])
			} else if value.Valid {
				bs.AutoBackup = value.Bool
			}
		case backupsettings.FieldAutoBackupIntervalHours:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field auto_backup_interval_hours", values[i])
			} else if value.Valid {
				bs.AutoBackupIntervalHours = int32(value.Int64)
			}
		default:
			bs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BackupSettings.
// This includes values selected through modifiers, order, etc.
func (bs *BackupSettings) Value(name string) (ent.Value, error) {
	return bs.selectValues.Get(name)
}

// Update returns a builder for updating this BackupSettings.
// Note that you need to call BackupSettings.Unwrap() before calling this method if this BackupSettings
// was returned from a transaction, and the transaction was committed or rolled back.
func (bs *BackupSettings) Update() *BackupSettingsUpdateOne {
	return NewBackupSettingsClient(bs.config).UpdateOne(bs)
}

// Unwrap unwraps the BackupSettings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bs *BackupSettings) Unwrap() *BackupSettings {
	_tx, ok := bs.config.driver.(*txDriver)
	if !ok {
		panic("ent: BackupSettings is not a transactional entity")
	}
	bs.config.driver = _tx.drv
	return bs
}

// String implements the fmt.Stringer.
func (bs *BackupSettings) String() string {
	var builder strings.Builder
	builder.WriteString("BackupSettings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bs.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", bs.TenantID))
	builder.WriteString(", ")
	builder.WriteString("auto_backup=")
	builder.WriteString(fmt.Sprintf("%v", bs.AutoBackup))
	builder.WriteString(", ")
	builder.WriteString("auto_backup_interval_hours=")
	builder.WriteString(fmt.Sprintf("%v", bs.AutoBackupIntervalHours))
	builder.WriteByte(')')
	return builder.String()
}

// BackupSettingsSlice is a parsable slice of BackupSettings.
type BackupSettingsSlice []*BackupSettings
