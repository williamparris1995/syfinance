// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/transaction/ent/transaction"
	"github.com/yucai/server/internal/transaction/ent/transactionentry"
)

// TransactionEntry is the model entity for the TransactionEntry schema.
type TransactionEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to Transaction
	TransactionID uuid.UUID `json:"transaction_id,omitempty"`
	// FK to Account
	AccountID uuid.UUID `json:"account_id,omitempty"`
	// ChartOfAccountCode holds the value of the "chart_of_account_code" field.
	ChartOfAccountCode string `json:"chart_of_account_code,omitempty"`
	// Debit amount in cents
	DebitCents int64 `json:"debit_cents,omitempty"`
	// Credit amount in cents
	CreditCents int64 `json:"credit_cents,omitempty"`
	// Note holds the value of the "note" field.
	Note string `json:"note,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TransactionEntryQuery when eager-loading is set.
	Edges        TransactionEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TransactionEntryEdges holds the relations/edges for other nodes in the graph.
type TransactionEntryEdges struct {
	// Transaction holds the value of the transaction edge.
	Transaction *Transaction `json:"transaction,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TransactionOrErr returns the Transaction value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TransactionEntryEdges) TransactionOrErr() (*Transaction, error) {
	if e.Transaction != nil {
		return e.Transaction, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: transaction.Label}
	}
	return nil, &NotLoadedError{edge: "transaction"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TransactionEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case transactionentry.FieldDebitCents, transactionentry.FieldCreditCents:
			values[i] = new(sql.NullInt64)
		case transactionentry.FieldChartOfAccountCode, transactionentry.FieldNote:
			values[i] = new(sql.NullString)
		case transactionentry.FieldID, transactionentry.FieldTransactionID, transactionentry.FieldAccountID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TransactionEntry fields.
func (te *TransactionEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case transactionentry.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				te.ID = *value
			}
		case transactionentry.FieldTransactionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field transaction_id", values[i])
			} else if value != nil {
				te.TransactionID = *value
			}
		case transactionentry.FieldAccountID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value != nil {
				te.AccountID = *value
			}
		case transactionentry.FieldChartOfAccountCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field chart_of_account_code", values[i])
			} else if value.Valid {
				te.ChartOfAccountCode = value.String
			}
		case transactionentry.FieldDebitCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field debit_cents", values[i])
			} else if value.Valid {
				te.DebitCents = value.Int64
			}
		case transactionentry.FieldCreditCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field credit_cents", values[i])
			} else if value.Valid {
				te.CreditCents = value.Int64
			}
		case transactionentry.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				te.Note = value.String
			}
		default:
			te.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TransactionEntry.
// This includes values selected through modifiers, order, etc.
func (te *TransactionEntry) Value(name string) (ent.Value, error) {
	return te.selectValues.Get(name)
}

// QueryTransaction queries the "transaction" edge of the TransactionEntry entity.
func (te *TransactionEntry) QueryTransaction() *TransactionQuery {
	return NewTransactionEntryClient(te.config).QueryTransaction(te)
}

// Update returns a builder for updating this TransactionEntry.
// Note that you need to call TransactionEntry.Unwrap() before calling this method if this TransactionEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (te *TransactionEntry) Update() *TransactionEntryUpdateOne {
	return NewTransactionEntryClient(te.config).UpdateOne(te)
}

// Unwrap unwraps the TransactionEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (te *TransactionEntry) Unwrap() *TransactionEntry {
	_tx, ok := te.config.driver.(*txDriver)
	if !ok {
		panic("ent: TransactionEntry is not a transactional entity")
	}
	te.config.driver = _tx.drv
	return te
}

// String implements the fmt.Stringer.
func (te *TransactionEntry) String() string {
	var builder strings.Builder
	builder.WriteString("TransactionEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", te.ID))
	builder.WriteString("transaction_id=")
	builder.WriteString(fmt.Sprintf("%v", te.TransactionID))
	builder.WriteString(", ")
	builder.WriteString("account_id=")
	builder.WriteString(fmt.Sprintf("%v", te.AccountID))
	builder.WriteString(", ")
	builder.WriteString("chart_of_account_code=")
	builder.WriteString(te.ChartOfAccountCode)
	builder.WriteString(", ")
	builder.WriteString("debit_cents=")
	builder.WriteString(fmt.Sprintf("%v", te.DebitCents))
	builder.WriteString(", ")
	builder.WriteString("credit_cents=")
	builder.WriteString(fmt.Sprintf("%v", te.CreditCents))
	builder.WriteString(", ")
	builder.WriteString("note=")
	builder.WriteString(te.Note)
	builder.WriteByte(')')
	return builder.String()
}

// TransactionEntries is a parsable slice of TransactionEntry.
type TransactionEntries []*TransactionEntry
