// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/account/ent/chartofaccounts"
	"github.com/yucai/server/internal/account/ent/predicate"
)

// ChartOfAccountsQuery is the builder for querying ChartOfAccounts entities.
type ChartOfAccountsQuery struct {
	config
	ctx        *QueryContext
	order      []chartofaccounts.OrderOption
	inters     []Interceptor
	predicates []predicate.ChartOfAccounts
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChartOfAccountsQuery builder.
func (coaq *ChartOfAccountsQuery) Where(ps ...predicate.ChartOfAccounts) *ChartOfAccountsQuery {
	coaq.predicates = append(coaq.predicates, ps...)
	return coaq
}

// Limit the number of records to be returned by this query.
func (coaq *ChartOfAccountsQuery) Limit(limit int) *ChartOfAccountsQuery {
	coaq.ctx.Limit = &limit
	return coaq
}

// Offset to start from.
func (coaq *ChartOfAccountsQuery) Offset(offset int) *ChartOfAccountsQuery {
	coaq.ctx.Offset = &offset
	return coaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (coaq *ChartOfAccountsQuery) Unique(unique bool) *ChartOfAccountsQuery {
	coaq.ctx.Unique = &unique
	return coaq
}

// Order specifies how the records should be ordered.
func (coaq *ChartOfAccountsQuery) Order(o ...chartofaccounts.OrderOption) *ChartOfAccountsQuery {
	coaq.order = append(coaq.order, o...)
	return coaq
}

// First returns the first ChartOfAccounts entity from the query.
// Returns a *NotFoundError when no ChartOfAccounts was found.
func (coaq *ChartOfAccountsQuery) First(ctx context.Context) (*ChartOfAccounts, error) {
	nodes, err := coaq.Limit(1).All(setContextOp(ctx, coaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{chartofaccounts.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) FirstX(ctx context.Context) *ChartOfAccounts {
	node, err := coaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ChartOfAccounts ID from the query.
// Returns a *NotFoundError when no ChartOfAccounts ID was found.
func (coaq *ChartOfAccountsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = coaq.Limit(1).IDs(setContextOp(ctx, coaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{chartofaccounts.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := coaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ChartOfAccounts entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ChartOfAccounts entity is found.
// Returns a *NotFoundError when no ChartOfAccounts entities are found.
func (coaq *ChartOfAccountsQuery) Only(ctx context.Context) (*ChartOfAccounts, error) {
	nodes, err := coaq.Limit(2).All(setContextOp(ctx, coaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{chartofaccounts.Label}
	default:
		return nil, &NotSingularError{chartofaccounts.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) OnlyX(ctx context.Context) *ChartOfAccounts {
	node, err := coaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ChartOfAccounts ID in the query.
// Returns a *NotSingularError when more than one ChartOfAccounts ID is found.
// Returns a *NotFoundError when no entities are found.
func (coaq *ChartOfAccountsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = coaq.Limit(2).IDs(setContextOp(ctx, coaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{chartofaccounts.Label}
	default:
		err = &NotSingularError{chartofaccounts.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := coaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ChartOfAccountsSlice.
func (coaq *ChartOfAccountsQuery) All(ctx context.Context) ([]*ChartOfAccounts, error) {
	ctx = setContextOp(ctx, coaq.ctx, ent.OpQueryAll)
	if err := coaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ChartOfAccounts, *ChartOfAccountsQuery]()
	return withInterceptors[[]*ChartOfAccounts](ctx, coaq, qr, coaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) AllX(ctx context.Context) []*ChartOfAccounts {
	nodes, err := coaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ChartOfAccounts IDs.
func (coaq *ChartOfAccountsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if coaq.ctx.Unique == nil && coaq.path != nil {
		coaq.Unique(true)
	}
	ctx = setContextOp(ctx, coaq.ctx, ent.OpQueryIDs)
	if err = coaq.Select(chartofaccounts.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := coaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (coaq *ChartOfAccountsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, coaq.ctx, ent.OpQueryCount)
	if err := coaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, coaq, querierCount[*ChartOfAccountsQuery](), coaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) CountX(ctx context.Context) int {
	count, err := coaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (coaq *ChartOfAccountsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, coaq.ctx, ent.OpQueryExist)
	switch _, err := coaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (coaq *ChartOfAccountsQuery) ExistX(ctx context.Context) bool {
	exist, err := coaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChartOfAccountsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (coaq *ChartOfAccountsQuery) Clone() *ChartOfAccountsQuery {
	if coaq == nil {
		return nil
	}
	return &ChartOfAccountsQuery{
		config:     coaq.config,
		ctx:        coaq.ctx.Clone(),
		order:      append([]chartofaccounts.OrderOption{}, coaq.order...),
		inters:     append([]Interceptor{}, coaq.inters...),
		predicates: append([]predicate.ChartOfAccounts{}, coaq.predicates...),
		// clone intermediate query.
		sql:  coaq.sql.Clone(),
		path: coaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ChartOfAccounts.Query().
//		GroupBy(chartofaccounts.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (coaq *ChartOfAccountsQuery) GroupBy(field string, fields ...string) *ChartOfAccountsGroupBy {
	coaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ChartOfAccountsGroupBy{build: coaq}
	grbuild.flds = &coaq.ctx.Fields
	grbuild.label = chartofaccounts.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.ChartOfAccounts.Query().
//		Select(chartofaccounts.FieldTenantID).
//		Scan(ctx, &v)
func (coaq *ChartOfAccountsQuery) Select(fields ...string) *ChartOfAccountsSelect {
	coaq.ctx.Fields = append(coaq.ctx.Fields, fields...)
	sbuild := &ChartOfAccountsSelect{ChartOfAccountsQuery: coaq}
	sbuild.label = chartofaccounts.Label
	sbuild.flds, sbuild.scan = &coaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ChartOfAccountsSelect configured with the given aggregations.
func (coaq *ChartOfAccountsQuery) Aggregate(fns ...AggregateFunc) *ChartOfAccountsSelect {
	return coaq.Select().Aggregate(fns...)
}

func (coaq *ChartOfAccountsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range coaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, coaq); err != nil {
				return err
			}
		}
	}
	for _, f := range coaq.ctx.Fields {
		if !chartofaccounts.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if coaq.path != nil {
		prev, err := coaq.path(ctx)
		if err != nil {
			return err
		}
		coaq.sql = prev
	}
	return nil
}

func (coaq *ChartOfAccountsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ChartOfAccounts, error) {
	var (
		nodes = []*ChartOfAccounts{}
		_spec = coaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ChartOfAccounts).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ChartOfAccounts{config: coaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, coaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (coaq *ChartOfAccountsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := coaq.querySpec()
	_spec.Node.Columns = coaq.ctx.Fields
	if len(coaq.ctx.Fields) > 0 {
		_spec.Unique = coaq.ctx.Unique != nil && *coaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, coaq.driver, _spec)
}

func (coaq *ChartOfAccountsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(chartofaccounts.Table, chartofaccounts.Columns, sqlgraph.NewFieldSpec(chartofaccounts.FieldID, field.TypeUUID))
	_spec.From = coaq.sql
	if unique := coaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if coaq.path != nil {
		_spec.Unique = true
	}
	if fields := coaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, chartofaccounts.FieldID)
		for i := range fields {
			if fields[i] != chartofaccounts.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := coaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := coaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := coaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := coaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (coaq *ChartOfAccountsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(coaq.driver.Dialect())
	t1 := builder.Table(chartofaccounts.Table)
	columns := coaq.ctx.Fields
	if len(columns) == 0 {
		columns = chartofaccounts.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if coaq.sql != nil {
		selector = coaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if coaq.ctx.Unique != nil && *coaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range coaq.predicates {
		p(selector)
	}
	for _, p := range coaq.order {
		p(selector)
	}
	if offset := coaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := coaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ChartOfAccountsGroupBy is the group-by builder for ChartOfAccounts entities.
type ChartOfAccountsGroupBy struct {
	selector
	build *ChartOfAccountsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (coagb *ChartOfAccountsGroupBy) Aggregate(fns ...AggregateFunc) *ChartOfAccountsGroupBy {
	coagb.fns = append(coagb.fns, fns...)
	return coagb
}

// Scan applies the selector query and scans the result into the given value.
func (coagb *ChartOfAccountsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, coagb.build.ctx, ent.OpQueryGroupBy)
	if err := coagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChartOfAccountsQuery, *ChartOfAccountsGroupBy](ctx, coagb.build, coagb, coagb.build.inters, v)
}

func (coagb *ChartOfAccountsGroupBy) sqlScan(ctx context.Context, root *ChartOfAccountsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(coagb.fns))
	for _, fn := range coagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*coagb.flds)+len(coagb.fns))
		for _, f := range *coagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*coagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := coagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ChartOfAccountsSelect is the builder for selecting fields of ChartOfAccounts entities.
type ChartOfAccountsSelect struct {
	*ChartOfAccountsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (coas *ChartOfAccountsSelect) Aggregate(fns ...AggregateFunc) *ChartOfAccountsSelect {
	coas.fns = append(coas.fns, fns...)
	return coas
}

// Scan applies the selector query and scans the result into the given value.
func (coas *ChartOfAccountsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, coas.ctx, ent.OpQuerySelect)
	if err := coas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChartOfAccountsQuery, *ChartOfAccountsSelect](ctx, coas.ChartOfAccountsQuery, coas, coas.inters, v)
}

func (coas *ChartOfAccountsSelect) sqlScan(ctx context.Context, root *ChartOfAccountsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(coas.fns))
	for _, fn := range coas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*coas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := coas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
