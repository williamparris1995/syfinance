// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/goal/ent/goalprogresssnapshot"
	"github.com/yucai/server/internal/goal/ent/predicate"
)

// GoalProgressSnapshotQuery is the builder for querying GoalProgressSnapshot entities.
type GoalProgressSnapshotQuery struct {
	config
	ctx        *QueryContext
	order      []goalprogresssnapshot.OrderOption
	inters     []Interceptor
	predicates []predicate.GoalProgressSnapshot
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoalProgressSnapshotQuery builder.
func (gpsq *GoalProgressSnapshotQuery) Where(ps ...predicate.GoalProgressSnapshot) *GoalProgressSnapshotQuery {
	gpsq.predicates = append(gpsq.predicates, ps...)
	return gpsq
}

// Limit the number of records to be returned by this query.
func (gpsq *GoalProgressSnapshotQuery) Limit(limit int) *GoalProgressSnapshotQuery {
	gpsq.ctx.Limit = &limit
	return gpsq
}

// Offset to start from.
func (gpsq *GoalProgressSnapshotQuery) Offset(offset int) *GoalProgressSnapshotQuery {
	gpsq.ctx.Offset = &offset
	return gpsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gpsq *GoalProgressSnapshotQuery) Unique(unique bool) *GoalProgressSnapshotQuery {
	gpsq.ctx.Unique = &unique
	return gpsq
}

// Order specifies how the records should be ordered.
func (gpsq *GoalProgressSnapshotQuery) Order(o ...goalprogresssnapshot.OrderOption) *GoalProgressSnapshotQuery {
	gpsq.order = append(gpsq.order, o...)
	return gpsq
}

// First returns the first GoalProgressSnapshot entity from the query.
// Returns a *NotFoundError when no GoalProgressSnapshot was found.
func (gpsq *GoalProgressSnapshotQuery) First(ctx context.Context) (*GoalProgressSnapshot, error) {
	nodes, err := gpsq.Limit(1).All(setContextOp(ctx, gpsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goalprogresssnapshot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) FirstX(ctx context.Context) *GoalProgressSnapshot {
	node, err := gpsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoalProgressSnapshot ID from the query.
// Returns a *NotFoundError when no GoalProgressSnapshot ID was found.
func (gpsq *GoalProgressSnapshotQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = gpsq.Limit(1).IDs(setContextOp(ctx, gpsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goalprogresssnapshot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := gpsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoalProgressSnapshot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoalProgressSnapshot entity is found.
// Returns a *NotFoundError when no GoalProgressSnapshot entities are found.
func (gpsq *GoalProgressSnapshotQuery) Only(ctx context.Context) (*GoalProgressSnapshot, error) {
	nodes, err := gpsq.Limit(2).All(setContextOp(ctx, gpsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goalprogresssnapshot.Label}
	default:
		return nil, &NotSingularError{goalprogresssnapshot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) OnlyX(ctx context.Context) *GoalProgressSnapshot {
	node, err := gpsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoalProgressSnapshot ID in the query.
// Returns a *NotSingularError when more than one GoalProgressSnapshot ID is found.
// Returns a *NotFoundError when no entities are found.
func (gpsq *GoalProgressSnapshotQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = gpsq.Limit(2).IDs(setContextOp(ctx, gpsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goalprogresssnapshot.Label}
	default:
		err = &NotSingularError{goalprogresssnapshot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := gpsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoalProgressSnapshots.
func (gpsq *GoalProgressSnapshotQuery) All(ctx context.Context) ([]*GoalProgressSnapshot, error) {
	ctx = setContextOp(ctx, gpsq.ctx, ent.OpQueryAll)
	if err := gpsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GoalProgressSnapshot, *GoalProgressSnapshotQuery]()
	return withInterceptors[[]*GoalProgressSnapshot](ctx, gpsq, qr, gpsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) AllX(ctx context.Context) []*GoalProgressSnapshot {
	nodes, err := gpsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoalProgressSnapshot IDs.
func (gpsq *GoalProgressSnapshotQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if gpsq.ctx.Unique == nil && gpsq.path != nil {
		gpsq.Unique(true)
	}
	ctx = setContextOp(ctx, gpsq.ctx, ent.OpQueryIDs)
	if err = gpsq.Select(goalprogresssnapshot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := gpsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gpsq *GoalProgressSnapshotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gpsq.ctx, ent.OpQueryCount)
	if err := gpsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gpsq, querierCount[*GoalProgressSnapshotQuery](), gpsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) CountX(ctx context.Context) int {
	count, err := gpsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gpsq *GoalProgressSnapshotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gpsq.ctx, ent.OpQueryExist)
	switch _, err := gpsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gpsq *GoalProgressSnapshotQuery) ExistX(ctx context.Context) bool {
	exist, err := gpsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoalProgressSnapshotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gpsq *GoalProgressSnapshotQuery) Clone() *GoalProgressSnapshotQuery {
	if gpsq == nil {
		return nil
	}
	return &GoalProgressSnapshotQuery{
		config:     gpsq.config,
		ctx:        gpsq.ctx.Clone(),
		order:      append([]goalprogresssnapshot.OrderOption{}, gpsq.order...),
		inters:     append([]Interceptor{}, gpsq.inters...),
		predicates: append([]predicate.GoalProgressSnapshot{}, gpsq.predicates...),
		// clone intermediate query.
		sql:  gpsq.sql.Clone(),
		path: gpsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoalProgressSnapshot.Query().
//		GroupBy(goalprogresssnapshot.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (gpsq *GoalProgressSnapshotQuery) GroupBy(field string, fields ...string) *GoalProgressSnapshotGroupBy {
	gpsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GoalProgressSnapshotGroupBy{build: gpsq}
	grbuild.flds = &gpsq.ctx.Fields
	grbuild.label = goalprogresssnapshot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.GoalProgressSnapshot.Query().
//		Select(goalprogresssnapshot.FieldTenantID).
//		Scan(ctx, &v)
func (gpsq *GoalProgressSnapshotQuery) Select(fields ...string) *GoalProgressSnapshotSelect {
	gpsq.ctx.Fields = append(gpsq.ctx.Fields, fields...)
	sbuild := &GoalProgressSnapshotSelect{GoalProgressSnapshotQuery: gpsq}
	sbuild.label = goalprogresssnapshot.Label
	sbuild.flds, sbuild.scan = &gpsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GoalProgressSnapshotSelect configured with the given aggregations.
func (gpsq *GoalProgressSnapshotQuery) Aggregate(fns ...AggregateFunc) *GoalProgressSnapshotSelect {
	return gpsq.Select().Aggregate(fns...)
}

func (gpsq *GoalProgressSnapshotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gpsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gpsq); err != nil {
				return err
			}
		}
	}
	for _, f := range gpsq.ctx.Fields {
		if !goalprogresssnapshot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gpsq.path != nil {
		prev, err := gpsq.path(ctx)
		if err != nil {
			return err
		}
		gpsq.sql = prev
	}
	return nil
}

func (gpsq *GoalProgressSnapshotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoalProgressSnapshot, error) {
	var (
		nodes = []*GoalProgressSnapshot{}
		_spec = gpsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GoalProgressSnapshot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GoalProgressSnapshot{config: gpsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gpsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gpsq *GoalProgressSnapshotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gpsq.querySpec()
	_spec.Node.Columns = gpsq.ctx.Fields
	if len(gpsq.ctx.Fields) > 0 {
		_spec.Unique = gpsq.ctx.Unique != nil && *gpsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gpsq.driver, _spec)
}

func (gpsq *GoalProgressSnapshotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(goalprogresssnapshot.Table, goalprogresssnapshot.Columns, sqlgraph.NewFieldSpec(goalprogresssnapshot.FieldID, field.TypeUUID))
	_spec.From = gpsq.sql
	if unique := gpsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gpsq.path != nil {
		_spec.Unique = true
	}
	if fields := gpsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goalprogresssnapshot.FieldID)
		for i := range fields {
			if fields[i] != goalprogresssnapshot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gpsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gpsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gpsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gpsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gpsq *GoalProgressSnapshotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gpsq.driver.Dialect())
	t1 := builder.Table(goalprogresssnapshot.Table)
	columns := gpsq.ctx.Fields
	if len(columns) == 0 {
		columns = goalprogresssnapshot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gpsq.sql != nil {
		selector = gpsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gpsq.ctx.Unique != nil && *gpsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gpsq.predicates {
		p(selector)
	}
	for _, p := range gpsq.order {
		p(selector)
	}
	if offset := gpsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gpsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GoalProgressSnapshotGroupBy is the group-by builder for GoalProgressSnapshot entities.
type GoalProgressSnapshotGroupBy struct {
	selector
	build *GoalProgressSnapshotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gpsgb *GoalProgressSnapshotGroupBy) Aggregate(fns ...AggregateFunc) *GoalProgressSnapshotGroupBy {
	gpsgb.fns = append(gpsgb.fns, fns...)
	return gpsgb
}

// Scan applies the selector query and scans the result into the given value.
func (gpsgb *GoalProgressSnapshotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gpsgb.build.ctx, ent.OpQueryGroupBy)
	if err := gpsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoalProgressSnapshotQuery, *GoalProgressSnapshotGroupBy](ctx, gpsgb.build, gpsgb, gpsgb.build.inters, v)
}

func (gpsgb *GoalProgressSnapshotGroupBy) sqlScan(ctx context.Context, root *GoalProgressSnapshotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gpsgb.fns))
	for _, fn := range gpsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gpsgb.flds)+len(gpsgb.fns))
		for _, f := range *gpsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gpsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gpsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GoalProgressSnapshotSelect is the builder for selecting fields of GoalProgressSnapshot entities.
type GoalProgressSnapshotSelect struct {
	*GoalProgressSnapshotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gpss *GoalProgressSnapshotSelect) Aggregate(fns ...AggregateFunc) *GoalProgressSnapshotSelect {
	gpss.fns = append(gpss.fns, fns...)
	return gpss
}

// Scan applies the selector query and scans the result into the given value.
func (gpss *GoalProgressSnapshotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gpss.ctx, ent.OpQuerySelect)
	if err := gpss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoalProgressSnapshotQuery, *GoalProgressSnapshotSelect](ctx, gpss.GoalProgressSnapshotQuery, gpss, gpss.inters, v)
}

func (gpss *GoalProgressSnapshotSelect) sqlScan(ctx context.Context, root *GoalProgressSnapshotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gpss.fns))
	for _, fn := range gpss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gpss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gpss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
