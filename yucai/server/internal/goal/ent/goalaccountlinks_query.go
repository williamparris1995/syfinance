// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/goal/ent/goalaccountlinks"
	"github.com/yucai/server/internal/goal/ent/predicate"
)

// GoalAccountLinksQuery is the builder for querying GoalAccountLinks entities.
type GoalAccountLinksQuery struct {
	config
	ctx        *QueryContext
	order      []goalaccountlinks.OrderOption
	inters     []Interceptor
	predicates []predicate.GoalAccountLinks
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoalAccountLinksQuery builder.
func (galq *GoalAccountLinksQuery) Where(ps ...predicate.GoalAccountLinks) *GoalAccountLinksQuery {
	galq.predicates = append(galq.predicates, ps...)
	return galq
}

// Limit the number of records to be returned by this query.
func (galq *GoalAccountLinksQuery) Limit(limit int) *GoalAccountLinksQuery {
	galq.ctx.Limit = &limit
	return galq
}

// Offset to start from.
func (galq *GoalAccountLinksQuery) Offset(offset int) *GoalAccountLinksQuery {
	galq.ctx.Offset = &offset
	return galq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (galq *GoalAccountLinksQuery) Unique(unique bool) *GoalAccountLinksQuery {
	galq.ctx.Unique = &unique
	return galq
}

// Order specifies how the records should be ordered.
func (galq *GoalAccountLinksQuery) Order(o ...goalaccountlinks.OrderOption) *GoalAccountLinksQuery {
	galq.order = append(galq.order, o...)
	return galq
}

// First returns the first GoalAccountLinks entity from the query.
// Returns a *NotFoundError when no GoalAccountLinks was found.
func (galq *GoalAccountLinksQuery) First(ctx context.Context) (*GoalAccountLinks, error) {
	nodes, err := galq.Limit(1).All(setContextOp(ctx, galq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goalaccountlinks.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) FirstX(ctx context.Context) *GoalAccountLinks {
	node, err := galq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoalAccountLinks ID from the query.
// Returns a *NotFoundError when no GoalAccountLinks ID was found.
func (galq *GoalAccountLinksQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = galq.Limit(1).IDs(setContextOp(ctx, galq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goalaccountlinks.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := galq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoalAccountLinks entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoalAccountLinks entity is found.
// Returns a *NotFoundError when no GoalAccountLinks entities are found.
func (galq *GoalAccountLinksQuery) Only(ctx context.Context) (*GoalAccountLinks, error) {
	nodes, err := galq.Limit(2).All(setContextOp(ctx, galq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goalaccountlinks.Label}
	default:
		return nil, &NotSingularError{goalaccountlinks.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) OnlyX(ctx context.Context) *GoalAccountLinks {
	node, err := galq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoalAccountLinks ID in the query.
// Returns a *NotSingularError when more than one GoalAccountLinks ID is found.
// Returns a *NotFoundError when no entities are found.
func (galq *GoalAccountLinksQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = galq.Limit(2).IDs(setContextOp(ctx, galq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goalaccountlinks.Label}
	default:
		err = &NotSingularError{goalaccountlinks.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := galq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoalAccountLinksSlice.
func (galq *GoalAccountLinksQuery) All(ctx context.Context) ([]*GoalAccountLinks, error) {
	ctx = setContextOp(ctx, galq.ctx, ent.OpQueryAll)
	if err := galq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GoalAccountLinks, *GoalAccountLinksQuery]()
	return withInterceptors[[]*GoalAccountLinks](ctx, galq, qr, galq.inters)
}

// AllX is like All, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) AllX(ctx context.Context) []*GoalAccountLinks {
	nodes, err := galq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoalAccountLinks IDs.
func (galq *GoalAccountLinksQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if galq.ctx.Unique == nil && galq.path != nil {
		galq.Unique(true)
	}
	ctx = setContextOp(ctx, galq.ctx, ent.OpQueryIDs)
	if err = galq.Select(goalaccountlinks.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := galq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (galq *GoalAccountLinksQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, galq.ctx, ent.OpQueryCount)
	if err := galq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, galq, querierCount[*GoalAccountLinksQuery](), galq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) CountX(ctx context.Context) int {
	count, err := galq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (galq *GoalAccountLinksQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, galq.ctx, ent.OpQueryExist)
	switch _, err := galq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (galq *GoalAccountLinksQuery) ExistX(ctx context.Context) bool {
	exist, err := galq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoalAccountLinksQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (galq *GoalAccountLinksQuery) Clone() *GoalAccountLinksQuery {
	if galq == nil {
		return nil
	}
	return &GoalAccountLinksQuery{
		config:     galq.config,
		ctx:        galq.ctx.Clone(),
		order:      append([]goalaccountlinks.OrderOption{}, galq.order...),
		inters:     append([]Interceptor{}, galq.inters...),
		predicates: append([]predicate.GoalAccountLinks{}, galq.predicates...),
		// clone intermediate query.
		sql:  galq.sql.Clone(),
		path: galq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoalAccountLinks.Query().
//		GroupBy(goalaccountlinks.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (galq *GoalAccountLinksQuery) GroupBy(field string, fields ...string) *GoalAccountLinksGroupBy {
	galq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GoalAccountLinksGroupBy{build: galq}
	grbuild.flds = &galq.ctx.Fields
	grbuild.label = goalaccountlinks.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.GoalAccountLinks.Query().
//		Select(goalaccountlinks.FieldTenantID).
//		Scan(ctx, &v)
func (galq *GoalAccountLinksQuery) Select(fields ...string) *GoalAccountLinksSelect {
	galq.ctx.Fields = append(galq.ctx.Fields, fields...)
	sbuild := &GoalAccountLinksSelect{GoalAccountLinksQuery: galq}
	sbuild.label = goalaccountlinks.Label
	sbuild.flds, sbuild.scan = &galq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GoalAccountLinksSelect configured with the given aggregations.
func (galq *GoalAccountLinksQuery) Aggregate(fns ...AggregateFunc) *GoalAccountLinksSelect {
	return galq.Select().Aggregate(fns...)
}

func (galq *GoalAccountLinksQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range galq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, galq); err != nil {
				return err
			}
		}
	}
	for _, f := range galq.ctx.Fields {
		if !goalaccountlinks.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if galq.path != nil {
		prev, err := galq.path(ctx)
		if err != nil {
			return err
		}
		galq.sql = prev
	}
	return nil
}

func (galq *GoalAccountLinksQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoalAccountLinks, error) {
	var (
		nodes = []*GoalAccountLinks{}
		_spec = galq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GoalAccountLinks).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GoalAccountLinks{config: galq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, galq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (galq *GoalAccountLinksQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := galq.querySpec()
	_spec.Node.Columns = galq.ctx.Fields
	if len(galq.ctx.Fields) > 0 {
		_spec.Unique = galq.ctx.Unique != nil && *galq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, galq.driver, _spec)
}

func (galq *GoalAccountLinksQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(goalaccountlinks.Table, goalaccountlinks.Columns, sqlgraph.NewFieldSpec(goalaccountlinks.FieldID, field.TypeUUID))
	_spec.From = galq.sql
	if unique := galq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if galq.path != nil {
		_spec.Unique = true
	}
	if fields := galq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goalaccountlinks.FieldID)
		for i := range fields {
			if fields[i] != goalaccountlinks.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := galq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := galq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := galq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := galq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (galq *GoalAccountLinksQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(galq.driver.Dialect())
	t1 := builder.Table(goalaccountlinks.Table)
	columns := galq.ctx.Fields
	if len(columns) == 0 {
		columns = goalaccountlinks.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if galq.sql != nil {
		selector = galq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if galq.ctx.Unique != nil && *galq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range galq.predicates {
		p(selector)
	}
	for _, p := range galq.order {
		p(selector)
	}
	if offset := galq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := galq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GoalAccountLinksGroupBy is the group-by builder for GoalAccountLinks entities.
type GoalAccountLinksGroupBy struct {
	selector
	build *GoalAccountLinksQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (galgb *GoalAccountLinksGroupBy) Aggregate(fns ...AggregateFunc) *GoalAccountLinksGroupBy {
	galgb.fns = append(galgb.fns, fns...)
	return galgb
}

// Scan applies the selector query and scans the result into the given value.
func (galgb *GoalAccountLinksGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, galgb.build.ctx, ent.OpQueryGroupBy)
	if err := galgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoalAccountLinksQuery, *GoalAccountLinksGroupBy](ctx, galgb.build, galgb, galgb.build.inters, v)
}

func (galgb *GoalAccountLinksGroupBy) sqlScan(ctx context.Context, root *GoalAccountLinksQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(galgb.fns))
	for _, fn := range galgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*galgb.flds)+len(galgb.fns))
		for _, f := range *galgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*galgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := galgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GoalAccountLinksSelect is the builder for selecting fields of GoalAccountLinks entities.
type GoalAccountLinksSelect struct {
	*GoalAccountLinksQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gals *GoalAccountLinksSelect) Aggregate(fns ...AggregateFunc) *GoalAccountLinksSelect {
	gals.fns = append(gals.fns, fns...)
	return gals
}

// Scan applies the selector query and scans the result into the given value.
func (gals *GoalAccountLinksSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gals.ctx, ent.OpQuerySelect)
	if err := gals.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GoalAccountLinksQuery, *GoalAccountLinksSelect](ctx, gals.GoalAccountLinksQuery, gals, gals.inters, v)
}

func (gals *GoalAccountLinksSelect) sqlScan(ctx context.Context, root *GoalAccountLinksQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gals.fns))
	for _, fn := range gals.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gals.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gals.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
