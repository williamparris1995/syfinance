// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/goal/ent/goal"
	"github.com/yucai/server/internal/goal/ent/goalprogresssnapshot"
)

// GoalProgressSnapshot is the model entity for the GoalProgressSnapshot schema.
type GoalProgressSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// GoalID holds the value of the "goal_id" field.
	GoalID uuid.UUID `json:"goal_id,omitempty"`
	// SnapshotDate holds the value of the "snapshot_date" field.
	SnapshotDate time.Time `json:"snapshot_date,omitempty"`
	// goal progress at snapshot_date, original currency
	CurrentAmountCents int64 `json:"current_amount_cents,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GoalProgressSnapshotQuery when eager-loading is set.
	Edges        GoalProgressSnapshotEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GoalProgressSnapshotEdges holds the relations/edges for other nodes in the graph.
type GoalProgressSnapshotEdges struct {
	// Goal holds the value of the goal edge.
	Goal *Goal `json:"goal,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// GoalOrErr returns the Goal value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GoalProgressSnapshotEdges) GoalOrErr() (*Goal, error) {
	if e.Goal != nil {
		return e.Goal, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: goal.Label}
	}
	return nil, &NotLoadedError{edge: "goal"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GoalProgressSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case goalprogresssnapshot.FieldCurrentAmountCents:
			values[i] = new(sql.NullInt64)
		case goalprogresssnapshot.FieldSnapshotDate, goalprogresssnapshot.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case goalprogresssnapshot.FieldID, goalprogresssnapshot.FieldTenantID, goalprogresssnapshot.FieldGoalID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GoalProgressSnapshot fields.
func (gps *GoalProgressSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case goalprogresssnapshot.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				gps.ID = *value
			}
		case goalprogresssnapshot.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				gps.TenantID = *value
			}
		case goalprogresssnapshot.FieldGoalID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field goal_id", values[i])
			} else if value != nil {
				gps.GoalID = *value
			}
		case goalprogresssnapshot.FieldSnapshotDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field snapshot_date", values[i])
			} else if value.Valid {
				gps.SnapshotDate = value.Time
			}
		case goalprogresssnapshot.FieldCurrentAmountCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field current_amount_cents", values[i])
			} else if value.Valid {
				gps.CurrentAmountCents = value.Int64
			}
		case goalprogresssnapshot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				gps.CreatedAt = value.Time
			}
		default:
			gps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GoalProgressSnapshot.
// This includes values selected through modifiers, order, etc.
func (gps *GoalProgressSnapshot) Value(name string) (ent.Value, error) {
	return gps.selectValues.Get(name)
}

// QueryGoal queries the "goal" edge of the GoalProgressSnapshot entity.
func (gps *GoalProgressSnapshot) QueryGoal() *GoalQuery {
	return NewGoalProgressSnapshotClient(gps.config).QueryGoal(gps)
}

// Update returns a builder for updating this GoalProgressSnapshot.
// Note that you need to call GoalProgressSnapshot.Unwrap() before calling this method if this GoalProgressSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (gps *GoalProgressSnapshot) Update() *GoalProgressSnapshotUpdateOne {
	return NewGoalProgressSnapshotClient(gps.config).UpdateOne(gps)
}

// Unwrap unwraps the GoalProgressSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gps *GoalProgressSnapshot) Unwrap() *GoalProgressSnapshot {
	_tx, ok := gps.config.driver.(*txDriver)
	if !ok {
		panic("ent: GoalProgressSnapshot is not a transactional entity")
	}
	gps.config.driver = _tx.drv
	return gps
}

// String implements the fmt.Stringer.
func (gps *GoalProgressSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("GoalProgressSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gps.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", gps.TenantID))
	builder.WriteString(", ")
	builder.WriteString("goal_id=")
	builder.WriteString(fmt.Sprintf("%v", gps.GoalID))
	builder.WriteString(", ")
	builder.WriteString("snapshot_date=")
	builder.WriteString(gps.SnapshotDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("current_amount_cents=")
	builder.WriteString(fmt.Sprintf("%v", gps.CurrentAmountCents))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(gps.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GoalProgressSnapshots is a parsable slice of GoalProgressSnapshot.
type GoalProgressSnapshots []*GoalProgressSnapshot
