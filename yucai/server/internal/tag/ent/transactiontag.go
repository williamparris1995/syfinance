// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/tag/ent/transactiontag"
)

// TransactionTag is the model entity for the TransactionTag schema.
type TransactionTag struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TransactionID holds the value of the "transaction_id" field.
	TransactionID uuid.UUID `json:"transaction_id,omitempty"`
	// TagID holds the value of the "tag_id" field.
	TagID        uuid.UUID `json:"tag_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TransactionTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case transactiontag.FieldID, transactiontag.FieldTransactionID, transactiontag.FieldTagID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TransactionTag fields.
func (tt *TransactionTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case transactiontag.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tt.ID = *value
			}
		case transactiontag.FieldTransactionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field transaction_id", values[i])
			} else if value != nil {
				tt.TransactionID = *value
			}
		case transactiontag.FieldTagID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tag_id", values[i])
			} else if value != nil {
				tt.TagID = *value
			}
		default:
			tt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TransactionTag.
// This includes values selected through modifiers, order, etc.
func (tt *TransactionTag) Value(name string) (ent.Value, error) {
	return tt.selectValues.Get(name)
}

// Update returns a builder for updating this TransactionTag.
// Note that you need to call TransactionTag.Unwrap() before calling this method if this TransactionTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (tt *TransactionTag) Update() *TransactionTagUpdateOne {
	return NewTransactionTagClient(tt.config).UpdateOne(tt)
}

// Unwrap unwraps the TransactionTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tt *TransactionTag) Unwrap() *TransactionTag {
	_tx, ok := tt.config.driver.(*txDriver)
	if !ok {
		panic("ent: TransactionTag is not a transactional entity")
	}
	tt.config.driver = _tx.drv
	return tt
}

// String implements the fmt.Stringer.
func (tt *TransactionTag) String() string {
	var builder strings.Builder
	builder.WriteString("TransactionTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tt.ID))
	builder.WriteString("transaction_id=")
	builder.WriteString(fmt.Sprintf("%v", tt.TransactionID))
	builder.WriteString(", ")
	builder.WriteString("tag_id=")
	builder.WriteString(fmt.Sprintf("%v", tt.TagID))
	builder.WriteByte(')')
	return builder.String()
}

// TransactionTags is a parsable slice of TransactionTag.
type TransactionTags []*TransactionTag
