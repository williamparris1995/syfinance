// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/currency/ent/ratehistory"
)

// RateHistory is the model entity for the RateHistory schema.
type RateHistory struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ISO 4217 (CNY=base=1.0)
	CurrencyCode string `json:"currency_code,omitempty"`
	// RateDate holds the value of the "rate_date" field.
	RateDate time.Time `json:"rate_date,omitempty"`
	// to base (CNY)
	ExchangeRate float64 `json:"exchange_rate,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RateHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ratehistory.FieldExchangeRate:
			values[i] = new(sql.NullFloat64)
		case ratehistory.FieldCurrencyCode:
			values[i] = new(sql.NullString)
		case ratehistory.FieldRateDate, ratehistory.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case ratehistory.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RateHistory fields.
func (rh *RateHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ratehistory.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				rh.ID = *value
			}
		case ratehistory.FieldCurrencyCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field currency_code", values[i])
			} else if value.Valid {
				rh.CurrencyCode = value.String
			}
		case ratehistory.FieldRateDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field rate_date", values[i])
			} else if value.Valid {
				rh.RateDate = value.Time
			}
		case ratehistory.FieldExchangeRate:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field exchange_rate", values[i])
			} else if value.Valid {
				rh.ExchangeRate = value.Float64
			}
		case ratehistory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rh.CreatedAt = value.Time
			}
		default:
			rh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RateHistory.
// This includes values selected through modifiers, order, etc.
func (rh *RateHistory) Value(name string) (ent.Value, error) {
	return rh.selectValues.Get(name)
}

// Update returns a builder for updating this RateHistory.
// Note that you need to call RateHistory.Unwrap() before calling this method if this RateHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (rh *RateHistory) Update() *RateHistoryUpdateOne {
	return NewRateHistoryClient(rh.config).UpdateOne(rh)
}

// Unwrap unwraps the RateHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rh *RateHistory) Unwrap() *RateHistory {
	_tx, ok := rh.config.driver.(*txDriver)
	if !ok {
		panic("ent: RateHistory is not a transactional entity")
	}
	rh.config.driver = _tx.drv
	return rh
}

// String implements the fmt.Stringer.
func (rh *RateHistory) String() string {
	var builder strings.Builder
	builder.WriteString("RateHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rh.ID))
	builder.WriteString("currency_code=")
	builder.WriteString(rh.CurrencyCode)
	builder.WriteString(", ")
	builder.WriteString("rate_date=")
	builder.WriteString(rh.RateDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("exchange_rate=")
	builder.WriteString(fmt.Sprintf("%v", rh.ExchangeRate))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(rh.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RateHistories is a parsable slice of RateHistory.
type RateHistories []*RateHistory
