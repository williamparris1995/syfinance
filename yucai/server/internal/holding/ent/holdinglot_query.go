// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/holding/ent/holdinglot"
	"github.com/yucai/server/internal/holding/ent/predicate"
)

// HoldingLotQuery is the builder for querying HoldingLot entities.
type HoldingLotQuery struct {
	config
	ctx        *QueryContext
	order      []holdinglot.OrderOption
	inters     []Interceptor
	predicates []predicate.HoldingLot
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HoldingLotQuery builder.
func (hlq *HoldingLotQuery) Where(ps ...predicate.HoldingLot) *HoldingLotQuery {
	hlq.predicates = append(hlq.predicates, ps...)
	return hlq
}

// Limit the number of records to be returned by this query.
func (hlq *HoldingLotQuery) Limit(limit int) *HoldingLotQuery {
	hlq.ctx.Limit = &limit
	return hlq
}

// Offset to start from.
func (hlq *HoldingLotQuery) Offset(offset int) *HoldingLotQuery {
	hlq.ctx.Offset = &offset
	return hlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hlq *HoldingLotQuery) Unique(unique bool) *HoldingLotQuery {
	hlq.ctx.Unique = &unique
	return hlq
}

// Order specifies how the records should be ordered.
func (hlq *HoldingLotQuery) Order(o ...holdinglot.OrderOption) *HoldingLotQuery {
	hlq.order = append(hlq.order, o...)
	return hlq
}

// First returns the first HoldingLot entity from the query.
// Returns a *NotFoundError when no HoldingLot was found.
func (hlq *HoldingLotQuery) First(ctx context.Context) (*HoldingLot, error) {
	nodes, err := hlq.Limit(1).All(setContextOp(ctx, hlq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{holdinglot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hlq *HoldingLotQuery) FirstX(ctx context.Context) *HoldingLot {
	node, err := hlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HoldingLot ID from the query.
// Returns a *NotFoundError when no HoldingLot ID was found.
func (hlq *HoldingLotQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = hlq.Limit(1).IDs(setContextOp(ctx, hlq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{holdinglot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hlq *HoldingLotQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := hlq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HoldingLot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HoldingLot entity is found.
// Returns a *NotFoundError when no HoldingLot entities are found.
func (hlq *HoldingLotQuery) Only(ctx context.Context) (*HoldingLot, error) {
	nodes, err := hlq.Limit(2).All(setContextOp(ctx, hlq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{holdinglot.Label}
	default:
		return nil, &NotSingularError{holdinglot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hlq *HoldingLotQuery) OnlyX(ctx context.Context) *HoldingLot {
	node, err := hlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HoldingLot ID in the query.
// Returns a *NotSingularError when more than one HoldingLot ID is found.
// Returns a *NotFoundError when no entities are found.
func (hlq *HoldingLotQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = hlq.Limit(2).IDs(setContextOp(ctx, hlq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{holdinglot.Label}
	default:
		err = &NotSingularError{holdinglot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hlq *HoldingLotQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := hlq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HoldingLots.
func (hlq *HoldingLotQuery) All(ctx context.Context) ([]*HoldingLot, error) {
	ctx = setContextOp(ctx, hlq.ctx, ent.OpQueryAll)
	if err := hlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HoldingLot, *HoldingLotQuery]()
	return withInterceptors[[]*HoldingLot](ctx, hlq, qr, hlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hlq *HoldingLotQuery) AllX(ctx context.Context) []*HoldingLot {
	nodes, err := hlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HoldingLot IDs.
func (hlq *HoldingLotQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if hlq.ctx.Unique == nil && hlq.path != nil {
		hlq.Unique(true)
	}
	ctx = setContextOp(ctx, hlq.ctx, ent.OpQueryIDs)
	if err = hlq.Select(holdinglot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hlq *HoldingLotQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := hlq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hlq *HoldingLotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hlq.ctx, ent.OpQueryCount)
	if err := hlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hlq, querierCount[*HoldingLotQuery](), hlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hlq *HoldingLotQuery) CountX(ctx context.Context) int {
	count, err := hlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hlq *HoldingLotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hlq.ctx, ent.OpQueryExist)
	switch _, err := hlq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hlq *HoldingLotQuery) ExistX(ctx context.Context) bool {
	exist, err := hlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HoldingLotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hlq *HoldingLotQuery) Clone() *HoldingLotQuery {
	if hlq == nil {
		return nil
	}
	return &HoldingLotQuery{
		config:     hlq.config,
		ctx:        hlq.ctx.Clone(),
		order:      append([]holdinglot.OrderOption{}, hlq.order...),
		inters:     append([]Interceptor{}, hlq.inters...),
		predicates: append([]predicate.HoldingLot{}, hlq.predicates...),
		// clone intermediate query.
		sql:  hlq.sql.Clone(),
		path: hlq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HoldingLot.Query().
//		GroupBy(holdinglot.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hlq *HoldingLotQuery) GroupBy(field string, fields ...string) *HoldingLotGroupBy {
	hlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HoldingLotGroupBy{build: hlq}
	grbuild.flds = &hlq.ctx.Fields
	grbuild.label = holdinglot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.HoldingLot.Query().
//		Select(holdinglot.FieldTenantID).
//		Scan(ctx, &v)
func (hlq *HoldingLotQuery) Select(fields ...string) *HoldingLotSelect {
	hlq.ctx.Fields = append(hlq.ctx.Fields, fields...)
	sbuild := &HoldingLotSelect{HoldingLotQuery: hlq}
	sbuild.label = holdinglot.Label
	sbuild.flds, sbuild.scan = &hlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HoldingLotSelect configured with the given aggregations.
func (hlq *HoldingLotQuery) Aggregate(fns ...AggregateFunc) *HoldingLotSelect {
	return hlq.Select().Aggregate(fns...)
}

func (hlq *HoldingLotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hlq); err != nil {
				return err
			}
		}
	}
	for _, f := range hlq.ctx.Fields {
		if !holdinglot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hlq.path != nil {
		prev, err := hlq.path(ctx)
		if err != nil {
			return err
		}
		hlq.sql = prev
	}
	return nil
}

func (hlq *HoldingLotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HoldingLot, error) {
	var (
		nodes = []*HoldingLot{}
		_spec = hlq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HoldingLot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HoldingLot{config: hlq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hlq *HoldingLotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hlq.querySpec()
	_spec.Node.Columns = hlq.ctx.Fields
	if len(hlq.ctx.Fields) > 0 {
		_spec.Unique = hlq.ctx.Unique != nil && *hlq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hlq.driver, _spec)
}

func (hlq *HoldingLotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(holdinglot.Table, holdinglot.Columns, sqlgraph.NewFieldSpec(holdinglot.FieldID, field.TypeUUID))
	_spec.From = hlq.sql
	if unique := hlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hlq.path != nil {
		_spec.Unique = true
	}
	if fields := hlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, holdinglot.FieldID)
		for i := range fields {
			if fields[i] != holdinglot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hlq *HoldingLotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hlq.driver.Dialect())
	t1 := builder.Table(holdinglot.Table)
	columns := hlq.ctx.Fields
	if len(columns) == 0 {
		columns = holdinglot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hlq.sql != nil {
		selector = hlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hlq.ctx.Unique != nil && *hlq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hlq.predicates {
		p(selector)
	}
	for _, p := range hlq.order {
		p(selector)
	}
	if offset := hlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HoldingLotGroupBy is the group-by builder for HoldingLot entities.
type HoldingLotGroupBy struct {
	selector
	build *HoldingLotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hlgb *HoldingLotGroupBy) Aggregate(fns ...AggregateFunc) *HoldingLotGroupBy {
	hlgb.fns = append(hlgb.fns, fns...)
	return hlgb
}

// Scan applies the selector query and scans the result into the given value.
func (hlgb *HoldingLotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hlgb.build.ctx, ent.OpQueryGroupBy)
	if err := hlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldingLotQuery, *HoldingLotGroupBy](ctx, hlgb.build, hlgb, hlgb.build.inters, v)
}

func (hlgb *HoldingLotGroupBy) sqlScan(ctx context.Context, root *HoldingLotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hlgb.fns))
	for _, fn := range hlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hlgb.flds)+len(hlgb.fns))
		for _, f := range *hlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HoldingLotSelect is the builder for selecting fields of HoldingLot entities.
type HoldingLotSelect struct {
	*HoldingLotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hls *HoldingLotSelect) Aggregate(fns ...AggregateFunc) *HoldingLotSelect {
	hls.fns = append(hls.fns, fns...)
	return hls
}

// Scan applies the selector query and scans the result into the given value.
func (hls *HoldingLotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hls.ctx, ent.OpQuerySelect)
	if err := hls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldingLotQuery, *HoldingLotSelect](ctx, hls.HoldingLotQuery, hls, hls.inters, v)
}

func (hls *HoldingLotSelect) sqlScan(ctx context.Context, root *HoldingLotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hls.fns))
	for _, fn := range hls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
