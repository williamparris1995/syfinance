// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/holding/ent/holdingsnapshot"
	"github.com/yucai/server/internal/holding/ent/predicate"
)

// HoldingSnapshotQuery is the builder for querying HoldingSnapshot entities.
type HoldingSnapshotQuery struct {
	config
	ctx        *QueryContext
	order      []holdingsnapshot.OrderOption
	inters     []Interceptor
	predicates []predicate.HoldingSnapshot
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HoldingSnapshotQuery builder.
func (hsq *HoldingSnapshotQuery) Where(ps ...predicate.HoldingSnapshot) *HoldingSnapshotQuery {
	hsq.predicates = append(hsq.predicates, ps...)
	return hsq
}

// Limit the number of records to be returned by this query.
func (hsq *HoldingSnapshotQuery) Limit(limit int) *HoldingSnapshotQuery {
	hsq.ctx.Limit = &limit
	return hsq
}

// Offset to start from.
func (hsq *HoldingSnapshotQuery) Offset(offset int) *HoldingSnapshotQuery {
	hsq.ctx.Offset = &offset
	return hsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hsq *HoldingSnapshotQuery) Unique(unique bool) *HoldingSnapshotQuery {
	hsq.ctx.Unique = &unique
	return hsq
}

// Order specifies how the records should be ordered.
func (hsq *HoldingSnapshotQuery) Order(o ...holdingsnapshot.OrderOption) *HoldingSnapshotQuery {
	hsq.order = append(hsq.order, o...)
	return hsq
}

// First returns the first HoldingSnapshot entity from the query.
// Returns a *NotFoundError when no HoldingSnapshot was found.
func (hsq *HoldingSnapshotQuery) First(ctx context.Context) (*HoldingSnapshot, error) {
	nodes, err := hsq.Limit(1).All(setContextOp(ctx, hsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{holdingsnapshot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) FirstX(ctx context.Context) *HoldingSnapshot {
	node, err := hsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HoldingSnapshot ID from the query.
// Returns a *NotFoundError when no HoldingSnapshot ID was found.
func (hsq *HoldingSnapshotQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = hsq.Limit(1).IDs(setContextOp(ctx, hsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{holdingsnapshot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := hsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HoldingSnapshot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HoldingSnapshot entity is found.
// Returns a *NotFoundError when no HoldingSnapshot entities are found.
func (hsq *HoldingSnapshotQuery) Only(ctx context.Context) (*HoldingSnapshot, error) {
	nodes, err := hsq.Limit(2).All(setContextOp(ctx, hsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{holdingsnapshot.Label}
	default:
		return nil, &NotSingularError{holdingsnapshot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) OnlyX(ctx context.Context) *HoldingSnapshot {
	node, err := hsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HoldingSnapshot ID in the query.
// Returns a *NotSingularError when more than one HoldingSnapshot ID is found.
// Returns a *NotFoundError when no entities are found.
func (hsq *HoldingSnapshotQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = hsq.Limit(2).IDs(setContextOp(ctx, hsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{holdingsnapshot.Label}
	default:
		err = &NotSingularError{holdingsnapshot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := hsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HoldingSnapshots.
func (hsq *HoldingSnapshotQuery) All(ctx context.Context) ([]*HoldingSnapshot, error) {
	ctx = setContextOp(ctx, hsq.ctx, ent.OpQueryAll)
	if err := hsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HoldingSnapshot, *HoldingSnapshotQuery]()
	return withInterceptors[[]*HoldingSnapshot](ctx, hsq, qr, hsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) AllX(ctx context.Context) []*HoldingSnapshot {
	nodes, err := hsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HoldingSnapshot IDs.
func (hsq *HoldingSnapshotQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if hsq.ctx.Unique == nil && hsq.path != nil {
		hsq.Unique(true)
	}
	ctx = setContextOp(ctx, hsq.ctx, ent.OpQueryIDs)
	if err = hsq.Select(holdingsnapshot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := hsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hsq *HoldingSnapshotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hsq.ctx, ent.OpQueryCount)
	if err := hsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hsq, querierCount[*HoldingSnapshotQuery](), hsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) CountX(ctx context.Context) int {
	count, err := hsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hsq *HoldingSnapshotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hsq.ctx, ent.OpQueryExist)
	switch _, err := hsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hsq *HoldingSnapshotQuery) ExistX(ctx context.Context) bool {
	exist, err := hsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HoldingSnapshotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hsq *HoldingSnapshotQuery) Clone() *HoldingSnapshotQuery {
	if hsq == nil {
		return nil
	}
	return &HoldingSnapshotQuery{
		config:     hsq.config,
		ctx:        hsq.ctx.Clone(),
		order:      append([]holdingsnapshot.OrderOption{}, hsq.order...),
		inters:     append([]Interceptor{}, hsq.inters...),
		predicates: append([]predicate.HoldingSnapshot{}, hsq.predicates...),
		// clone intermediate query.
		sql:  hsq.sql.Clone(),
		path: hsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HoldingSnapshot.Query().
//		GroupBy(holdingsnapshot.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hsq *HoldingSnapshotQuery) GroupBy(field string, fields ...string) *HoldingSnapshotGroupBy {
	hsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HoldingSnapshotGroupBy{build: hsq}
	grbuild.flds = &hsq.ctx.Fields
	grbuild.label = holdingsnapshot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.HoldingSnapshot.Query().
//		Select(holdingsnapshot.FieldTenantID).
//		Scan(ctx, &v)
func (hsq *HoldingSnapshotQuery) Select(fields ...string) *HoldingSnapshotSelect {
	hsq.ctx.Fields = append(hsq.ctx.Fields, fields...)
	sbuild := &HoldingSnapshotSelect{HoldingSnapshotQuery: hsq}
	sbuild.label = holdingsnapshot.Label
	sbuild.flds, sbuild.scan = &hsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HoldingSnapshotSelect configured with the given aggregations.
func (hsq *HoldingSnapshotQuery) Aggregate(fns ...AggregateFunc) *HoldingSnapshotSelect {
	return hsq.Select().Aggregate(fns...)
}

func (hsq *HoldingSnapshotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hsq); err != nil {
				return err
			}
		}
	}
	for _, f := range hsq.ctx.Fields {
		if !holdingsnapshot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hsq.path != nil {
		prev, err := hsq.path(ctx)
		if err != nil {
			return err
		}
		hsq.sql = prev
	}
	return nil
}

func (hsq *HoldingSnapshotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HoldingSnapshot, error) {
	var (
		nodes = []*HoldingSnapshot{}
		_spec = hsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HoldingSnapshot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HoldingSnapshot{config: hsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hsq *HoldingSnapshotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hsq.querySpec()
	_spec.Node.Columns = hsq.ctx.Fields
	if len(hsq.ctx.Fields) > 0 {
		_spec.Unique = hsq.ctx.Unique != nil && *hsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hsq.driver, _spec)
}

func (hsq *HoldingSnapshotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(holdingsnapshot.Table, holdingsnapshot.Columns, sqlgraph.NewFieldSpec(holdingsnapshot.FieldID, field.TypeUUID))
	_spec.From = hsq.sql
	if unique := hsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hsq.path != nil {
		_spec.Unique = true
	}
	if fields := hsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, holdingsnapshot.FieldID)
		for i := range fields {
			if fields[i] != holdingsnapshot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hsq *HoldingSnapshotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hsq.driver.Dialect())
	t1 := builder.Table(holdingsnapshot.Table)
	columns := hsq.ctx.Fields
	if len(columns) == 0 {
		columns = holdingsnapshot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hsq.sql != nil {
		selector = hsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hsq.ctx.Unique != nil && *hsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hsq.predicates {
		p(selector)
	}
	for _, p := range hsq.order {
		p(selector)
	}
	if offset := hsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HoldingSnapshotGroupBy is the group-by builder for HoldingSnapshot entities.
type HoldingSnapshotGroupBy struct {
	selector
	build *HoldingSnapshotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hsgb *HoldingSnapshotGroupBy) Aggregate(fns ...AggregateFunc) *HoldingSnapshotGroupBy {
	hsgb.fns = append(hsgb.fns, fns...)
	return hsgb
}

// Scan applies the selector query and scans the result into the given value.
func (hsgb *HoldingSnapshotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hsgb.build.ctx, ent.OpQueryGroupBy)
	if err := hsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldingSnapshotQuery, *HoldingSnapshotGroupBy](ctx, hsgb.build, hsgb, hsgb.build.inters, v)
}

func (hsgb *HoldingSnapshotGroupBy) sqlScan(ctx context.Context, root *HoldingSnapshotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hsgb.fns))
	for _, fn := range hsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hsgb.flds)+len(hsgb.fns))
		for _, f := range *hsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HoldingSnapshotSelect is the builder for selecting fields of HoldingSnapshot entities.
type HoldingSnapshotSelect struct {
	*HoldingSnapshotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hss *HoldingSnapshotSelect) Aggregate(fns ...AggregateFunc) *HoldingSnapshotSelect {
	hss.fns = append(hss.fns, fns...)
	return hss
}

// Scan applies the selector query and scans the result into the given value.
func (hss *HoldingSnapshotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hss.ctx, ent.OpQuerySelect)
	if err := hss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HoldingSnapshotQuery, *HoldingSnapshotSelect](ctx, hss.HoldingSnapshotQuery, hss, hss.inters, v)
}

func (hss *HoldingSnapshotSelect) sqlScan(ctx context.Context, root *HoldingSnapshotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hss.fns))
	for _, fn := range hss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
