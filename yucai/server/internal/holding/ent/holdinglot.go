// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/holding/ent/holdinglot"
)

// HoldingLot is the model entity for the HoldingLot schema.
type HoldingLot struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// HoldingID holds the value of the "holding_id" field.
	HoldingID uuid.UUID `json:"holding_id,omitempty"`
	// denormalized
	SecurityID uuid.UUID `json:"security_id,omitempty"`
	// buy trade date; FIFO ordering key
	AcquiredDate time.Time `json:"acquired_date,omitempty"`
	// holding_transaction.id of the buy
	AcquiredTradeID uuid.UUID `json:"acquired_trade_id,omitempty"`
	// buy cost price
	PriceCents int64 `json:"price_cents,omitempty"`
	// original acquired quantity
	Quantity float64 `json:"quantity,omitempty"`
	// remaining after sells/splits
	RemainingQuantity float64 `json:"remaining_quantity,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HoldingLot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case holdinglot.FieldQuantity, holdinglot.FieldRemainingQuantity:
			values[i] = new(sql.NullFloat64)
		case holdinglot.FieldPriceCents:
			values[i] = new(sql.NullInt64)
		case holdinglot.FieldAcquiredDate, holdinglot.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case holdinglot.FieldID, holdinglot.FieldTenantID, holdinglot.FieldHoldingID, holdinglot.FieldSecurityID, holdinglot.FieldAcquiredTradeID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HoldingLot fields.
func (hl *HoldingLot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case holdinglot.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				hl.ID = *value
			}
		case holdinglot.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				hl.TenantID = *value
			}
		case holdinglot.FieldHoldingID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field holding_id", values[i])
			} else if value != nil {
				hl.HoldingID = *value
			}
		case holdinglot.FieldSecurityID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field security_id", values[i])
			} else if value != nil {
				hl.SecurityID = *value
			}
		case holdinglot.FieldAcquiredDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field acquired_date", values[i])
			} else if value.Valid {
				hl.AcquiredDate = value.Time
			}
		case holdinglot.FieldAcquiredTradeID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field acquired_trade_id", values[i])
			} else if value != nil {
				hl.AcquiredTradeID = *value
			}
		case holdinglot.FieldPriceCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field price_cents", values[i])
			} else if value.Valid {
				hl.PriceCents = value.Int64
			}
		case holdinglot.FieldQuantity:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				hl.Quantity = value.Float64
			}
		case holdinglot.FieldRemainingQuantity:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field remaining_quantity", values[i])
			} else if value.Valid {
				hl.RemainingQuantity = value.Float64
			}
		case holdinglot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				hl.CreatedAt = value.Time
			}
		default:
			hl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HoldingLot.
// This includes values selected through modifiers, order, etc.
func (hl *HoldingLot) Value(name string) (ent.Value, error) {
	return hl.selectValues.Get(name)
}

// Update returns a builder for updating this HoldingLot.
// Note that you need to call HoldingLot.Unwrap() before calling this method if this HoldingLot
// was returned from a transaction, and the transaction was committed or rolled back.
func (hl *HoldingLot) Update() *HoldingLotUpdateOne {
	return NewHoldingLotClient(hl.config).UpdateOne(hl)
}

// Unwrap unwraps the HoldingLot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (hl *HoldingLot) Unwrap() *HoldingLot {
	_tx, ok := hl.config.driver.(*txDriver)
	if !ok {
		panic("ent: HoldingLot is not a transactional entity")
	}
	hl.config.driver = _tx.drv
	return hl
}

// String implements the fmt.Stringer.
func (hl *HoldingLot) String() string {
	var builder strings.Builder
	builder.WriteString("HoldingLot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", hl.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", hl.TenantID))
	builder.WriteString(", ")
	builder.WriteString("holding_id=")
	builder.WriteString(fmt.Sprintf("%v", hl.HoldingID))
	builder.WriteString(", ")
	builder.WriteString("security_id=")
	builder.WriteString(fmt.Sprintf("%v", hl.SecurityID))
	builder.WriteString(", ")
	builder.WriteString("acquired_date=")
	builder.WriteString(hl.AcquiredDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("acquired_trade_id=")
	builder.WriteString(fmt.Sprintf("%v", hl.AcquiredTradeID))
	builder.WriteString(", ")
	builder.WriteString("price_cents=")
	builder.WriteString(fmt.Sprintf("%v", hl.PriceCents))
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", hl.Quantity))
	builder.WriteString(", ")
	builder.WriteString("remaining_quantity=")
	builder.WriteString(fmt.Sprintf("%v", hl.RemainingQuantity))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(hl.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// HoldingLots is a parsable slice of HoldingLot.
type HoldingLots []*HoldingLot
