// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/holding/ent/holding"
)

// Holding is the model entity for the Holding schema.
type Holding struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// AccountID holds the value of the "account_id" field.
	AccountID uuid.UUID `json:"account_id,omitempty"`
	// SecurityID holds the value of the "security_id" field.
	SecurityID uuid.UUID `json:"security_id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity float64 `json:"quantity,omitempty"`
	// AvgCostCents holds the value of the "avg_cost_cents" field.
	AvgCostCents int64 `json:"avg_cost_cents,omitempty"`
	// Version holds the value of the "version" field.
	Version int64 `json:"version,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Holding) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case holding.FieldQuantity:
			values[i] = new(sql.NullFloat64)
		case holding.FieldAvgCostCents, holding.FieldVersion:
			values[i] = new(sql.NullInt64)
		case holding.FieldCreatedAt, holding.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case holding.FieldID, holding.FieldTenantID, holding.FieldAccountID, holding.FieldSecurityID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Holding fields.
func (h *Holding) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case holding.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				h.ID = *value
			}
		case holding.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				h.TenantID = *value
			}
		case holding.FieldAccountID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value != nil {
				h.AccountID = *value
			}
		case holding.FieldSecurityID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field security_id", values[i])
			} else if value != nil {
				h.SecurityID = *value
			}
		case holding.FieldQuantity:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				h.Quantity = value.Float64
			}
		case holding.FieldAvgCostCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field avg_cost_cents", values[i])
			} else if value.Valid {
				h.AvgCostCents = value.Int64
			}
		case holding.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				h.Version = value.Int64
			}
		case holding.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				h.CreatedAt = value.Time
			}
		case holding.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				h.UpdatedAt = value.Time
			}
		default:
			h.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Holding.
// This includes values selected through modifiers, order, etc.
func (h *Holding) Value(name string) (ent.Value, error) {
	return h.selectValues.Get(name)
}

// Update returns a builder for updating this Holding.
// Note that you need to call Holding.Unwrap() before calling this method if this Holding
// was returned from a transaction, and the transaction was committed or rolled back.
func (h *Holding) Update() *HoldingUpdateOne {
	return NewHoldingClient(h.config).UpdateOne(h)
}

// Unwrap unwraps the Holding entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (h *Holding) Unwrap() *Holding {
	_tx, ok := h.config.driver.(*txDriver)
	if !ok {
		panic("ent: Holding is not a transactional entity")
	}
	h.config.driver = _tx.drv
	return h
}

// String implements the fmt.Stringer.
func (h *Holding) String() string {
	var builder strings.Builder
	builder.WriteString("Holding(")
	builder.WriteString(fmt.Sprintf("id=%v, ", h.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", h.TenantID))
	builder.WriteString(", ")
	builder.WriteString("account_id=")
	builder.WriteString(fmt.Sprintf("%v", h.AccountID))
	builder.WriteString(", ")
	builder.WriteString("security_id=")
	builder.WriteString(fmt.Sprintf("%v", h.SecurityID))
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", h.Quantity))
	builder.WriteString(", ")
	builder.WriteString("avg_cost_cents=")
	builder.WriteString(fmt.Sprintf("%v", h.AvgCostCents))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", h.Version))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(h.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(h.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Holdings is a parsable slice of Holding.
type Holdings []*Holding
