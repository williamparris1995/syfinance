// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/sync/ent/synclog"
)

// SyncLog is the model entity for the SyncLog schema.
type SyncLog struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// account, transaction, budget, etc.
	EntityType string `json:"entity_type,omitempty"`
	// ID of the changed entity
	EntityID uuid.UUID `json:"entity_id,omitempty"`
	// create, update, delete
	Operation string `json:"operation,omitempty"`
	// Serialized entity JSON
	Payload []byte `json:"payload,omitempty"`
	// Monotonically increasing per tenant
	Version int64 `json:"version,omitempty"`
	// Device that originated the change
	DeviceID uuid.UUID `json:"device_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case synclog.FieldPayload:
			values[i] = new([]byte)
		case synclog.FieldVersion:
			values[i] = new(sql.NullInt64)
		case synclog.FieldEntityType, synclog.FieldOperation:
			values[i] = new(sql.NullString)
		case synclog.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case synclog.FieldID, synclog.FieldTenantID, synclog.FieldEntityID, synclog.FieldDeviceID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncLog fields.
func (sl *SyncLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case synclog.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sl.ID = *value
			}
		case synclog.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sl.TenantID = *value
			}
		case synclog.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				sl.EntityType = value.String
			}
		case synclog.FieldEntityID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value != nil {
				sl.EntityID = *value
			}
		case synclog.FieldOperation:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field operation", values[i])
			} else if value.Valid {
				sl.Operation = value.String
			}
		case synclog.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil {
				sl.Payload = *value
			}
		case synclog.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				sl.Version = value.Int64
			}
		case synclog.FieldDeviceID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value != nil {
				sl.DeviceID = *value
			}
		case synclog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sl.CreatedAt = value.Time
			}
		default:
			sl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SyncLog.
// This includes values selected through modifiers, order, etc.
func (sl *SyncLog) Value(name string) (ent.Value, error) {
	return sl.selectValues.Get(name)
}

// Update returns a builder for updating this SyncLog.
// Note that you need to call SyncLog.Unwrap() before calling this method if this SyncLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (sl *SyncLog) Update() *SyncLogUpdateOne {
	return NewSyncLogClient(sl.config).UpdateOne(sl)
}

// Unwrap unwraps the SyncLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sl *SyncLog) Unwrap() *SyncLog {
	_tx, ok := sl.config.driver.(*txDriver)
	if !ok {
		panic("ent: SyncLog is not a transactional entity")
	}
	sl.config.driver = _tx.drv
	return sl
}

// String implements the fmt.Stringer.
func (sl *SyncLog) String() string {
	var builder strings.Builder
	builder.WriteString("SyncLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sl.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sl.TenantID))
	builder.WriteString(", ")
	builder.WriteString("entity_type=")
	builder.WriteString(sl.EntityType)
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(fmt.Sprintf("%v", sl.EntityID))
	builder.WriteString(", ")
	builder.WriteString("operation=")
	builder.WriteString(sl.Operation)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", sl.Payload))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", sl.Version))
	builder.WriteString(", ")
	builder.WriteString("device_id=")
	builder.WriteString(fmt.Sprintf("%v", sl.DeviceID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sl.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SyncLogs is a parsable slice of SyncLog.
type SyncLogs []*SyncLog
