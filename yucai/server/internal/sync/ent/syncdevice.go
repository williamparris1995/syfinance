// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/sync/ent/syncdevice"
)

// SyncDevice is the model entity for the SyncDevice schema.
type SyncDevice struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to tenants table — data isolation boundary
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// DeviceName holds the value of the "device_name" field.
	DeviceName string `json:"device_name,omitempty"`
	// LastSyncVersion holds the value of the "last_sync_version" field.
	LastSyncVersion int64 `json:"last_sync_version,omitempty"`
	// LastSyncAt holds the value of the "last_sync_at" field.
	LastSyncAt time.Time `json:"last_sync_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SyncDevice) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case syncdevice.FieldLastSyncVersion:
			values[i] = new(sql.NullInt64)
		case syncdevice.FieldDeviceName:
			values[i] = new(sql.NullString)
		case syncdevice.FieldLastSyncAt, syncdevice.FieldCreatedAt, syncdevice.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case syncdevice.FieldID, syncdevice.FieldTenantID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SyncDevice fields.
func (sd *SyncDevice) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case syncdevice.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sd.ID = *value
			}
		case syncdevice.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				sd.TenantID = *value
			}
		case syncdevice.FieldDeviceName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field device_name", values[i])
			} else if value.Valid {
				sd.DeviceName = value.String
			}
		case syncdevice.FieldLastSyncVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_version", values[i])
			} else if value.Valid {
				sd.LastSyncVersion = value.Int64
			}
		case syncdevice.FieldLastSyncAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_sync_at", values[i])
			} else if value.Valid {
				sd.LastSyncAt = value.Time
			}
		case syncdevice.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sd.CreatedAt = value.Time
			}
		case syncdevice.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sd.UpdatedAt = value.Time
			}
		default:
			sd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SyncDevice.
// This includes values selected through modifiers, order, etc.
func (sd *SyncDevice) Value(name string) (ent.Value, error) {
	return sd.selectValues.Get(name)
}

// Update returns a builder for updating this SyncDevice.
// Note that you need to call SyncDevice.Unwrap() before calling this method if this SyncDevice
// was returned from a transaction, and the transaction was committed or rolled back.
func (sd *SyncDevice) Update() *SyncDeviceUpdateOne {
	return NewSyncDeviceClient(sd.config).UpdateOne(sd)
}

// Unwrap unwraps the SyncDevice entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sd *SyncDevice) Unwrap() *SyncDevice {
	_tx, ok := sd.config.driver.(*txDriver)
	if !ok {
		panic("ent: SyncDevice is not a transactional entity")
	}
	sd.config.driver = _tx.drv
	return sd
}

// String implements the fmt.Stringer.
func (sd *SyncDevice) String() string {
	var builder strings.Builder
	builder.WriteString("SyncDevice(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sd.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", sd.TenantID))
	builder.WriteString(", ")
	builder.WriteString("device_name=")
	builder.WriteString(sd.DeviceName)
	builder.WriteString(", ")
	builder.WriteString("last_sync_version=")
	builder.WriteString(fmt.Sprintf("%v", sd.LastSyncVersion))
	builder.WriteString(", ")
	builder.WriteString("last_sync_at=")
	builder.WriteString(sd.LastSyncAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sd.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sd.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SyncDevices is a parsable slice of SyncDevice.
type SyncDevices []*SyncDevice
