// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/budget/ent/budgetitem"
	"github.com/yucai/server/internal/budget/ent/predicate"
)

// BudgetItemQuery is the builder for querying BudgetItem entities.
type BudgetItemQuery struct {
	config
	ctx        *QueryContext
	order      []budgetitem.OrderOption
	inters     []Interceptor
	predicates []predicate.BudgetItem
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BudgetItemQuery builder.
func (biq *BudgetItemQuery) Where(ps ...predicate.BudgetItem) *BudgetItemQuery {
	biq.predicates = append(biq.predicates, ps...)
	return biq
}

// Limit the number of records to be returned by this query.
func (biq *BudgetItemQuery) Limit(limit int) *BudgetItemQuery {
	biq.ctx.Limit = &limit
	return biq
}

// Offset to start from.
func (biq *BudgetItemQuery) Offset(offset int) *BudgetItemQuery {
	biq.ctx.Offset = &offset
	return biq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (biq *BudgetItemQuery) Unique(unique bool) *BudgetItemQuery {
	biq.ctx.Unique = &unique
	return biq
}

// Order specifies how the records should be ordered.
func (biq *BudgetItemQuery) Order(o ...budgetitem.OrderOption) *BudgetItemQuery {
	biq.order = append(biq.order, o...)
	return biq
}

// First returns the first BudgetItem entity from the query.
// Returns a *NotFoundError when no BudgetItem was found.
func (biq *BudgetItemQuery) First(ctx context.Context) (*BudgetItem, error) {
	nodes, err := biq.Limit(1).All(setContextOp(ctx, biq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{budgetitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (biq *BudgetItemQuery) FirstX(ctx context.Context) *BudgetItem {
	node, err := biq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BudgetItem ID from the query.
// Returns a *NotFoundError when no BudgetItem ID was found.
func (biq *BudgetItemQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = biq.Limit(1).IDs(setContextOp(ctx, biq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{budgetitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (biq *BudgetItemQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := biq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BudgetItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BudgetItem entity is found.
// Returns a *NotFoundError when no BudgetItem entities are found.
func (biq *BudgetItemQuery) Only(ctx context.Context) (*BudgetItem, error) {
	nodes, err := biq.Limit(2).All(setContextOp(ctx, biq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{budgetitem.Label}
	default:
		return nil, &NotSingularError{budgetitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (biq *BudgetItemQuery) OnlyX(ctx context.Context) *BudgetItem {
	node, err := biq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BudgetItem ID in the query.
// Returns a *NotSingularError when more than one BudgetItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (biq *BudgetItemQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = biq.Limit(2).IDs(setContextOp(ctx, biq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{budgetitem.Label}
	default:
		err = &NotSingularError{budgetitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (biq *BudgetItemQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := biq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BudgetItems.
func (biq *BudgetItemQuery) All(ctx context.Context) ([]*BudgetItem, error) {
	ctx = setContextOp(ctx, biq.ctx, ent.OpQueryAll)
	if err := biq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BudgetItem, *BudgetItemQuery]()
	return withInterceptors[[]*BudgetItem](ctx, biq, qr, biq.inters)
}

// AllX is like All, but panics if an error occurs.
func (biq *BudgetItemQuery) AllX(ctx context.Context) []*BudgetItem {
	nodes, err := biq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BudgetItem IDs.
func (biq *BudgetItemQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if biq.ctx.Unique == nil && biq.path != nil {
		biq.Unique(true)
	}
	ctx = setContextOp(ctx, biq.ctx, ent.OpQueryIDs)
	if err = biq.Select(budgetitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (biq *BudgetItemQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := biq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (biq *BudgetItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, biq.ctx, ent.OpQueryCount)
	if err := biq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, biq, querierCount[*BudgetItemQuery](), biq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (biq *BudgetItemQuery) CountX(ctx context.Context) int {
	count, err := biq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (biq *BudgetItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, biq.ctx, ent.OpQueryExist)
	switch _, err := biq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (biq *BudgetItemQuery) ExistX(ctx context.Context) bool {
	exist, err := biq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BudgetItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (biq *BudgetItemQuery) Clone() *BudgetItemQuery {
	if biq == nil {
		return nil
	}
	return &BudgetItemQuery{
		config:     biq.config,
		ctx:        biq.ctx.Clone(),
		order:      append([]budgetitem.OrderOption{}, biq.order...),
		inters:     append([]Interceptor{}, biq.inters...),
		predicates: append([]predicate.BudgetItem{}, biq.predicates...),
		// clone intermediate query.
		sql:  biq.sql.Clone(),
		path: biq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BudgetID uuid.UUID `json:"budget_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BudgetItem.Query().
//		GroupBy(budgetitem.FieldBudgetID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (biq *BudgetItemQuery) GroupBy(field string, fields ...string) *BudgetItemGroupBy {
	biq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BudgetItemGroupBy{build: biq}
	grbuild.flds = &biq.ctx.Fields
	grbuild.label = budgetitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BudgetID uuid.UUID `json:"budget_id,omitempty"`
//	}
//
//	client.BudgetItem.Query().
//		Select(budgetitem.FieldBudgetID).
//		Scan(ctx, &v)
func (biq *BudgetItemQuery) Select(fields ...string) *BudgetItemSelect {
	biq.ctx.Fields = append(biq.ctx.Fields, fields...)
	sbuild := &BudgetItemSelect{BudgetItemQuery: biq}
	sbuild.label = budgetitem.Label
	sbuild.flds, sbuild.scan = &biq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BudgetItemSelect configured with the given aggregations.
func (biq *BudgetItemQuery) Aggregate(fns ...AggregateFunc) *BudgetItemSelect {
	return biq.Select().Aggregate(fns...)
}

func (biq *BudgetItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range biq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, biq); err != nil {
				return err
			}
		}
	}
	for _, f := range biq.ctx.Fields {
		if !budgetitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if biq.path != nil {
		prev, err := biq.path(ctx)
		if err != nil {
			return err
		}
		biq.sql = prev
	}
	return nil
}

func (biq *BudgetItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BudgetItem, error) {
	var (
		nodes = []*BudgetItem{}
		_spec = biq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BudgetItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BudgetItem{config: biq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, biq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (biq *BudgetItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := biq.querySpec()
	_spec.Node.Columns = biq.ctx.Fields
	if len(biq.ctx.Fields) > 0 {
		_spec.Unique = biq.ctx.Unique != nil && *biq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, biq.driver, _spec)
}

func (biq *BudgetItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(budgetitem.Table, budgetitem.Columns, sqlgraph.NewFieldSpec(budgetitem.FieldID, field.TypeUUID))
	_spec.From = biq.sql
	if unique := biq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if biq.path != nil {
		_spec.Unique = true
	}
	if fields := biq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, budgetitem.FieldID)
		for i := range fields {
			if fields[i] != budgetitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := biq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := biq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := biq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := biq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (biq *BudgetItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(biq.driver.Dialect())
	t1 := builder.Table(budgetitem.Table)
	columns := biq.ctx.Fields
	if len(columns) == 0 {
		columns = budgetitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if biq.sql != nil {
		selector = biq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if biq.ctx.Unique != nil && *biq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range biq.predicates {
		p(selector)
	}
	for _, p := range biq.order {
		p(selector)
	}
	if offset := biq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := biq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BudgetItemGroupBy is the group-by builder for BudgetItem entities.
type BudgetItemGroupBy struct {
	selector
	build *BudgetItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bigb *BudgetItemGroupBy) Aggregate(fns ...AggregateFunc) *BudgetItemGroupBy {
	bigb.fns = append(bigb.fns, fns...)
	return bigb
}

// Scan applies the selector query and scans the result into the given value.
func (bigb *BudgetItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bigb.build.ctx, ent.OpQueryGroupBy)
	if err := bigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BudgetItemQuery, *BudgetItemGroupBy](ctx, bigb.build, bigb, bigb.build.inters, v)
}

func (bigb *BudgetItemGroupBy) sqlScan(ctx context.Context, root *BudgetItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bigb.fns))
	for _, fn := range bigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bigb.flds)+len(bigb.fns))
		for _, f := range *bigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BudgetItemSelect is the builder for selecting fields of BudgetItem entities.
type BudgetItemSelect struct {
	*BudgetItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bis *BudgetItemSelect) Aggregate(fns ...AggregateFunc) *BudgetItemSelect {
	bis.fns = append(bis.fns, fns...)
	return bis
}

// Scan applies the selector query and scans the result into the given value.
func (bis *BudgetItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bis.ctx, ent.OpQuerySelect)
	if err := bis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BudgetItemQuery, *BudgetItemSelect](ctx, bis.BudgetItemQuery, bis, bis.inters, v)
}

func (bis *BudgetItemSelect) sqlScan(ctx context.Context, root *BudgetItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bis.fns))
	for _, fn := range bis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
