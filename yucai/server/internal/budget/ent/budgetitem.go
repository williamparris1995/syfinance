// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yucai/server/internal/budget/ent/budgetitem"
)

// BudgetItem is the model entity for the BudgetItem schema.
type BudgetItem struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// FK to Budget
	BudgetID uuid.UUID `json:"budget_id,omitempty"`
	// FK to Account
	AccountID uuid.UUID `json:"account_id,omitempty"`
	// PlannedAmountCents holds the value of the "planned_amount_cents" field.
	PlannedAmountCents int64 `json:"planned_amount_cents,omitempty"`
	// ActualAmountCents holds the value of the "actual_amount_cents" field.
	ActualAmountCents int64 `json:"actual_amount_cents,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes        string `json:"notes,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BudgetItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case budgetitem.FieldPlannedAmountCents, budgetitem.FieldActualAmountCents:
			values[i] = new(sql.NullInt64)
		case budgetitem.FieldNotes:
			values[i] = new(sql.NullString)
		case budgetitem.FieldID, budgetitem.FieldBudgetID, budgetitem.FieldAccountID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BudgetItem fields.
func (bi *BudgetItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case budgetitem.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				bi.ID = *value
			}
		case budgetitem.FieldBudgetID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field budget_id", values[i])
			} else if value != nil {
				bi.BudgetID = *value
			}
		case budgetitem.FieldAccountID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value != nil {
				bi.AccountID = *value
			}
		case budgetitem.FieldPlannedAmountCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field planned_amount_cents", values[i])
			} else if value.Valid {
				bi.PlannedAmountCents = value.Int64
			}
		case budgetitem.FieldActualAmountCents:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field actual_amount_cents", values[i])
			} else if value.Valid {
				bi.ActualAmountCents = value.Int64
			}
		case budgetitem.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				bi.Notes = value.String
			}
		default:
			bi.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BudgetItem.
// This includes values selected through modifiers, order, etc.
func (bi *BudgetItem) Value(name string) (ent.Value, error) {
	return bi.selectValues.Get(name)
}

// Update returns a builder for updating this BudgetItem.
// Note that you need to call BudgetItem.Unwrap() before calling this method if this BudgetItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (bi *BudgetItem) Update() *BudgetItemUpdateOne {
	return NewBudgetItemClient(bi.config).UpdateOne(bi)
}

// Unwrap unwraps the BudgetItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bi *BudgetItem) Unwrap() *BudgetItem {
	_tx, ok := bi.config.driver.(*txDriver)
	if !ok {
		panic("ent: BudgetItem is not a transactional entity")
	}
	bi.config.driver = _tx.drv
	return bi
}

// String implements the fmt.Stringer.
func (bi *BudgetItem) String() string {
	var builder strings.Builder
	builder.WriteString("BudgetItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bi.ID))
	builder.WriteString("budget_id=")
	builder.WriteString(fmt.Sprintf("%v", bi.BudgetID))
	builder.WriteString(", ")
	builder.WriteString("account_id=")
	builder.WriteString(fmt.Sprintf("%v", bi.AccountID))
	builder.WriteString(", ")
	builder.WriteString("planned_amount_cents=")
	builder.WriteString(fmt.Sprintf("%v", bi.PlannedAmountCents))
	builder.WriteString(", ")
	builder.WriteString("actual_amount_cents=")
	builder.WriteString(fmt.Sprintf("%v", bi.ActualAmountCents))
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(bi.Notes)
	builder.WriteByte(')')
	return builder.String()
}

// BudgetItems is a parsable slice of BudgetItem.
type BudgetItems []*BudgetItem
